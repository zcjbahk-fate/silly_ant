# 更新日志

## v1.03

- 规范：全面统一世界书 XML 语义化标签（`worldinfo_`、`dungeon_`、`rule_`），修复各副本与世界观条目之间的标签风格断层
- 修复：彻底修复 `城市.txt` 中会展中心、大教堂、富人区、黑市、蛾衣等核心地点的多级缩进断裂与错误嵌套，消除 AI 地理认知偏差
- 修复：全局将各条目中的硬编码 `user` / `User` / `主角` 替换为 `{{user}}` 宏，保证酒馆运行时玩家名称动态渲染
- 修复：补齐 `改造剧情指导` 第 40 行末尾文本截断，修正第 248 行叠词 typo，规范各步骤序号
- 修复：清除 `过往角色速览2` 中因历史替换残留的 `<user>晨` 错别字（恢复为 `凌晨`）
- 优化：移除 `副本列表.txt` 硬编码分割线，规范全系条目 YAML 缩进与层级结构

## v1.02

- 修复：在 Zod Schema（变量结构尸检）中补齐 `在场角色列表` 校验与 `部位.淫纹` 字段，确保数据类型与输出规范完整对齐
- 修复：调整深度 0 处的 `新手教程` 执行顺序至 206，消除与变量输出格式条目的顺序号冲突
- 优化：剔除 `防全知防出戏` 中的重复前言声明
- 瘦身：清理世界书与正则中废弃的旧版本文件（旧城市、旧供销关系、v2.0状态栏、尸检面板1.1及重复脚本引用），大幅精简包体

## v1.01

- 修复：开场白控制台面板支持动态自适应识别世界书名称，彻底解耦角色卡改名对世界书条目控制的影响

## v1.00

- 初版发布
