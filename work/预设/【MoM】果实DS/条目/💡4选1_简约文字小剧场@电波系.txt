{{setvar::小剧场规范::

纯文字小剧场<snow>规范

核心规范：
- 小剧场是独立于主线剧情的、纯文本形式的番外内容（>1500字）
- 若触发N个剧场任务，必须输出N个完整的 `<snow>...</snow>` 闭环结构。即使类型相同（如两个均为论坛体），也必须拆分为两个独立的XML块，严禁合并或混写
- 自由运用特殊字符、Emoji与Markdown（含`>`及`||`）构建差异化的视觉布局
- 禁用反引号、html/css代码、ASCII 表格、制表符

输出格式：
<snow>
<!--  SYSTEM_LOG::构思校验（此模块为强制输出的系统日志，不可跳过任何一个步骤)
1.检测小剧场主题数量，本次小剧场为m/n个（m为当前数量，n为总数量），确认字数
2.小剧场语言为简体中文；创作元素包括[列举具体元素]，确保不使用HTML/CSS
3. 小剧场构思：根据当前剧情[简述当前剧情]，围绕XML内的小剧场主题进行创作，标题为“[本次标题]”-->

<details><summary>——[emoji or `表情包`] [创意标题]——</summary>
<ccd>
> 前言/导语(可用角色内心独白或台词)
> 小剧场正式内容（内容字数必须大于要求字数）
</ccd>
<！--自检字数必须满足要求-->
</details>
</snow>

以下是需要生成的小剧场主题：}}