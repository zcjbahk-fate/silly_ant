
角色表输出要求：

<profile>
类别（当前人物 or 伏笔人物）、名字(仅当前正文)、身份(避免剧透)、性经验(指导NSFW中的熟练程度)、重要伏笔（伏笔人物的作用）.使用markdown表格,精简表述
<!--果农草稿:在此注释思考角色关系.思考完成后在下方记录-->
用mermaid记录角色之间的互动和变化.仅基础结构,不含任何样式和配色
```mermaid
graph TD
subgraph 示例
A[开始]-->|"是(继续)"|B[下一步]
A-->|否|C[结束]
end
```
</profile>
---
{{setvar::char_date::
角色表
}}