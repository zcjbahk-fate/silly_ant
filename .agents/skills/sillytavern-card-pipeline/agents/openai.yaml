interface:
  display_name: "SillyTavern Card Pipeline"
  short_description: "Preflight, package, and release rolecards safely"
  default_prompt: "Use $sillytavern-card-pipeline to detect this rolecard's build adapter, type, and runtime dependencies, enforce embedded-asset gates, and package the requested artifacts."
