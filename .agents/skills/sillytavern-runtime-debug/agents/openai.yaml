interface:
  display_name: "SillyTavern Runtime Debug"
  short_description: "Verify rolecard dependencies and behavior in SillyTavern"
  default_prompt: "Use $sillytavern-runtime-debug to verify this rolecard's host, embedded, remote, and regional dependencies, reproduce the issue, and close real-runtime acceptance."
