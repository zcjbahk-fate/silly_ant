# 移动端与响应式适配

> 文档版本：v1.0.0 ｜ 最后更新：2026-07-14

> ST 角色卡移动端与响应式适配参考手册。涵盖 ST 宿主层断点与 CSS 变量、卡片层 iframe 高度同步、触控事件、媒体查询、安全区适配，以及已验证的生产范式代码与高频避坑。

---

## 置信度分级体系

| 级别 | 含义 | 施工许可 |
|---|---|---|
| **high** | 多源或运行时源码直接印证 | 可直接依赖 |
| **medium** | 单源或逻辑推断，尚无真机全覆盖 | 可用，建议加 fallback |
| **low** | 孤证无来源，纯推断 | **禁止作为施工依据，需先真机验证** |

依据类型轴：`[运行时源码]` / `[真机]` / `[文档·多源]` / `[文档·单源]` / `[社区]` / `[推断]`

收集日期：2026-06-28。回源基于 SillyTavern v1.14.0 + 本地成熟卡（星月 2.5.0 / 星月 3.0.2）。

---

## 目录

1. 机制总览
   - 1.1 ST 宿主层（非卡片作者控制）
   - 1.2 卡片层（角色卡作者可控）
2. 关键字段与 API 参考表
3. 完整可用范式代码
   - 3.1 iframe 高度自适应（星月 3.0.x 生产用法）
   - 3.2 响应式 CSS 变量设计系统（星月 2.5.0 生产用法）
   - 3.3 viewport 感知的弹出框定位（min() 函数防溢出）
   - 3.4 媒体查询断点（卡片内）
   - 3.5 触控事件正确绑定
   - 3.6 动态网格宽度检测（容器查询替代方案）
4. 高频避坑
5. ST 宿主 CSS 变量速查
6. 悬案与验证路径
7. 来源索引

---

## 1. 机制总览

### 1.1 ST 宿主层（非卡片作者控制）

SillyTavern 宿主在移动端的行为由五个子机制决定。

#### A. 视口声明（index.html）

```html
<meta name="viewport" content="width=device-width, viewport-fit=cover,
  initial-scale=1, maximum-scale=1.0, user-scalable=no,
  interactive-widget=resizes-content">
<meta name="apple-mobile-web-app-capable" content="yes">
<meta name="mobile-web-app-capable" content="yes">
```

`user-scalable=no` 禁止用户手势缩放；`interactive-widget=resizes-content` 使虚拟键盘弹出时 viewport 收缩而非仅覆盖内容，整体布局随之压缩。

**来源：** ST v1.14.0 `public/index.html` | **置信度：high** `[运行时源码]`

---

#### B. 移动端主断点：1000px

ST 在 `mobile-styles.css` 里以 `@media (max-width: 1000px)` 为核心断点做全面 reflow：

- 所有侧边栏切换为 `width: 100dvw; max-width: 100dvw; position: fixed; left: 0`（覆盖全屏抽屉）
- 面板 pin 按钮隐藏（`display: none`）
- body 固定：`touch-action: none; overflow: hidden; position: fixed`
- 导航面板强制 `min-width: 100dvw !important`
- 次断点 `@media (max-width: 450px)` 管窄屏手机（如 320px 竖屏）额外调整

**来源：** ST v1.14.0 `public/css/mobile-styles.css` | **置信度：high** `[运行时源码]`

---

#### C. iOS 专项

```css
@supports (-webkit-touch-callout: none) {
  #sheld {
    padding-right: max(env(safe-area-inset-right), 0px);
    padding-left:  max(env(safe-area-inset-left),  0px);
    padding-bottom: 0;
  }
  /* PWA 模式（body.PWA 类）额外应用底部安全区 */
  body.PWA #sheld {
    padding-right:  max(env(safe-area-inset-right), 2px);
    padding-left:   max(env(safe-area-inset-left),  2px);
    padding-bottom: max(env(safe-area-inset-bottom), 15px);
  }
}
```

`env(safe-area-inset-*)` 处理 iPhone 刘海与 Home 条的安全区，防止内容被系统 UI 遮挡。PWA 模式（网页添加到主屏幕后）额外补偿底部安全区 15px。

> **校准备注（2026-06-28）：** 原草稿将 PWA 底部补偿注释掉，与源码不符——实际是 `body.PWA #sheld` 规则，非注释。

**来源：** ST v1.14.0 `public/css/mobile-styles.css` 第 498–570 行 | **置信度：high** `[运行时源码]`

---

#### D. JS 层动态 CSS 变量

```javascript
// power-user.js（宿主启动时注入到 document.documentElement）
document.documentElement.style.setProperty('--sheldWidth', `${power_user.chat_width}vw`);
document.documentElement.style.setProperty('--fontScale', String(power_user.font_scale));
document.documentElement.style.setProperty('--blurStrength', String(power_user.blur_strength));
```

`--sheldWidth` 控制聊天窗宽度（桌面端默认 50vw），在移动端因 `mobile-styles.css` 覆盖为 `100dvw`，卡片内一般不需要另行处理宽度。

**来源：** ST v1.14.0 `public/scripts/power-user.js` | **置信度：high** `[运行时源码]`

---

#### E. 移动端 UI 限制

- `isMobile()` 函数（`export function`，底层使用 Bowser 库做 UA 检测，定义于 `RossAscends-mods.js`）：禁用 movingUI（可拖拽面板）。**「卡片内无法调用」为推断（module scope 隔离），未真机验证。** `[运行时源码]·high` / `[推断]·medium`
- 横屏（landscape）额外 media query 调整表情图片 `object-fit` 和 `zoomed-avatar` 定位 `[运行时源码]·high`
- ST 官方唯一标注"移动端专属"的 UI 选项：紧凑输入框（单行 `<input>` 模式）`[文档·单源]·medium`

**来源：** ST v1.14.0 `RossAscends-mods.js` + `mobile-styles.css` + ST 文档 | **置信度（分项见上）：整体 medium** `[运行时源码]` `[推断]`

---

### 1.2 卡片层（角色卡作者可控）

卡片的 HTML 内容（通过正则扩展替换）被注入到 `.mes_text` 内。ST v1.14.0 源码中**不存在由 ST 框架层创建 iframe 的逻辑**（已 grep 全部 .js 文件确认）；卡片内的 `<iframe>` 是卡片自身 HTML 创建的（如挂载壳自含 `<iframe id="xy-sb-inner">`）。这个卡片自创的 iframe 在 ST 聊天气泡内运行，其 `window.frameElement` 返回的是 ST `.mes_text` 内卡片 HTML 自己的外层 iframe 元素，从而卡片 JS 可控制其高度。**ST 框架层不施加 `sandbox` 属性**（源码层无该逻辑），卡片 iframe 可自由访问 `document`。

**来源：** 本地实战生产卡 3.0.2（status_bar_regex.html 实际代码）+ ST v1.14.0 全脚本 grep 确认 | **置信度：medium·单源孤证** `[运行时源码]`

> **校准备注（2026-06-28）：** 原描述「ST 以 iframe 形式渲染」有误——是卡片 HTML 自含 iframe，不是 ST 框架创建。sandbox 问题（原悬案 U2）经源码 grep 可排除「ST 主动施加 sandbox」，但浏览器自身对 blob: 来源的限制行为未做真机测试，保留 U2 供验证。

---

## 2. 关键字段与 API 参考表

| 名称 | 类型 | 值 / 说明 | 来源 | 置信度 |
|---|---|---|---|---|
| `--sheldWidth` | CSS var（宿主） | 默认 `50vw`，移动端被覆盖为 `100dvw` | 运行时源码 | high |
| `--SmartThemeBorderColor` | CSS var（宿主） | 当前主题边框色（由 `power-user.js` `applyThemeColor` 注入） | 运行时源码 | high |
| `--SmartThemeBodyColor` | CSS var（宿主） | 主文本色（由 `power-user.js` `applyThemeColor` 注入） | 运行时源码 | high |
| `--SmartThemeBlurStrength` | CSS var（宿主，派生变量） | backdrop-filter 系数（`= calc(var(--blurStrength) * 1px)`，由 `power-user.js` 注入的 `--blurStrength` 数值派生） | 运行时源码 | high |
| `--fontScale` | CSS var（宿主） | 用户字体缩放比例 0.5–1.5 | 运行时源码 | high |
| `window.frameElement` | JS API | 取外层 iframe DOM 元素，可强制设 `style.height` | 本地实战源码 | high |
| `ResizeObserver` | Web API | 监听内层 body 尺寸变化来持续同步 iframe 高度 | 本地实战源码 | high |
| `isMobile()` | ST 内部函数（`RossAscends-mods.js` export） | 定义在宿主脚本，卡片 iframe 内 JS 无法直接访问宿主 module scope，需用替代方案 | 运行时源码+推断 | medium·推导未验证 |
| `@media (max-width: 1000px)` | ST 主断点 | 宿主全面 reflow 触发点 | 运行时源码 | high |
| `@media (max-width: 450px)` | ST 次断点 | 窄屏手机额外调整 | 运行时源码 | high |
| `@media (max-width: 600px)` | 卡片内断点（生产实战） | Tab 按钮从 8 列改为 4 列 | 本地卡片源码 | high |
| `env(safe-area-inset-*)` | CSS 函数 | iOS 刘海/Home 条安全区补偿 | 运行时源码 | high |
| `100dvw / 100dvh` | CSS 单位 | 动态 viewport，不受浏览器 chrome 高度影响 | 运行时源码 | high |
| `-webkit-overflow-scrolling: touch` | CSS 属性 | iOS iframe 内平滑滚动（旧式写法，新版已弃用） | 社区 | medium |

---

## 3. 完整可用范式代码

### 3.1 iframe 高度自适应（星月 3.0.x 生产用法）

**来源：** `ST开发指南DB/本地证据/星月历史/3.0.2/components/status_bar_regex.html` | **置信度：high** `[运行时源码]`

机制：外层「挂载壳」fetch 远程真实 HTML → Blob URL 挂到内层 iframe → ResizeObserver 持续同步高度到 ST 控制的最外层 iframe。

```html
<!-- 挂载壳 body -->
<style>
  html, body { margin: 0; padding: 0; overflow: hidden; }
  #xy-sb-inner {
    display: block;
    width: 100%;
    border: 0;
    overflow: hidden;
  }
</style>

<iframe id="xy-sb-inner" scrolling="no"></iframe>

<script>
(function () {
  var inner = document.getElementById('xy-sb-inner');

  /* ── 高度同步：把内层 iframe 真实高度同步到 ST 外层 iframe ── */
  function syncHeight() {
    try {
      var fe = window.frameElement;   // ST 控制的外层 iframe 元素
      if (!fe) return;
      var doc = inner.contentDocument ||
                (inner.contentWindow && inner.contentWindow.document);
      if (!doc) return;

      /* 量高前禁横向溢出，避免横向滚动条撑高 */
      try {
        if (doc.body) {
          doc.body.style.overflowX = 'hidden';
          doc.body.style.maxWidth  = '100%';
        }
      } catch (_) {}

      var h = Math.max(
        doc.documentElement ? doc.documentElement.scrollHeight : 0,
        doc.body            ? doc.body.scrollHeight            : 0
      );

      if (h > 0 && Math.abs((parseInt(fe.style.height, 10) || 0) - h) > 2) {
        fe.style.height    = h + 'px';
        fe.style.minHeight = h + 'px';
        inner.style.height = h + 'px';
        try { fe.setAttribute('height', String(h)); } catch (_) {}
      }
    } catch (_) {}
  }

  function mountInner(html) {
    var blob    = new Blob([html], { type: 'text/html' });
    var blobUrl = URL.createObjectURL(blob);

    inner.addEventListener('load', function onLoad() {
      inner.removeEventListener('load', onLoad);
      syncHeight();
      // 异步渲染补偿（内部脚本延迟填充内容）
      [100, 400, 900, 1800, 3000].forEach(function (t) {
        setTimeout(syncHeight, t);
      });
      // ResizeObserver 持续同步
      try {
        var doc = inner.contentDocument ||
                  (inner.contentWindow && inner.contentWindow.document);
        if (doc && doc.body) new ResizeObserver(syncHeight).observe(doc.body);
      } catch (_) {}
    });

    inner.src = blobUrl;
  }

  // 顺序尝试主 CDN → 备源 → fail
  fetch('https://cdn.example.com/status-bar.html')
    .then(function (r) { if (!r.ok) throw new Error('HTTP ' + r.status); return r.text(); })
    .then(function (html) { mountInner(html); })
    .catch(function () { /* 备源逻辑 */ });
})();
</script>
```

**关键要点：**

- 量高前先强制 `overflowX: hidden; maxWidth: 100%`，避免横向滚动条干扰 scrollHeight
- 容差 `> 2px` 防止高频无意义 DOM 写入
- 多个延迟 `setTimeout([100, 400, 900, 1800, 3000])` 补偿内部异步渲染（动画/数据填充）
- `ResizeObserver` 兜底持续同步，覆盖内容动态变化

---

### 3.2 响应式 CSS 变量设计系统（星月 2.5.0 生产用法）

**来源：** `星月/星月 2.5.0/components/status_bar_regex_beauty_r5.html` | **置信度：high** `[运行时源码]`

核心思路：**所有尺寸锚定到一个根变量 `--hud-base-font`**，用户通过 settings slider 调整后自动缩放所有元素。

```css
:root {
  --hud-base-font:  12px;   /* 锚点；用户可通过设置滑块调节 */
  --hud-base-width: 100%;
  --cut: 10px;              /* 切角尺寸 */
}

body {
  margin: 0; padding: 0;
  font-size: var(--hud-base-font);
}

/* 容器：宽度 100% 不设固定 max-width，由用户设置的 widthPercent 动态注入 */
.hud-container {
  width: 100%;
  max-width: var(--hud-base-width);
  margin: 0 auto;
  box-sizing: border-box;
}

/* 字体比例关系（全部用 calc 相对于锚点） */
.hud-title    { font-size: calc(var(--hud-base-font) * 1.64); }
.hud-subtitle { font-size: calc(var(--hud-base-font) * 0.82); }
.hud-tag      { font-size: calc(var(--hud-base-font) * 0.92); }
.settings-note { font-size: calc(var(--hud-base-font) * 0.86); }
```

JS 端动态注入（响应用户调节）：

```javascript
var hudSettings = { fontSize: 12, widthPercent: 100 };

function applyHudSettings(s) {
  var root = document.documentElement;
  root.style.setProperty('--hud-base-font',  s.fontSize + 'px');
  root.style.setProperty('--hud-base-width', s.widthPercent + '%');
}

// 绑定 slider
document.getElementById('opt-fontsize').addEventListener('input', function () {
  applyHudSettings({ fontSize: +this.value, widthPercent: hudSettings.widthPercent });
});
```

---

### 3.3 viewport 感知的弹出框定位（min() 函数防溢出）

**来源：** `status_bar_regex_beauty_r5.html` 第 187、511 行 | **置信度：high** `[运行时源码]`

```css
/* 设置面板：宽度不超过视口减去安全边距 */
.hud-settings-panel {
  width: min(328px, calc(100vw - 24px));
  max-height: min(372px, calc(100vh - 84px));
}

/* 颜色选择弹出框 */
.hud-color-popover {
  width: min(310px, calc(100vw - 20px));
}
```

JS 端弹出框定位（防止溢出屏幕右/下边缘）：

```javascript
function positionPopover(anchor, $popover) {
  var rect = anchor.getBoundingClientRect();
  var pw   = $popover.outerWidth()  || 310;
  var ph   = $popover.outerHeight() || 260;
  var left = Math.max(8, Math.min(window.innerWidth - pw - 8, rect.left));
  var top  = rect.bottom + 6;
  // 下方空间不足时翻转到上方
  if (top + ph > window.innerHeight - 8) top = Math.max(8, rect.top - ph - 6);
  $popover.css({ left: left + 'px', top: top + 'px' });
}
// 窗口 resize 时重新定位
$(window).on('resize', function () { positionPopover(anchor, $popover); });
```

---

### 3.4 媒体查询断点（卡片内）

**来源：** `status_bar_regex_beauty_r5.html` 第 721、1166 行 | **置信度：high** `[运行时源码]`

```css
/* Tab 按钮：默认 8 列，小屏改 4 列 */
.tab-btn {
  flex: 0 1 calc(12.5% - 6px);   /* 8 列 */
  min-width: 60px;
}
@media (max-width: 600px) {
  .tab-btn { flex-basis: calc(25% - 6px); }  /* 4 列 */
}

/* 属性网格：默认 3 列，窄屏改 2 列 */
.env-space-grid {
  display: grid;
  grid-template-columns: repeat(3, minmax(0, 1fr));
}
@media (max-width: 600px) {
  .env-space-grid {
    grid-template-columns: repeat(2, minmax(0, 1fr));
  }
}
```

注意：iframe 内的 `@media` 以 iframe 自身宽度为准，而非宿主 window 宽度（详见 4.4 P4 避坑）。

---

### 3.5 触控事件正确绑定

**来源：** `status_bar_regex_beauty_r5.html` 第 4554–4570 行 | **置信度：high** `[运行时源码]`

```javascript
// 面板：同时监听 pointer/mouse/touch 防止移动端遗漏
['pointerdown', 'mousedown', 'touchstart', 'wheel'].forEach(function (evt) {
  panelNode.addEventListener(evt, function (e) { e.stopPropagation(); }, true);
});

// 弹出框关闭按钮：同时响应 touch
$('#color-popover-close').on('click', function (e) {
  e.preventDefault();
  e.stopPropagation();
  closeColorPopover();
});
$('#hud-color-popover').on(
  'pointerdown mousedown touchstart click wheel',
  function (e) { e.stopPropagation(); }
);

// 颜色拾取器：用 pointerdown 统一覆盖 mouse + touch
$picker.on('pointerdown', '.picker-stage', function (e) {
  e.preventDefault();
  e.stopPropagation();
  if (this.setPointerCapture && e.pointerId !== undefined) {
    this.setPointerCapture(e.pointerId);
  }
  // ... 拖拽逻辑
});
```

**规律：** 以 `pointerdown` 为主事件（统一覆盖 mouse + touch），`touchstart` 作备用，不单独依赖 `mousedown`。

---

### 3.6 动态网格宽度检测（容器查询替代方案）

**来源：** `status_bar_regex_beauty_r5.html` 第 2474–2477 行 | **置信度：high** `[运行时源码]`

```javascript
// 用 getBoundingClientRect 做运行时响应（以容器宽度判断，而非 viewport 宽度）
function updateMediaLayoutBlocks() {
  document.querySelectorAll('.cz-media-block').forEach(function (el) {
    var width = el.getBoundingClientRect().width;
    el.classList.toggle('is-narrow', width > 0 && width < 560);
  });
}
// 在 window resize 时调用
window.addEventListener('resize', updateMediaLayoutBlocks);
```

此方案可规避 iframe 宽度 vs window 宽度的歧义，直接测量目标元素实际渲染宽度。

---

## 4. 高频避坑

### P1 iframe 高度在移动端卡 0 或撑不开

**原因：** 量 `scrollHeight` 时若有横向溢出，`body.scrollHeight` 会包含溢出宽度导致计算异常；移动端系统字体放大时 layout 可能延迟完成。

**解法（已验证）：** 量高之前强制 `doc.body.style.overflowX = 'hidden'; doc.body.style.maxWidth = '100%'`，多轮 `setTimeout [100, 400, 900, 1800, 3000]` + `ResizeObserver` 持续跟踪。

**来源：** 本地实战源码 3.0.2 | **置信度：high** `[运行时源码]`

---

### P2 `100vh` 在移动端 Safari 比实际可用高度高

**原因：** `100vh` 包含被浏览器工具栏遮挡的区域（Safari 底部工具栏约 56–80px）。

**解法：** ST 宿主已在 `mobile-styles.css` 改用 `100dvh`（dynamic viewport height）。卡片内部弹出框用 `min(N px, calc(100vh - 偏移))` 兜底；若需精确贴合，改用 `100dvh`。

**来源：** 运行时源码（ST 使用 `100dvh` 已确认）+ 推断（卡片内解法未真机验证，Safari 工具栏高度为社区通识） | **置信度：high（ST 宿主用 dvh 部分）** + **medium·推导未验证（卡片内 dvh 应用建议）** `[运行时源码]` `[推断]`

---

### P3 固定宽度 px 值在窄屏被截断

**原因：** 状态栏面板 `width: 328px` 在 320px 宽手机上溢出屏幕右侧。

**解法（已验证）：** `width: min(328px, calc(100vw - 24px))`，`24px` 为两侧各留 12px 安全边距。

**来源：** 本地实战源码 2.5.0 | **置信度：high** `[运行时源码]`

---

### P4 媒体查询在 iframe 内以 iframe 宽度为参照而非 window

**原因：** iframe 内的 `@media (max-width: 600px)` 响应 iframe 自身渲染宽度，不是宿主 window 宽度（Web 标准行为）。

**影响（正向）：** 即使宿主 window 是大屏，只要 iframe 被 ST 聊天气泡约束到窄宽度，媒体查询也会正确触发；卡片内断点可直接以气泡/消息容器宽度为设计基准。

**注意：** ST 是否对 iframe 施加 `sandbox` 属性覆盖此行为尚未完整验证（见 6 悬案 U2）。

**来源：** Web 标准规范 + 推断 | **置信度：medium** `[推断]`（需真机验证 sandbox 模式）

---

### P5 `backdrop-filter: blur()` 在移动端低配机型性能危险

**解法：** 参考 ST 官方 `No Blur Effect` toggle 的存在。状态栏若使用 `backdrop-filter`，须提供 fallback：用 `@supports (backdrop-filter: blur(4px))` 条件包裹，或提供可关闭的配置项。

**来源：** 文档单源 + 推断 | **置信度：medium** `[文档·单源]` `[推断]`

---

### P6 触控点击热区太小（< 44px）

**ST 观测值：** 移动端 menu toggle 按钮实测约 30px，ST 自身也存在不达标的情况。

**建议：** 可交互元素最小尺寸 44×44px（iOS HIG 标准）；卡片内按钮加 `padding: 8px 12px` 通常可达标。

**来源：** 运行时源码观测 + iOS HIG | **置信度：medium** `[运行时源码]` `[推断]`

---

### P7 `position: fixed` 在 iframe 内相对于 iframe 视口而非宿主 window

**原因：** iframe 建立独立 stacking context，`fixed` 相对 iframe 视口定位，而非宿主页面视口。

**影响：** 卡片内 `position: fixed` 的弹出框/设置面板在移动端可能被卡片自身 iframe 的高度限制截断（iframe 高度由 syncHeight 同步到内容高度，弹出框若超出可视区则被裁切）。注意：ST 框架层不直接限制 iframe 高度，高度由卡片自身 JS 控制。

**解法（已验证）：** 改用 `position: absolute` + 父容器 `position: relative; overflow: visible`；或动态检测下方剩余空间，不足时翻转弹出方向（见 3.3 JS 定位代码）。

**来源：** Web 标准 + 本地实战源码翻转逻辑 | **置信度：high** `[运行时源码]` `[推断]`

---

### P8 移动端卡片内无法调用 ST 内部函数 `isMobile()`

**原因：** `isMobile()` 以 `export function` 形式定义在 ST 宿主 `RossAscends-mods.js` 中（已源码确认），属于宿主 ES module scope，卡片内 iframe JS 无法通过 `window.isMobile()` 直接调用（不同 document 上下文且 module export 不挂载到 window）。

**替代方案：**

```javascript
// 方案 A：视口宽度判断
var isMobileLayout = window.innerWidth < 1000;

// 方案 B：媒体查询对象
var isMobileLayout = window.matchMedia('(max-width: 600px)').matches;

// 方案 C：UA 检测（粗略）
var isMobileUA = /Mobi|Android|iPhone|iPad/i.test(navigator.userAgent);
```

**来源：** 运行时源码（isMobile 定义位置已确认）+ 推断（iframe 跨 scope 不可访问，理论层面无真机实测） | **置信度：medium·推导未验证** `[运行时源码]` `[推断]`

---

### P9 横竖屏切换时 iframe 高度不更新

**原因：** 设备旋转后视口宽高互换，内容重排，但若没有监听 resize/orientation 事件，iframe 高度停留在旋转前的值。

**解法：**

```javascript
window.addEventListener('resize', syncHeight);
if (screen.orientation) {
  screen.orientation.addEventListener('change', function () {
    setTimeout(syncHeight, 300); // 旋转动画完成后再量
  });
}
```

**来源：** 推断 | **置信度：medium** `[推断]`（需真机验证）

---

## 5. ST 宿主 CSS 变量速查

| 变量 | 用途 | iframe 内可读 |
|---|---|---|
| `--SmartThemeBodyColor` | 主文本色 | 否（跨文档，同源时可通过 `window.parent` 读） |
| `--SmartThemeBorderColor` | 边框色 | 否 |
| `--SmartThemeBlurStrength` | blur 系数 | 否 |
| `--sheldWidth` | 聊天窗宽度（vw 值），可读不可写 | 否 |
| `--fontScale` | 用户字体缩放比例 0.5–1.5 | 否（见悬案 U3） |

**重要说明：** 这些变量定义在宿主 `document.documentElement`，iframe 内**不继承**（跨文档边界）。需要主题适配时，需在卡片 JS 里读 `window.parent.document.documentElement.style.getPropertyValue(...)` — 此操作仅在同源（ST 本地部署）时有效，云端部署可能跨域失败。

**来源：** 运行时源码 + Web 标准 | **置信度：high** `[运行时源码]`

---

## 6. 悬案与验证路径

| 编号 | 悬案描述 | 置信度 | 真机验证路径 |
|---|---|---|---|
| U1 | iframe 内 `@media` 究竟以 iframe 自身宽度还是宿主 window 宽度为准（ST sandbox 模式下） | low | ST 真机实测：在注入卡片里打印 `window.innerWidth`，对比宿主实际窗口宽度；同时观察 600px 断点是否按 iframe 宽度触发 |
| U2 | ST 框架层是否施加 `sandbox` 属性（**部分已解决**：ST v1.14.0 全 .js 源码 grep 无 `sandbox`/`createIframe` 逻辑，ST 不主动施加；剩余问题：浏览器对 blob: URL iframe 的自动限制行为） | medium→low（ST 主动施加部分已排除） | DevTools 检查卡片自创 iframe 元素是否有 `sandbox` 属性；测试 `ResizeObserver` / `postMessage` 实际能否工作 |
| U3 | `--fontScale` 变量在 iframe 内是否可读（同源 ST 本地部署情况下） | medium | 控制台执行 `window.parent.document.documentElement.style.getPropertyValue('--fontScale')` |
| U4 | `dvh/dvw` 在 Android Termux WebView 场景的支持情况 | low | Termux + Chrome WebView 实测虚拟键盘弹出时高度变化；查 caniuse.com/dynamic-viewport-units |
| U5 | 移动端系统辅助功能"大字体"对 iframe 内 12px 字号的覆盖行为 | low | 开启 Android/iOS 大字体辅助功能后加载卡片，观察字体渲染实际尺寸 |
| U6 | `interactive-widget=resizes-content` 在 Android WebView 是否生效（虚拟键盘弹出时 iframe 高度变化） | low | Android WebView 实测弹出键盘前后对比 `window.innerHeight` |

**原则：low 置信度悬案均禁止直接作为施工依据，须先经真机验证。**

---

## 7. 来源索引

| 编号 | 来源描述 | URL / 路径 | 类型 |
|---|---|---|---|
| S1 | ST v1.14.0 index.html（viewport 声明） | `raw.githubusercontent.com/SillyTavern/SillyTavern/release/public/index.html` | 运行时源码 |
| S2 | ST v1.14.0 mobile-styles.css（1000px 断点、dvh、iOS safe area） | `raw.githubusercontent.com/SillyTavern/SillyTavern/release/public/css/mobile-styles.css` | 运行时源码 |
| S3 | ST v1.14.0 power-user.js（CSS 变量动态注入） | `raw.githubusercontent.com/SillyTavern/SillyTavern/release/public/scripts/power-user.js` | 运行时源码 |
| S4 | ST 官方 UI 自定义文档 | `docs.sillytavern.app/usage/core-concepts/uicustomization/` | 文档·多源 |
| S5 | ST DeepWiki UI 章节 | `deepwiki.com/SillyTavern/SillyTavern/4-user-interface` | 文档·多源 |
| S6 | ST GitHub Issue #5100（100% 宽度移动端布局） | `github.com/SillyTavern/SillyTavern/issues/5100` | 社区 |
| S7 | GreasyFork HTML Code Injector 脚本（iframe 高度同步参考） | `greasyfork.org/en/scripts/503174` | 社区 |
| S8 | 本地生产卡 2.5.0 status_bar 挂载壳 | `C:/Users/Administrator/OneDrive/ST-/角色卡工作区/星月/星月 2.5.0/components/status_bar_regex_beauty_r5.html` | 运行时源码·本地验证 |
| S9 | 本地生产卡 3.0.2 status_bar 挂载壳 | `ST开发指南DB/本地证据/星月历史/3.0.2/components/status_bar_regex.html` | 运行时源码·本地验证 |
| S10 | ST v1.14.0 RossAscends-mods.js（isMobile/Bowser 定义） | `C:/Users/Administrator/OneDrive/ST-/SillyTavern-1.14.0/public/scripts/RossAscends-mods.js` | 运行时源码 |
| S11 | ST v1.14.0 style.css（CSS 变量定义，--SmartThemeBlurStrength 派生关系） | `C:/Users/Administrator/OneDrive/ST-/SillyTavern-1.14.0/public/style.css` | 运行时源码 |
| S12 | ST v1.14.0 全 .js 源码 grep（确认无 ST 主动创建 iframe / sandbox 逻辑） | 本地 grep `iframe\|sandbox\|createIframe` | 运行时源码·本次回源 |
