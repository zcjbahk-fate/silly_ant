# 渲染管线与宏系统参考手册

> 文档版本：v1.0.1 ｜ 最后更新：2026-07-14

> SillyTavern 消息从 LLM 输出到屏幕的完整管线，以及宏求值双引擎架构参考。

---

## 置信度分级体系

本手册采用三级置信度标注，格式为 `置信度 [依据类型]`：

- **high**：多源一致 + 源码直接验证 / 生产卡实战佐证，可直接施工
- **medium**：单源或推断，可参考施工，宜在真机验证后再大规模应用；强制子标签：`medium·单源孤证` / `medium·推导未验证`（须随附真机验证路径）
- **low**：孤证无源，**禁止据此施工**，须先真机验证

依据类型轴：`[运行时源码]` / `[文档·多源]` / `[文档·单源]` / `[社区]` / `[推断]` / `[本地实战]`

收集日期：2026-06-28
回源基准：SillyTavern v1.14.0 + 本地成熟卡（星月 2.5.0 / 交错宙域 2.6.0）

**两条铁规：**

1. **「从源码事实再推一步」的下游推导，即使源码本身已直读，该推导结论最高只能 `medium·推导未验证`**——除非已真机验证；源码内部事实仍标 `high [运行时源码]`。
2. **节级标注 = 节内条目下确界**——禁止整节标 high 却内含悬案条目；冲突即在节内局部降级，并交叉引用悬案章对应条目。

---

## 目录

0. [置信度分级体系（全手册适用）](#置信度分级体系)
1. [消息渲染完整管线](#1-消息渲染完整管线)
2. [宏求值管线三阶段](#2-宏求值管线三阶段)
3. [宏系统双引擎架构](#3-宏系统双引擎架构)
   - 3.1 [两套引擎对比](#31-两套引擎对比)
   - 3.2 [宏处理技术细节](#32-宏处理技术细节)
4. [内置宏完整分类参考](#4-内置宏完整分类参考)
5. [正则脚本字段完整 API](#5-正则脚本字段完整-api)
   - 5.1 [字段定义表](#51-字段定义表)
   - 5.2 [placement 枚举值](#52-placement-枚举值)
   - 5.3 [markdownOnly 执行条件](#53-markdownonly-执行条件)
6. [messageFormatter Hook API](#6-messageformatter-hook-api)
7. [自定义宏注册 API](#7-自定义宏注册-api)
8. [DOMPurify 与 custom-style 陷阱](#8-domPurify-与-custom-style-陷阱)
   - 8.1 [style 属性转换机制](#81-style-属性转换机制)
   - 8.2 [自定义 XML 标签 + simpleLineBreaks 双重 bug（#3996）](#82-自定义-xml-标签--simplelinebreaks-双重-bug-3996)
   - 8.3 [CSS class 前缀（无此行为）](#83-css-class-前缀无此行为)
9. [Showdown Markdown 配置](#9-showdown-markdown-配置)
10. [变量系统与宏交互](#10-变量系统与宏交互)
11. [完整生产范式代码](#11-完整生产范式代码)
    - 11.1 [范式 A：正则替换注入 HTML 组件](#111-范式-a正则替换注入-html-组件)
    - 11.2 [范式 B：占位符 + 状态栏 iframe 注入](#112-范式-b占位符--状态栏-iframe-注入)
    - 11.3 [范式 C：对话气泡正则（data-* 属性绕过 DOMPurify）](#113-范式-c对话气泡正则data--属性绕过-dompurify)
    - 11.4 [范式 D：first_mes 开局页占位 HTML](#114-范式-dfirst_mes-开局页占位-html)
12. [高频避坑清单](#12-高频避坑清单)
13. [悬而未决：未决项与真机验证路径](#13-悬而未决未决项与真机验证路径)
14. [来源索引](#14-来源索引)

---

## 1. 消息渲染完整管线

> **核心结论** | 来源：`script.js` `messageFormatting()` 函数直读 + `engine.js`/`chats.js` | 适用版本：v1.14.0+ | **high [运行时源码]**
>
> 注：草稿原标注「engine.js 源码分析 + ST 扩展开发文档」不够精确。v1.14.0 中 `messageFormatting()` 是主渲染函数（script.js:1618），管线可在此直读。`[hook] beforeRegex/afterRegex/afterMarkdown` 为 messageFormatter API（v1.14.0 不存在），故从管线图中区分标注。

消息从 LLM 产生到最终在 `.mes_text` DOM 中渲染，经过以下完整管线（宏求值在管线最前，见第 2 章）：

```
LLM 原始文本
  │
  ▼ prompt-bias 剥离（替换开头的 prompt bias 字串）
  │  ← script.js:1645-1648 直读
  │
  ▼ 正则脚本（placement=[2] AI_OUTPUT 且 markdownOnly=true 时，此阶段执行）
  │  ← getRegexedString(raw, AI_OUTPUT, { isMarkdown: true }) [high 运行时源码]
  │
  ▼ encode_tags 处理（若开启，转义 < >）+ 双引号 <q> 包裹
  │  ← script.js:1685-1736 直读
  │
  ▼ Showdown 转 HTML（markdown → HTML）
  │   开启: simpleLineBreaks / tables / strikethrough / ghCodeBlocks
  │   ← script.js:1745 直读
  │
  ▼ encodeStyleTags（<style>→<custom-style>，DOMPurify 前保护）
  │   ← chats.js:538-543 直读 [high 运行时源码]
  │
  ▼ DOMPurify.sanitize(mes, config)
  │   config: MESSAGE_SANITIZE=true / ADD_TAGS: ['custom-style']
  │   hook A: uponSanitizeAttribute → class 名加 custom- 前缀
  │   hook B: uponSanitizeElement → HTMLUnknownElement 内 \n→<br>
  │   ← chats.js:1906-1977 直读 [high 运行时源码]
  │
  ▼ decodeStyleTags（<custom-style>→<style>，CSS AST 重写选择器前缀）
  │   ← chats.js:553-702 直读 [high 运行时源码]
  │
  ▼ 渲染进 .mes_text DOM
```

**关于 messageFormatter hook（BEFORE_REGEX/AFTER_REGEX/AFTER_MARKDOWN）：**
- 这三个 hook 点在 v1.14.0 **不存在**（getContext() 无此 API）
- 属官方文档记录的扩展点，可能在更高版本引入 | **medium·推导未验证 [文档·多源]**

**关键时序说明（v1.14.0 实测）：**

- `markdownOnly: true` 的正则脚本在 **showdown 之前**、encode_tags 之前执行 | **high [运行时源码]**（script.js:1674-1678 直读）
- `promptOnly: true` 的正则脚本在**发送给模型时**执行，不影响 display 层 | **high [运行时源码]**
- `{{trim}}` 已在宏求值阶段（管线最前）处理，不属于此管线内部 | **high [运行时源码]**
- DOMPurify 是 display 层最后净化关；之后 decodeStyleTags 再 decode CSS | **high [运行时源码]**

---

## 2. 宏求值管线三阶段

> **核心结论** | 来源：`macros.js` `evaluateMacros()` 源码 | 适用版本：v1.14.0+ | **high [运行时源码]**

宏求值发生在消息渲染管线之前，适用于世界书、系统提示、角色卡字段、消息体所有字符串：

```
原始字符串
  │
  ▼ Phase 1 — Pre-env Macros
  │   包含：骰子/instruct/newline/trim/legacy <USER><BOT> 标签
  │   getVariableMacros() 返回值也在此（变量宏最早执行）
  │
  ▼ Phase 2 — Env Variables
  │   用户传入 macro map，支持函数值
  │
  ▼ Phase 3 — Post-env Macros
  │   包含：token 限制/消息访问/时间/random/pick/banned
  │
  ▼ 求值完成字符串 → 进入消息渲染管线
```

**重要特性：**
- 变量宏（`{{getvar::}}` 等）处于 Phase 1，是最早执行的宏之一 | high [运行时源码]
- 旧引擎中宏求值顺序不稳定；新引擎保证 stable evaluation order | high [运行时源码]

---

## 3. 宏系统双引擎架构

### 3.1 两套引擎对比

> **medium·推导未验证** | 来源：官方宏文档 [文档·多源] | 适用版本：**v1.14.0 后某版本引入，v1.14.0 本地源码未见** | ~~high~~ → **medium·推导未验证 [文档·多源]**
>
> **降级原因**：本地 v1.14.0 `macros.js` + `st-context.js` 直读——未发现 `experimental_macro_engine` 开关、chevrotain 依赖、新引擎任何痕迹；`getContext()` 不暴露 `macros` 对象。双引擎架构在官方文档中有记录，但属于 v1.14.0 之后引入的特性，本地版本无法验证。**若仍使用 v1.14.0，下表描述不适用。**
>
> 真机验证路径：在目标 ST 版本设置页搜索 "experimental macro"，或 grep `experimental_macro_engine` 确认是否存在。

| | 旧引擎 MacrosParser | 新引擎 MacroEngine（Macros 2.0） |
|---|---|---|
| 存储结构 | 静态 `Map` | chevrotain CST 树 |
| 处理管线 | 正则逐条替换 | Lexer → Parser → CstWalker |
| 嵌套支持 | **不支持** | **支持**：`{{getvar::{{char}}_mood}}` |
| 求值顺序 | 不保证 | stable evaluation order |
| 开关 | 旧安装默认（未打开实验选项） | `power_user.experimental_macro_engine`（新安装默认开） |
| `{{pick}}` 兼容性 | 旧 hash 算法 | 新 hash 算法，结果不兼容 |

**切换风险**：从旧引擎切换到新引擎时，`{{pick}}` 的结果会因 hash 算法不同而改变，同 chat 同位置的"稳定随机"结果将全部刷新。已有依赖 `{{pick}}` 的卡需告知用户此风险。

来源 | 适用版本 | 置信度：medium·推导未验证 [文档·多源]（v1.14.0 本地无此特性）

### 3.2 宏处理技术细节

> 来源：`macros.js` + `variables.js` 源码 | 适用版本：v1.14.0+ | **high [运行时源码]**（v1.14.0 直读，以下各条分别标注）

**`{{trim}}`**
正则 `/(?:\r?\n)*{{trim}}(?:\r?\n)*/gi` → 替换为空字符串，连同前后换行一并清除。在 Phase 1（Pre-env）阶段处理，早于所有其他用户可见宏。| **high [运行时源码]**（macros.js:521 直读）

**`{{pick}}`**
哈希公式：`seedrandom(getStringHash("${chatIdHash}-${rawContentHash}-${offset}"))` — 三因子（chat ID、内容 hash、偏移量）保证同 chat 同位置一致。**注意：v1.14.0 只有一套 hash 实现，「跨引擎不兼容」说法仅适用于新引擎已引入的更高版本。** | **high [运行时源码]**（macros.js:404-432 直读）

**`{{random}}`**
使用 `seedrandom('added entropy.', { entropy: true })` 加密种子真随机，每次求值结果不同，不具备跨轮次一致性。| **high [运行时源码]**（macros.js:391 直读）

**变量宏求值**
变量名不经过宏预处理，直接字符串 trim 后匹配。`resolveVariable()` 查找顺序：
`scoped → local (chat_metadata) → global (extension_settings) → 返回原字符串（静默回退）`
| **high [运行时源码]**

**新引擎嵌套宏与变量速记语法**（需启用 experimental_macro_engine，v1.14.0 无此特性）：| **medium·推导未验证 [文档·多源]**（v1.14.0 无此功能，官方文档记录属更高版本）
- `.varname` = local 变量读
- `$varname` = global 变量读
- 运算符：`++/--/+=/=/-=`
- 空值合并：`||` / `??`
- 比较：`==/!=/>/</>=/<=`（返回字符串 "true"/"false"）

---

## 4. 内置宏完整分类参考

> 来源：官方宏文档 `docs.sillytavern.app/usage/core-concepts/macros/` | 适用版本：v1.14.0+ | **high [文档·多源]**

### 名称类

| 宏 | 说明 |
|---|---|
| `{{user}}` | 当前用户/人设名 |
| `{{char}}` | 当前角色名 |
| `{{group}}` | 群聊成员名，逗号分隔 |
| `{{groupNotMuted}}` | 群聊未静音成员 |
| `{{charIfNotGroup}}` | 非群聊时为角色名，否则为空 |
| `{{notChar}}` | 当前发言者以外的所有参与者 |

### 角色卡字段类

| 宏 | 说明 |
|---|---|
| `{{description}}` `{{personality}}` `{{scenario}}` | 卡片字段 |
| `{{persona}}` | 用户人设描述 |
| `{{charPrompt}}` `{{charInstruction}}` | 角色主提示 / 后史指令覆盖 |
| `{{charDepthPrompt}}` | @Depth Note |
| `{{charCreatorNotes}}` `{{charVersion}}` | 创作者备注 / 版本 |
| `{{mesExamples}}` / `{{mesExamplesRaw}}` | 对话示例（格式化 / 原始） |
| `{{charFirstMessage}}` / `{{charFirstMessage::N}}` | 第 N 条开场白（1-based） |

### 消息历史类

| 宏 | 说明 |
|---|---|
| `{{lastMessage}}` `{{lastUserMessage}}` `{{lastCharMessage}}` | 最近消息 |
| `{{lastMessageId}}` | 最后消息索引 |
| `{{firstIncludedMessageId}}` `{{firstDisplayedMessageId}}` | 上下文边界 |
| `{{lastSwipeId}}` `{{currentSwipeId}}` | 划动索引（1-based） |
| `{{allChatRange}}` | 全聊天范围，如 "0-50" |
| `{{summary}}` | 最新聊天摘要 |

### 时间类

| 宏 | 说明 |
|---|---|
| `{{time}}` `{{date}}` `{{weekday}}` | 本地时间 / 日期 / 星期 |
| `{{isotime}}` `{{isodate}}` | HH:mm / YYYY-MM-DD |
| `{{time::UTC±N}}` | 带时区偏移的时间 |
| `{{datetimeformat::format}}` | 自定义格式（Moment.js 语法） |
| `{{idleDuration}}` | 距上次用户消息时长 |
| `{{timeDiff::left::right}}` | 两时间差（Moment.js 解析） |

### 变量类 — Local

| 宏 | 说明 |
|---|---|
| `{{getvar::name}}` | 读取 local 变量 |
| `{{setvar::name::val}}` | 写入 local 变量 |
| `{{addvar::name::val}}` | 数值累加 |
| `{{incvar::name}}` `{{decvar::name}}` | 自增 / 自减 |
| `{{hasvar::name}}` | 存在检测（返回 "true"/"false"） |
| `{{deletevar::name}}` | 删除变量 |

### 变量类 — Global

| 宏 | 说明 |
|---|---|
| `{{getglobalvar::name}}` `{{setglobalvar::name::val}}` 等 | 同上，但写入 extension_settings 全局持久 |

### 变量速记类（新引擎专属）

> **medium·推导未验证** [运行时源码]：需确认 `experimental_macro_engine` 已开启

| 宏 | 说明 |
|---|---|
| `{{.varname}}` | local 读 |
| `{{.var=val}}` | local 写 |
| `{{.cnt++}}` `{{.cnt--}}` | 自增 / 自减 |
| `{{.score+=10}}` `{{.score-=5}}` | 累加 / 累减 |
| `{{.name\|\|fallback}}` `{{.name??fallback}}` | 空值回退（两者略有差异） |
| `{{.a==b}}` `{{.a!=b}}` `{{.a>b}}` 等 | 比较（返回字符串 "true"/"false"） |
| `{{$globalvar}}` | global 速记读 |

### 随机类

| 宏 | 说明 |
|---|---|
| `{{random::a::b::c}}` | 真随机选一项（每次不同） |
| `{{pick::a::b::c}}` | 稳定随机（同 chat 同位置一致） |
| `{{roll::1d20}}` | 骰子（droll 语法） |

### 运行状态类

| 宏 | 说明 |
|---|---|
| `{{maxPrompt}}` `{{maxContextTokens}}` `{{maxResponseTokens}}` | token 上限 |
| `{{model}}` | 当前模型名 |
| `{{isMobile}}` | "true" / "false" |
| `{{lastGenerationType}}` | normal / impersonate / regenerate 等 |
| `{{hasExtension::name}}` | 扩展是否激活 |

### 提示模板类

| 宏 | 说明 |
|---|---|
| `{{systemPrompt}}` `{{authorsNote}}` 等 | 各种提示字段 |
| `{{instructUserPrefix}}` `{{instructAssistantPrefix}}` 等 | Instruct 模板各段 |
| `{{chatSeparator}}` `{{chatStart}}` | 分隔符 |
| `{{reasoningPrefix}}` `{{reasoningSuffix}}` `{{reasoningSeparator}}` | 推理块标记 |

### 实用类

| 宏 | 说明 |
|---|---|
| `{{newline}}` / `{{newline::N}}` | 换行 / N 个换行 |
| `{{space}}` / `{{space::N}}` | 空格 / N 个空格 |
| `{{noop}}` | 空字符串（无操作占位） |
| `{{trim}}` | 去除前后换行（含宏周围换行） |
| `{{reverse::text}}` | 反转字符串 |
| `{{input}}` | 当前输入框内容 |
| `{{banned::word}}` | 文本补全禁词注入 |
| `{{outlet::key}}` | 世界书出口提示（Outlet 插槽） |

### 控制流类

> 来源：官方文档 [文档·多源] | 适用版本：**官方文档记录，v1.14.0 本地 macros.js 直读未见实现** | ~~high~~ → **medium·推导未验证 [文档·多源]**
>
> **降级原因**：v1.14.0 `macros.js` `evaluateMacros()` 的 `preEnvMacros` / `postEnvMacros` 中无任何 `{{if}}`/`{{//}}` 的正则处理；`{{//comment}}` 单行注释正则 `/\{\{\/\/([\s\S]*?)\}\}/gm` 在 postEnvMacros 中存在，但 `{{if}}` 块不存在。控制流宏可能属于新引擎或更高版本引入。`{{//comment}}` 单行注释可单独提升为 high（v1.14.0 直读）。
>
> 真机验证路径：在 v1.14.0 中发送 `{{if true}}SHOW{{/if}}`，观察是否渲染为 "SHOW"。

| 宏 | 说明 | 置信度 |
|---|---|---|
| `{{if cond}}...{{/if}}` | 条件渲染 | **medium·推导未验证 [文档·多源]**（v1.14.0 无此实现） |
| `{{if cond}}...{{else}}...{{/if}}` | if/else 分支 | **medium·推导未验证 [文档·多源]** |
| `{{if !cond}}` | 取反条件 | **medium·推导未验证 [文档·多源]** |
| `{{//comment}}` | 单行注释（求值后删除） | **high [运行时源码]**（macros.js:541 直读，postEnvMacros） |
| `{{//}}...{{///}}` | 多行注释块 | **medium·推导未验证 [文档·多源]**（v1.14.0 无此实现） |

### 遗留兼容类

| 宏 | 说明 |
|---|---|
| `<USER>` `<BOT>` / `<CHAR>` `<GROUP>` `<CHARIFNOTGROUP>` | 自动转换为对应新宏（Phase 1 处理） |

---

## 5. 正则脚本字段完整 API

### 5.1 字段定义表

> 来源：`extensions/regex/engine.js` + 本地成熟卡实战验证 | 适用版本：v1.14.0+ | **high [运行时源码] + [本地实战]**

| 字段 | 类型 | 说明 |
|---|---|---|
| `id` | string | 唯一标识符 |
| `scriptName` | string | UI 显示名（决定执行顺序中的 beforeScriptName 引用） |
| `findRegex` | string | JS 正则，格式 `/pattern/flags` |
| `replaceString` | string | 替换内容；支持 `$1..$n`、`$<name>`、`{{match}}` |
| `trimStrings` | array | 先从捕获组中删除这些字符串再替换 |
| `placement` | array | 见 5.2 节枚举（可多选） |
| `disabled` | boolean | 是否禁用 |
| `markdownOnly` | boolean | 仅在 Markdown 渲染时运行（display 层，不影响存储） |
| `promptOnly` | boolean | 仅在发送给模型时运行（prompt 层，不影响显示/存储） |
| `runOnEdit` | boolean | 消息编辑时是否重跑 |
| `substituteRegex` | 0/1/2 | 0=不替换参数；1=raw 替换；2=转义替换 |
| `minDepth` | number/null | 最小消息深度（0=最新消息） |
| `maxDepth` | number/null | 最大消息深度（null=无限制） |

**非运行时组件库构建字段**（仅在 `regex_object.json` 元数据中使用，不进入卡片最终 JSON）：

| 字段 | 说明 |
|---|---|
| `field` | 通常为 `"replaceString"`，标明关联的内容源字段 |
| `replaceStringSource` | 关联的 HTML 模板文件名（workflow_components.mjs 处理合并） |
| `beforeScriptName` | 指定此脚本在哪个脚本之后执行（控制顺序） |

### 5.2 placement 枚举值

> 来源：`engine.js` | **high [运行时源码]**

| 枚举名 | 数值 | 含义 |
|---|---|---|
| `MD_DISPLAY` | 0 | 已弃用（早期 display 触发点） |
| `USER_INPUT` | 1 | 用户输入消息 |
| `AI_OUTPUT` | 2 | AI 回复消息 ← **角色卡最常用** |
| `SLASH_COMMAND` | 3 | STscript 斜杠命令结果 |
| `WORLD_INFO` | 5 | 世界书条目（prompt 层处理） |
| `REASONING` | 6 | 推理 / 思考块 |

**注意**：枚举值 4 在当前版本未分配（跳号）。

### 5.3 markdownOnly 执行条件

> 来源：`engine.js` | **high [运行时源码]**

```javascript
// engine.js 中的执行判断逻辑：
(script.markdownOnly && isMarkdown)
|| (script.promptOnly && isPrompt)
|| (!script.markdownOnly && !script.promptOnly && !isMarkdown && !isPrompt)
```

**三态解读：**

| 配置 | 执行场景 |
|---|---|
| `markdownOnly: true` | 仅在 display 渲染（showdown 前）执行，存储/prompt 不受影响 |
| `promptOnly: true` | 仅在 prompt 构建（发给模型）时执行，display 不受影响 |
| 两者均为 false | 在所有非 markdown、非 prompt 的上下文执行（如编辑存储） |

---

## 6. messageFormatter Hook API

> 来源：ST 扩展开发文档 [文档·多源] | 适用版本：**官方文档记录，v1.14.0 本地源码未见此 API** | ~~high~~ → **medium·推导未验证 [文档·多源]**
>
> **降级原因**：本地 v1.14.0 `st-context.js` `getContext()` 返回对象直读，无 `messageFormatter` 字段；全库 grep `messageFormatter` 零命中（除 `messageFormatting` 函数）。该 API 属官方文档记录的扩展能力，很可能在 v1.14.0 之后版本引入。**v1.14.0 用户不可使用此 API。**
>
> 真机验证路径：在目标 ST 版本控制台执行 `SillyTavern.getContext().messageFormatter`，检查是否为对象。

```javascript
const { messageFormatter } = SillyTavern.getContext();

messageFormatter.addHook((mes, ctx) => {
    // mes: 当前阶段的字符串（纯文本或 HTML，取决于 stage）
    // ctx: { characterName, isUser, isSystem, messageId, isReasoning, stage }
    return transformedString; // 必须同步返回字符串
}, {
    stage: messageFormatter.stage.AFTER_MARKDOWN, // 见下方三个 stage 常量
    order: messageFormatter.order.NORMAL,          // EARLIEST | EARLY | NORMAL | LATE | LATEST
});
```

**三个可用 stage：**

| stage 常量 | 时机 | mes 类型 |
|---|---|---|
| `BEFORE_REGEX` | 正则脚本执行前 | 纯文本 |
| `AFTER_REGEX` | 正则脚本执行后、showdown 前 | 纯文本 |
| `AFTER_MARKDOWN` | showdown 后、DOMPurify 前 | **HTML 字符串**（最常用扩展点） |

**约束：**
- 回调必须**同步**返回字符串，不支持 async/await
- AFTER_MARKDOWN hook 注入的内容仍会经过 DOMPurify（见第 8 章陷阱）

---

## 7. 自定义宏注册 API

> 来源：官方宏文档 [文档·多源] + `macros.js` / `st-context.js` 源码 | 适用版本：旧 API v1.14.0 已验证；新 API v1.14.0 未见

**旧 API（v1.14.0 直读验证）** | **high [运行时源码]**（st-context.js:164 直读，仍暴露 `registerMacro`）：

```javascript
// 通过 getContext() 访问
const { registerMacro } = SillyTavern.getContext();
registerMacro('myMacro', () => 'value', 'description');

// 或直接使用 MacrosParser（仍可用）
MacrosParser.registerMacro('myMacro', () => 'value', 'description');
```

**新 API（官方文档记录，v1.14.0 本地未见）** | ~~high~~ → **medium·推导未验证 [文档·多源]**：
> **降级原因**：v1.14.0 `st-context.js` `getContext()` 无 `macros` 字段，全库 grep `macros.register` 零命中。新 API 属更高版本特性。
>
> 真机验证路径：控制台执行 `typeof SillyTavern.getContext().macros`，检查是否为 object。

```javascript
const { macros } = SillyTavern.getContext();

// 新 API（推荐，若版本支持）
macros.register('myMacro', {
    category: 'UTILITY', // UTILITY/RANDOM/NAMES/CHARACTER/CHAT/TIME/VARIABLE/PROMPTS/STATE/MISC
    description: '描述文字',
    handler: (env) => {
        // env.dynamicMacros 可注入临时宏
        return 'computed value';
    }
});
```

---

## 8. DOMPurify 与 custom-style 陷阱

### 8.1 style 属性与 style 标签的 DOMPurify 处理机制

> 来源：`chats.js` 源码直读 + Issue #3844 | 适用版本：v1.14.0+ | 整体 **medium·单源孤证→部分 high**（两种机制需分开标注）

**【重要纠错】草稿原描述「style= 属性名被改为 custom-style=」是错误的。实际存在两套独立机制：**

---

**机制 A：`<style>` 标签 → `<custom-style>` 标签（encode/decode 方案）** | **high [运行时源码]**（chats.js:538-543, 553-702 直读）

流程：
1. `encodeStyleTags()` 在 DOMPurify 前将 `<style>CSS内容</style>` 替换为 `<custom-style>URL编码内容</custom-style>`
2. DOMPurify 运行时 `ADD_TAGS: ['custom-style']`，`<custom-style>` 标签被保留
3. DOMPurify 后 `decodeStyleTags()` 将 `<custom-style>` decode 回 `<style>` 并通过 CSS AST 解析对所有选择器加 `.mes_text ` 前缀，防止跨楼层样式污染

**结论：`<style>` 标签能穿越 DOMPurify，最终在 DOM 中正常工作。** CSS 规则中的 class 选择器（如 `.foo`）会被改写为 `.custom-foo`（见 8.3 节）。

---

**机制 B：`style="..."` 内联属性处理** | **medium·版本敏感 [源码观察]**

源码中未见把属性名重命名为 `custom-style=` 的逻辑，但这不足以推出“DOMPurify 默认禁止所有 style”。现有 C3 与当前 release 观察也不支持把“必然删除”写成跨版本事实。应把内联 style 视为版本/净化配置敏感能力，而不是稳定契约。

> 真机验证路径：在最低支持版与当前版发送同一 `<span style="color:red">x</span>`，F12 同时检查最终属性与 computed style。

**生产结论：**
- 关键视觉使用 `<style>` 块 + class，避免依赖内联 style 是否保留；
- `<style>` 块在已核管线中经 encode/decode 保护；
- 动态数值使用 `data-*` + 受信 JS 或 iframe，不把用户数据拼进 style 字符串。

**绕坑方案（生产卡实战验证）：**

| 方案 | 适用场景 | 成本 | 置信度 |
|---|---|---|---|
| 使用 `<style>` 块 | 静态样式规则 | 低 | **high [运行时源码 + 本地实战]** |
| `data-*` 属性 + JS 后处理 | 需要动态数据的样式 | 中；需要配套 JS | **high [本地实战]** |
| iframe + Blob URL | 完整独立 UI 组件（如状态栏） | 高；完全绕开 ST sanitization | **high [本地实战]** |

来源：`chats.js` `encodeStyleTags()`/`decodeStyleTags()` 直读 + `ST开发指南DB\本地证据\星月历史\3.0.2\components\*.html` [本地实战]

### 8.2 自定义 XML 标签 + simpleLineBreaks 双重 bug（#3996）

> 来源：GitHub Issue #3996 + 本地实战 | 适用版本：受 Chromium 136+ 影响更严重 | **high [运行时源码]**

**三重叠加机制：**

1. Showdown `simpleLineBreaks: true` → 把标签内部的 `\n` 转为 `<br>`
2. DOMPurify `uponSanitizeElement` hook → 对 `HTMLUnknownElement`（未在 ADD_TAGS 声明的自定义标签）再次把 `\n` 转为 `<br>`
3. Chromium 136+ 对 HTML 解析更严格 → 双重 `<br>` 堆叠，版面破坏

**修复方案（#3996 采纳）：**
```javascript
// 在 DOMPurify ADD_TAGS 中声明自定义标签，避免被当做 HTMLUnknownElement
DOMPurify.sanitize(mes, {
    ADD_TAGS: ['custom-style', 'UpdateVariable', 'JSONPatch', 'analysis', 'content', ...],
    // ...
});
```

**角色卡工程绕坑（星月卡实战）：**
在 AI 回复正文中的 `<UpdateVariable>` 块由正则替换为 HTML，不让裸 XML 标签进入 showdown 处理流程。这是更彻底的方案：正则在 showdown 前消费自定义标签，根本不存在 HTMLUnknownElement 问题。

### 8.3 CSS class 前缀（双层前缀行为：存在且需区分）

> 来源：`chats.js` 源码直读 + Issue #3844 | **high [运行时源码]**（chats.js:1915-1939 直读）

**【重要纠错】草稿「ST 不对 CSS class 名称加前缀」是错误的。v1.14.0 源码有两套 class 前缀机制：**

---

**机制 A：`class` 属性 → `custom-` 前缀（DOMPurify `uponSanitizeAttribute` hook）** | **high [运行时源码]**

在 `MESSAGE_SANITIZE` 模式（消息渲染时）下，DOMPurify hook 将每个 class 名加上 `custom-` 前缀，**例外**：
- 以 `fa-` 开头的 class（FontAwesome 图标）保留原名
- 以 `note-` 开头的 class 保留原名
- `monospace` 保留原名
- 当 `config['MESSAGE_ALLOW_SYSTEM_UI']` 为 true 且节点包含 `menu_button` class 时，整个节点豁免

```javascript
// chats.js:1926-1939（直读）
case 'class': {
    data.attrValue = data.attrValue.split(' ').map((v) => {
        if (v.startsWith('fa-') || v.startsWith('note-') || v === 'monospace') {
            return v;  // 豁免
        }
        return 'custom-' + v;  // 所有其他 class 加前缀
    }).join(' ');
}
```

**实际影响**：正则注入 `<div class="foo">` → DOM 中变为 `<div class="custom-foo">`。**角色卡若在 JS 里用 `.querySelector('.foo')` 或 CSS 用 `.foo {}` 选择器，将全部失效。须改用 `.custom-foo` 或 `data-` 属性选择。**

---

**机制 B：`<style>` 块内的 CSS 选择器前缀（CSS AST 重写）** | **high [运行时源码]**（chats.js:562-598 直读）

`decodeStyleTags()` 在将 `<custom-style>` decode 回 `<style>` 时，通过 CSS AST 将所有选择器加 `.mes_text ` 前缀（如 `.foo` → `.mes_text .foo`）。同时将选择器中的 `.foo` 改写为 `.custom-foo`（与机制 A 一致）。目的是防止跨楼层 CSS 污染。

**「Issue #3844 not planned」的正确解读**：该 Issue 申请的是给 class 加前缀的功能，官方"not planned"指的是拒绝此功能——但实际上相关行为已经通过另外方式（DOMPurify hook + CSS AST 重写）实现，只是与 Issue 描述的方案不同。

---

## 9. Showdown Markdown 配置

> 来源：ST 源码推断 + 官方文档 | 适用版本：v1.14.0+ | **medium·推导未验证 [推断] + [文档·单源]**
>
> 真机验证路径：发送含单个 `\n` 的段落内文本，检查是否渲染为 `<br>`（simpleLineBreaks）；发送 `~~text~~` 检查删除线。

ST 中 showdown 的关键非默认配置：

| 选项 | ST 中的值 | 与 showdown 默认对比 |
|---|---|---|
| `simpleLineBreaks` | **true** | showdown 默认 false；**ST 打开了它** |
| `tables` | true | 默认 false |
| `strikethrough` | true | 默认 false |
| `ghCodeBlocks` | true | 默认 true（一致） |
| `noHeaderId` | true | 默认 false |

**核心陷阱**：`simpleLineBreaks: true` 导致段落内单个 `\n` 变 `<br>`，与自定义 XML 标签结合时（见 8.2 节）会产生意外 `<br>`。自定义 XML 标签内部的多行内容会被 `<br>` 劫持格式。

---

## 10. 变量系统与宏交互

> 来源：`variables.js` + `macros.js` 源码 | 适用版本：v1.14.0+ | **high [运行时源码]**

### 三层存储与宏映射

| 层级 | 宏前缀 | 存储位置 | 持久性 |
|---|---|---|---|
| Scoped | 无直接宏（只在 `SlashCommandScope` 内可见） | `SlashCommandScope.variables` | 作用域生命周期 |
| Local | `{{getvar::}}` / `{{.varname}}` | `chat_metadata.variables`（JSONL 文件） | 聊天会话持久 |
| Global | `{{getglobalvar::}}` / `{{$varname}}` | `extension_settings.variables.global`（settings.json） | 应用全局持久 |

### resolveVariable() 查找链

```
{{getvar::name}} 触发 resolveVariable()
  scoped → local (chat_metadata) → global (extension_settings) → 返回原字符串名（静默回退）
```

**危险**：找不到时**静默回退**返回原变量名字符串，不报错、不抛出。这会导致变量名拼写错误被静默吞掉，建议用 `{{hasvar::name}}` 检查，或用速记 `{{.name??defaultValue}}` 设默认值。

来源 | 适用版本 | 置信度：high [运行时源码]

---

## 11. 完整生产范式代码

> 来源：`ST开发指南DB\本地证据\星月历史\3.0.2\components\*.html` + `模块化组件\regex_suite\*` | **high [本地实战]**

### 11.1 范式 A：正则替换注入 HTML 组件

**使用场景**：包裹 AI 输出中的自定义 XML 标签，渲染为可视 HTML 组件。

`regex_object.json`（元数据）：
```json
{
  "id": "xingyue-sync-done-omni-nexus",
  "scriptName": "[美化]变量同步完成-OMNI-NEXUS",
  "findRegex": "/<UpdateVariable(?:variable)?>\\s*([\\s\\S]*?)\\s*<\\/UpdateVariable(?:variable)?>/gi",
  "placement": [2],
  "disabled": false,
  "markdownOnly": true,
  "promptOnly": false,
  "runOnEdit": false,
  "substituteRegex": 0,
  "minDepth": 0,
  "maxDepth": null
}
```

`replaceString`（HTML 模板，关键设计）：
```html
<div class="omni-sync-wrap">
<details class="omni-sync-done">
    <summary>
        <span class="omni-sync-dot">●</span>
        〔 OMNI-NEXUS // DATA STREAM 〕
        <span>COMPLETE</span>
    </summary>
    <div class="omni-sync-body">
        $1
    </div>
</details></div>
<style>
.omni-sync-wrap { width: 95%; margin: 12px auto; }
.omni-sync-done {
  background: linear-gradient(180deg, #0a1422 0%, #050912 100%);
  border: 1px solid #2a4858;
  border-left: 3px solid #4fd97a;
}
.omni-sync-dot { color: #4fd97a; }
.omni-sync-body { max-height: 250px; overflow-y: auto; }
.omni-sync-done summary::marker { display: none; }
</style>
```

**关键设计要点：**
- `placement: [2]` + `markdownOnly: true` → 仅在 AI 回复的 display 层运行，不影响发给模型的原始文本
- `$1` 承载捕获组内容（原始 XML 标签体）
- `<style>` 块经 encodeStyleTags→ADD_TAGS→decodeStyleTags 管线保护，最终在 DOM 中正常渲染（CSS 选择器会被加 `.mes_text ` 前缀且 class 改为 `.custom-*`，见 8.3 节 B 机制）
- 关键视觉全部由 `<style>` 块 + class 承担，不依赖目标版本是否保留内联 `style=`

### 11.2 范式 B：占位符 + 状态栏 iframe 注入

**使用场景**：把状态栏注入到 AI 消息的 UpdateVariable 块位置，通过 iframe 完全绕开 DOMPurify。

**两步正则链式执行**（通过 `beforeScriptName` 控制先后顺序）：

Step 1 — 占位符正则（先跑，将 UpdateVariable 块用占位符包裹）：
```
findRegex:
  /(?:<StatusPlaceHolderImpl\s*\/>\s*)*(<UpdateVariable(?:variable)?>\s*[\s\S]*?\s*<\/UpdateVariable(?:variable)?>)(?:\s*<StatusPlaceHolderImpl\s*\/>)*/gi

replaceString:
  <StatusPlaceHolderImpl/>
  $1
```

Step 2 — 状态栏美化正则（后跑，将占位符替换为完整 iframe HTML）：
```
findRegex: /<StatusPlaceHolderImpl\s*\/>/g

replaceString: (完整状态栏 HTML，含 Blob URL + iframe 挂载逻辑，约 95 行)
```

**Blob URL iframe 核心逻辑（概要）：**
```html
<script>
(function() {
    const htmlContent = `<!-- 完整状态栏 HTML -->`;
    const blob = new Blob([htmlContent], { type: 'text/html' });
    const url = URL.createObjectURL(blob);
    const iframe = document.createElement('iframe');
    iframe.src = url;
    iframe.style.cssText = 'width:100%;border:none;...';
    // ...挂载逻辑
})();
</script>
```

### 11.3 范式 C：对话气泡正则（data-* 属性绕过 DOMPurify）

**使用场景**：把 `<xy-dialog>` 自定义标签替换为携带数据的 span，由后续 JS 处理视觉渲染。

```
findRegex:
  /<xy-dialog\s+speaker=["']([^"']+)["'](?:\s+avatar=["']([^"']*)["'])?\s*>([\s\S]*?)<\/xy-dialog>/gi

replaceString:
  <span class="xy-dialog-render"
        data-xy-dialog-render
        data-xy-dialog-speaker="$1"
        data-xy-dialog-avatar="$2">
    <textarea class="xy-dialog-content-source"
              data-xy-dialog-content-source
              hidden readonly>$3</textarea>
  </span>
```

**核心设计**：不把动态数据拼入样式字符串，改用 `data-*` 存储，再由受信 JS 读取并渲染。这样避免依赖内联 `style=` 的版本差异，也降低样式注入风险。

### 11.4 范式 D：first_mes 开局页占位 HTML

**使用场景**：在开局消息（`first_mes`）字段中放置唯一短标记，由显示正则展开为本地 loading/error mount，再由受信脚本异步加载声明式开局页。

```html
<div data-xy-opening-remote="1">
  <div data-xy-opening-title>星月私立高等学院</div>
  <div data-xy-opening-loading>
    开局界面加载中…<br>
    首次打开需从云端载入开局页，请稍候。
  </div>
</div>
```

**注意**：消息 HTML 的内联样式、`<style>` 和 class 改名行为随 ST 渲染管线版本变化；本地 v1.14.0 历史观察不能推广为永久规则。可见 fallback 不依赖单一易变样式机制，正式加载还必须有超时、hash/协议/结构校验、净化、重试和完整销毁。开局草稿、预览、提交与正常消息链统一见 **《C10_开局页与自定义开局.md》**。

---

## 12. 高频避坑清单

> 综合来源：本地实战 + 源码分析 + GitHub Issues

| # | 坑 | 根本原因 | 解法 | 置信度 |
|---|---|---|---|---|
| P1 | `style=` 内联属性在不同版本/配置表现不一致 | 消息正则、showdown、DOMPurify 与 hook 会随版本变化 | 可见 fallback 不依赖内联 style；目标版本真机检查 computed DOM | **medium [版本敏感源码]** |
| P1-b | `class="foo"` 在 DOM 中变为 `class="custom-foo"` | DOMPurify `uponSanitizeAttribute` hook 给所有 class 加 `custom-` 前缀（见 8.3 节） | JS 选择器改用 `.custom-foo`；CSS 选择器同步改写；或用 `data-*` 属性 | **high [运行时源码]**（chats.js:1926-1939 直读）|
| P2 | 自定义标签内容 `<br>` 爆炸 | simpleLineBreaks=true + DOMPurify HTMLUnknownElement hook 双重转换（Chromium 136+ 更严重） | 正则提前消费自定义标签，不让裸标签进 showdown；或在 ADD_TAGS 中声明 | **high [运行时源码]**（chats.js:1942-1976 直读）|
| P3 | `{{pick}}` 切换引擎后结果全变 | 新旧引擎 hash 算法不同，不兼容 | 确定引擎后不切换；版本迁移要告知用户 | **medium·推导未验证 [文档·多源]**（双引擎在 v1.14.0 不存在，该坑仅适用更高版本）|
| P4 | 嵌套宏 `{{getvar::{{char}}_x}}` 不生效 | 旧引擎不支持嵌套宏 | 确认目标版本已有新引擎且 `experimental_macro_engine` 已开启后再使用 | **medium·推导未验证 [文档·多源]**（v1.14.0 无双引擎）|
| P5 | 正则注入的 HTML 被 showdown 加 `<br>` | 正则在 showdown 前执行（markdownOnly:true），HTML 标签内换行被转换 | 注入 HTML 时保持标签紧凑，换行内容改用 `data-` 属性 | **high [运行时源码]**（script.js:1674 直读，markdownOnly:true 在 showdown 前跑）|
| P6 | 变量名找不到时静默返回原字符串 | `resolveVariable()` 的 fallback 行为 | 读前用 `{{hasvar::name}}` 检查；或速记 `{{.name??defaultValue}}` | **high [运行时源码]**（variables.js 直读）|
| P7 | `markdownOnly:true` 脚本在流式生成中重复跑 | ST 每次新增 token 都重新渲染，脚本重复执行 | 确保正则替换幂等（替换前先检测目标是否已存在） | **medium·推导未验证 [推断]**（逻辑合理但无文档确认）|
| P8 | `<style>` 块选择器被自动加 `.mes_text ` 前缀且 class 被改为 `.custom-*` | `decodeStyleTags()` 通过 CSS AST 重写选择器，防跨楼层污染（见 8.3 节 B 机制） | 编写 `<style>` 时直接写 `.custom-foo` 而不是 `.foo`；利用前缀隔离而不是绕开它 | **high [运行时源码]**（chats.js:553-702 直读）|

---

## 13. 悬而未决：未决项与真机验证路径

| 编号 | 悬案 | 置信度 | 来源类型 | 真机验证路径 |
|---|---|---|---|---|
| U1 | **内联 `style=` 在目标 ST 版本/配置是否保留**：v1.14.0 历史审查曾判断删除，但当前 release 路线不能由该 hook 单独推出全版本结论 | medium `[版本敏感源码]` | 消息渲染配置会变化 | 在最低支持版与当前版发出同一含 style/class 的消息，检查最终 DOM/computed style |
| U2 | ~~`<style>` 标签是否在 DOMPurify `ADD_TAGS` 中声明或默认保留~~ **【已解答】`<style>` 标签在 DOMPurify 前被 encodeStyleTags() encode 为 `<custom-style>`，`ADD_TAGS: ['custom-style']` 保留，DOMPurify 后再 decode 回 `<style>`。不是直接保留，而是 encode-decode 方案。见 8.1 节。** | **high [运行时源码]**（chats.js 直读） | — | — |
| U3 | **`messageFormatter.afterMarkdown` hook 注入的内容如何经过内联 style 处理与 class 加前缀** | medium·推导未验证 [推断] | Hook 在 DOMPurify 前执行（若该 API 存在），推测会受同一净化链影响；但 v1.14.0 无此 API | 在高版本 ST 中于 afterMarkdown hook return 含 `style=`/`class=` 的 HTML，F12 检查最终 DOM |
| U4 | **正则脚本中 `<script>` 标签是否被 DOMPurify 删除** | medium·推导未验证 [推断]（原标注「high 猜测」措辞不当，猜测不能标 high） | DOMPurify 默认 FORBID_TAGS 含 script；但具体 ST 配置未见显式声明；生产卡用 iframe+blob 绕开 | 在 replaceString 中写 `<script>document.title='test'</script>` 发出，检查 document.title 是否被修改 |
| U5 | ~~`{{if}}` 条件宏是在 macros.js 还是 instruct-mode.js 中实现~~ **【已部分解答】v1.14.0 macros.js 的 evaluateMacros() 中无 `{{if}}` 实现；instruct-mode.js 的 getInstructMacros() 只返回 instruct 模板字段宏。`{{if}}` 不在 v1.14.0 中存在，属更高版本引入。** | **high [运行时源码]**（v1.14.0 macros.js + instruct-mode.js 直读确认缺席） | — | — |
| U6 | **`placement:[2]` + `markdownOnly:false` 时，正则是否在 DOMPurify 之后运行** | low | v1.14.0 engine.js getRegexedString() 逻辑：markdownOnly=false 时条件为 `!isMarkdown && !isPrompt`，消息渲染时 isMarkdown=true，实际**不会执行**——该坑可能是个误解 | 构造 `promptOnly:false, markdownOnly:false` 的脚本，replaceString 注入 HTML，F12 检查 DOM 是否完整保留 |
| U7 | **流式生成中正则脚本的幂等性要求是否有官方说明** | low | 仅逻辑推导，无文档明确说明流式行为 | 构造非幂等正则（如累加文本），观察流式生成结束时内容是否重复叠加 |

---

## 14. 来源索引

| 来源 | URL / 路径 | 来源类型 | 本次复核状态 |
|---|---|---|---|
| 宏官方文档 | https://docs.sillytavern.app/usage/core-concepts/macros/ | [文档·多源] | 联网复核（2026-06-28）|
| 扩展写法文档 | https://docs.sillytavern.app/for-contributors/writing-extensions/ | [文档·多源] | 联网复核（2026-06-28），确认 messageFormatter 存在于官方文档 |
| **script.js（消息渲染主函数）** | `C:\...\SillyTavern-1.14.0\public\script.js`（本地 v1.14.0） | **[运行时源码]** | **本次复核新增；messageFormatting() 主管线直读** |
| **chats.js（DOMPurify 配置）** | `C:\...\SillyTavern-1.14.0\public\scripts\chats.js`（本地 v1.14.0） | **[运行时源码]** | **本次复核新增；encodeStyleTags/decodeStyleTags/DOMPurify hook 直读** |
| **st-context.js（getContext API）** | `C:\...\SillyTavern-1.14.0\public\scripts\st-context.js`（本地 v1.14.0） | **[运行时源码]** | **本次复核新增；确认 v1.14.0 无 messageFormatter/macros 新 API** |
| macros.js 源码（本地 v1.14.0） | `C:\...\SillyTavern-1.14.0\public\scripts\macros.js` | [运行时源码] | 本次复核直读；确认 evaluateMacros 三段式管线、无双引擎 |
| instruct-mode.js 源码 | `C:\...\SillyTavern-1.14.0\public\scripts\instruct-mode.js` | [运行时源码] | 本次复核直读；getInstructMacros 仅含模板字段宏，无 {{if}} |
| engine.js（regex 插件） | `C:\...\SillyTavern-1.14.0\public\scripts\extensions\regex\engine.js` | [运行时源码] | 本次复核直读；regex_placement 枚举 + getRegexedString 直读 |
| variables.js 源码 | `C:\...\SillyTavern-1.14.0\public\scripts\variables.js` | [运行时源码] | — |
| DeepWiki 宏系统架构 | https://deepwiki.com/SillyTavern/SillyTavern/7.3-variables-macros-and-scripting | [文档·多源] | — |
| Issue #3996 Showdown+DOMPurify 双重 bug | https://github.com/SillyTavern/SillyTavern/issues/3996 | [社区] | Issue 属社区证据（非源码），本次改为 [社区] |
| Issue #3844 custom-style | https://github.com/SillyTavern/SillyTavern/issues/3844 | [社区] | Issue 属社区证据；描述部分有误（style= 改名说法不准确）|
| 1.15.0 发布说明 | https://github.com/SillyTavern/SillyTavern/discussions/4926 | [文档·单源] | — |
| 星月生产卡正则组件 | `ST开发指南DB\本地证据\星月历史\3.0.2\components\*.html` | [本地实战] | — |
| 模块化组件库正则套件 | `<local-user-path>/OneDrive\ST-\角色卡工作区\模块化组件\regex_suite\*` | [本地实战] | — |
