# 变量更新规则参考手册

> 文档版本：v1.0.0 ｜ 最后更新：2026-07-14

> 本手册为 JSONPatch + zod 正路（2026-06-28 合并定稿），旧命令式时代版见 变量更新规则_命令式时代_归档.md

---

## 置信度分级体系

**三级**：high（多源互证 / 生产卡实测）/ medium（单源文档 / 合理推断）/ low（孤证无源，标注后**禁止直接施工**）

**依据类型轴**：
- `[运行时源码]`——bundle.js / mvu_zod.js 反编译或阅读；JS-Slash-Runner / SillyTavern 本地源码
- `[本地实战]`——星月2.5.0 / 交错宙域2.6.0 生产组件
- `[文档·多源]`——Zod v4官方文档 + DeepWiki + 多篇社区文章互证
- `[文档·单源]`——单一官方或社区文档
- `[类型声明]`——exported.mvu.d.ts / event.d.ts / variables.d.ts 等本地 .d.ts 文件
- `[社区]`——社区帖子/Issue，无官方背书
- `[推断]`——基于已知信息的逻辑推导，未直接验证

**两条铁规**：
1. 仅由 `[类型声明]` 支撑的「运行时行为 / 可用性」结论，最高只能 `medium·推导未验证`——除非另有 `[运行时源码]` 或 `[本地实战]` 佐证才升 high。
2. 节级标注 = 节内条目下确界——禁止整节标 high 却内含悬案条目；冲突即拆节。

**收集日期**：2026-06-28（本次合并定稿）  
**回源环境**：SillyTavern v1.14.0 + JS-Slash-Runner v4.8.9 + 本地成熟卡（星月2.5.0 / 交错宙域2.6.0）

---

## 目录

1. [MVU 系统三层架构](#1-mvu-系统三层架构)
2. [JSONPatch (RFC 6902 方言) 完整规范](#2-jsonpatch-rfc-6902-方言-完整规范)
   - 2.1 [UpdateVariable 块结构](#21-updatevariable-块结构)
   - 2.2 [op 类型完整枚举](#22-op-类型完整枚举mvu方言非纯rfc6902)
   - 2.3 [路径语法](#23-路径语法)
   - 2.4 [顶层容器限定列表](#24-顶层容器限定列表)
3. [Zod Schema 集成](#3-zod-schema-集成)
   - 3.1 [prefault 详解](#31-prefault-详解)
   - 3.2 [registerMvuSchema API](#32-registermvuschema-api)
   - 3.3 [$meta 结构详解](#33-meta-结构详解)
   - 3.4 [MvuData.schema / SchemaNode](#34-mvudataschema--schemanode)
   - 3.5 [z 的来源与离线加载](#35-z-的来源与离线加载)
4. [zod 体系与跨实例风险](#4-zod-体系与跨实例风险)
   - 4.1 [三套 schema 体系对比](#41-三套-schema-体系对比)
   - 4.2 [registerVariableSchema 纯 UI 不拦截写入](#42-registerVariableSchema-纯-ui-不拦截写入)
   - 4.3 [跨 zod 实例风险（三实例地雷）](#43-跨-zod-实例风险三实例地雷)
5. [生命周期事件与 API](#5-生命周期事件与-api)
   - 5.1 [bundle.js 公开事件](#51-bundlejs-公开事件)
   - 5.2 [VARIABLE_INITIALIZED typo 警告](#52-variable_initialized-typo-警告)
   - 5.3 [Mvu 四个核心函数签名](#53-mvu-四个核心函数签名)
   - 5.4 [事件注册 API（JS-Slash-Runner）](#54-事件注册-apijs-slash-runner)
   - 5.5 [等待 MVU 就绪范式](#55-等待-mvu-就绪范式)
6. [ST 原生变量层对照](#6-st-原生变量层对照)
   - 6.1 [存储层分离](#61-存储层分离)
   - 6.2 [resolveVariable 三层优先级](#62-resolvevariable-三层优先级)
   - 6.3 [getLocalVariable 返回类型](#63-getlocalvariable-返回类型)
7. [完整可用范式代码](#7-完整可用范式代码)
   - 7.1 [zod_schema.js 标准模板](#71-zod_schemajs-标准模板)
   - 7.2 [mvu_zod_cn.js 标准模板](#72-mvu_zod_cnjs-标准模板)
   - 7.3 [典型 JSONPatch 输出示例](#73-典型-jsonpatch-输出示例)
8. [高频避坑](#8-高频避坑)
9. [悬案（未决项）](#9-悬案未决项)
10. [关键字段/API 参考表](#10-关键字段api-参考表)
11. [本地源文件索引](#11-本地源文件索引)

---

## 1. MVU 系统三层架构

**核心结论**：MVU（MagVarUpdate）由三层构成，各层职责分离，缺一不可。

| 层 | 载体 | 职责 |
|---|---|---|
| **世界书条目** | `[mvu_update]`标记的WI条目 | 向LLM传递变量JSON + 更新规则/schema描述 |
| **bundle脚本** | `artifact/bundle.js` | 解析LLM输出的`<UpdateVariable>`块，执行JSONPatch操作，触发生命周期事件 |
| **zod schema脚本** | `zod_schema.js` | 定义变量结构约束，注册到MVU做变量合法性验证 |

**变量存储结构**（MvuData）：

| 变量 | 用途 | 注入给 LLM |
|---|---|---|
| `stat_data` | 下一轮 LLM 读取当前状态 | 是（通过世界书条目） |
| `display_data` | 前端状态栏渲染（纯值/纯对象，无二元组） | 否 |

**VWD [value, desc] 二元组状态 — 核心结论**：**VWD [value, desc] 格式已在实战卡中彻底废弃。** 本地 InitialVariables.json（星月2.5.0、交错宙域2.6.0）均为纯值结构，路径直接指向值本身（如 `/环境/时间`），不是 `/环境/时间/0`。描述职责已完全迁移到 zod schema 的 `.describe()` 和 `prefault` 机制。bundle.js 仍保留 VWD 检测代码仅用于兼容旧卡，新卡**不应**生产此格式。

**CDN 地址**：

```
# 主镜像
https://cdn.jsdelivr.net/gh/MagicalAstrogy/MagVarUpdate/artifact/bundle.js

# 备用镜像（部分生产卡以此为主，如星月2.5.0 mvu_zod_cn.js）
https://testingcf.jsdelivr.net/gh/MagicalAstrogy/MagVarUpdate/artifact/bundle.js
```

> 实测：星月2.5.0 的 mvu_zod_cn.js 第一选择是 testingcf 镜像，mvu_zod_global.js 第一选择是 cdn.jsdelivr.net。两者内容相同，按网络情况二选一，不存在绝对的"生产推荐"之分。

来源：`[运行时源码]` + `[本地实战]`（星月2.5.0 mvu_zod_cn.js / mvu_zod_global.js 实测） | 置信度：**high**

---

## 2. JSONPatch (RFC 6902 方言) 完整规范

### 2.1 UpdateVariable 块结构

**核心结论**：LLM输出必须包裹在 `<UpdateVariable>` 块内，分 analysis 和 JSONPatch 两段。

```xml
<UpdateVariable>
<analysis>
变量预分析（详述正文中哪些状态发生了变化，每行对应至少一个path）
</analysis>
<JSONPatch>
[
  {"op":"replace","path":"/环境/时间","value":"14:25"},
  {"op":"add","path":"/任务/任务名","value":{...完整对象...}}
]
</JSONPatch>
</UpdateVariable>
```

**bundle.js 标签解析正则**：

```javascript
/<(update(?:variable)?|variableupdate)>/gi  // 外层匹配（大小写不敏感）
we(t, 'Analysis')        // 提取 analysis 内容
we(t, 'json_?patch')     // 提取 JSONPatch 内容（匹配 JSONPatch 和 json_patch 两种写法）
```

> 注：`we()` 函数实现为 `e.match(new RegExp('^\\s*<${t}>\\s*([\\s\\S]*?)\\s*</${t}>\\s*$','i'))`，第二参数传 `'json_?patch'`（正则模式），因此同时兼容 `<JSONPatch>` 和 `<json_patch>` 两种标签写法。

来源：`[运行时源码]`（bundle.js CDN 实测） | 置信度：**high**

---

### 2.2 op 类型完整枚举（MVU方言，非纯RFC6902）

**核心结论**：MVU支持6种op，但LLM输出层只应使用4种。

| op | RFC6902标准 | MVU特有 | LLM可用 | 说明 |
|---|---|---|---|---|
| `replace` | ✅ | — | ✅ | 替换已存在路径的值；路径不存在则报错 |
| `add` | ✅ | 扩展 | ✅ | 新增字段/record项；也用于追加数组元素 |
| `remove` | ✅ | — | ✅ | 删除指定路径 |
| `move` | ✅ | — | ✅ | 字段迁移，`from`→`path`（或`to`） |
| `delta` | ❌ | ✅ | ❌ | MVU专有：数值增量操作，`value`须为number；等同`field += value` |
| `insert` | ❌ | ✅ | ❌ | MVU专有：在数组指定位置插入；与`add`等价，在anyOf中合并处理 |

**关键区别**：

- `delta`：`{"op":"delta","path":"/user/体力","value":-10}` → 在当前值基础上加减
- `add` vs `insert`：功能几乎等价（两者在 anyOf 中合并为 `'insert'|'add'`），均可新增record/追加数组
- `replace`：**路径必须已存在**，否则禁止使用；路径不存在时必须用 `add`

**本地 mvu_update_full.txt 规则**（星月2.5.0）明确只允许：`add、replace、remove、move`，未包含 delta 和 insert。

来源：`[运行时源码]`（bundle.js 确认 delta/insert op 存在）+ `[本地实战]`（星月2.5.0 mvu_update_full.txt 规则文本确认 LLM 只用4种 op）| 置信度：op 技术存在：**high**；"delta/insert LLM不可用"结论：**high**（来自生产卡世界书强制规则文本，属规则层而非 bundle 强制拦截）

---

### 2.3 路径语法

**核心结论**：路径格式为 `/顶层容器/键名/子字段`，使用 JSON Pointer 风格的 `/` 分隔符。

| 规则 | 说明 |
|---|---|
| 分隔符 | `/`（JSON Pointer风格） |
| 数组追加 | `/-` 等效于数组末尾追加（bundle.js 中 `'-' === o ? c.length : o`） |
| 转义 | 使用 `lodash.toPath()` 而非标准 RFC6902 的 `~0`/`~1` 转义 |
| 中文键名 | 直接使用，不需转义 |

**嵌套路径示例**：

```
/环境/时间                          → string字段
/user/基本属性/等级                  → 嵌套对象字段
/当前互动角色/张三/衣着/上装          → record下的子字段
/当前互动角色/张三                    → 整个record项（add/replace完整对象）
/成就/-                              → 追加到数组末尾
```

来源：`[运行时源码]` + `[本地实战]` | 置信度：**high**

---

### 2.4 顶层容器限定列表

**核心结论**：以下为本地 mvu_update_full.txt 中的合法顶层容器（完整列表）。

```
环境、user、天赋与技能、持有物、仓库、雷达系统、当前互动角色、
关系网、羁绊角色、规则变化、道具放置列表、莉莉丝的商店、居所、任务、近期事件、载具
```

来源：`[本地实战]`（星月2.5.0 mvu_update_full.txt） | 置信度：**high**

> 注意：顶层容器列表因卡而异，上表为星月2.5.0的定义，其他卡以其世界书条目为准。

---

## 3. Zod Schema 集成

### 3.1 prefault 详解

**核心结论**：`prefault` 是 Zod v4 的**标准内置方法**（非prototype扩展、非TavernHelper注入），与 `default` 和 `catch` 有本质区别。

**定义**："pre-parse default"——在验证前应用后备值。当输入为 `undefined` 时，prefault 值被送入完整的验证管道（包括transform）。

```javascript
// prefault vs default 的关键区别
z.string().transform(val => val.length).prefault("tuna")
  .parse(undefined)  // → 4（"tuna"经过transform变为4）

z.string().transform(val => val.length).default("tuna")
  .parse(undefined)  // → "tuna"（绕过transform直接返回默认值）
```

**三者对比**：

| 方法 | 触发条件 | 后备值经过验证pipeline | 适用场景 |
|---|---|---|---|
| `prefault(v)` | 输入为 `undefined` | ✅ 是 | 后备值需经transform处理时 |
| `default(v)` | 输入为 `undefined` | ❌ 否（绕过） | 后备值已是最终类型时 |
| `catch(v)` | **任何验证失败** | ❌ 否 | 不可信输入的全兜底 |

**在角色卡中的惯用法**：

```javascript
// 字符串字段：undefined → ''
z.string().prefault('')

// 对象字段：undefined → {}（再经内部字段各自prefault）
z.object({ 上装: z.string().prefault('') }).prefault({})

// 数值字段（配合coerce）：undefined → 0
z.coerce.number().transform(v => _.clamp(v, 0, 100)).prefault(0)

// 布尔字段
z.coerce.boolean().prefault(false)
```

**`passthrough()` 的作用**：允许 schema 对象接受未声明的额外字段。LLM可能写入任意扩展字段时使用。

来源：`[文档·多源]`（Zod v4官方文档 + DeepWiki + Medium文章）+ `[本地实战]` | 置信度：**high**（prefault/default/catch 三者行为对比）；prefault 在 z.preprocess 嵌套中的触发顺序：**medium·推导未验证**（见 §9 悬案 7.3）

---

### 3.2 registerMvuSchema API

**核心结论**：`registerMvuSchema` 由 StageDog/tavern_resource 的 mvu_zod.js 提供，是连接 zod schema 与 MVU 变量管理器的桥接层。

**导入与注册**：

```javascript
// 1. 导入（带fallback）
let registerMvuSchema;
try {
  ({ registerMvuSchema } = await import(
    'https://cdn.jsdelivr.net/gh/StageDog/tavern_resource/dist/util/mvu_zod.js'
  ));
} catch (error) {
  ({ registerMvuSchema } = await import(
    'https://testingcf.jsdelivr.net/gh/StageDog/tavern_resource/dist/util/mvu_zod.js'
  ));
}

// 2. 定义schema（必须是z.object(...)，对应stat_data层级）
export const Schema = z.preprocess(normalizeRoot, z.object({
  环境: z.object({...}).prefault({}),
  user: z.object({...}).prefault({}),
  // ...其他顶层容器
}));

// 3. 注册（必须在$()回调中）
$(() => { registerMvuSchema(Schema); });
```

**内部原理（mvu_zod.js实现）**：

```javascript
function registerMvuSchema(r) {
  const schema = () => {
    const t = typeof r === 'function' ? r() : r;
    const e = t instanceof z.ZodObject ? z.looseObject(t.shape) : t;
    // 包装为 {stat_data: schema} 注册到MVU变量管理器
    if (typeof registerVariableSchema === 'function') {
      registerVariableSchema(z.object({ stat_data: e }), { type: 'message' });
    }
    return e;
  };
  // 监听MVU生命周期事件，在变量初始化/命令解析/更新结束时执行验证
}
```

**Zod import URL（mvu_zod.js内部）**：

```
https://testingcf.jsdelivr.net/npm/zod/v4/core/+esm
```

**`registerVariableSchema`（底层函数）**：由 JS-Slash-Runner（TavernHelper v4.3.7+）注入到全局环境，`mvu_zod.js` 只是检测并调用它。

来源：`[运行时源码]`（mvu_zod.js CDN 实测）+ `[本地实战]`（星月2.5.0 zod_schema.js）+ `[文档·单源]`（TavernHelper官方更新日志，v4.3.7 明确记录"新增 registerVariableSchema 函数"）| 置信度：**high**

---

### 3.3 $meta 结构详解

**核心结论**：`$meta` 是变量 JSON 中可附加的元信息对象，控制 MVU 对该变量的处理行为。**生产卡中不使用**（依赖 zod schema 约束结构）。

| 字段 | 类型 | 说明 |
|---|---|---|
| `$meta.extensible` | boolean | 对象/数组是否允许添加未在schema中声明的新字段 |
| `$meta.recursiveExtensible` | boolean | 递归地对所有子字段启用extensible |
| `$meta.template` | object/array | 模板，新增数组/对象元素时的默认结构 |
| `$meta.required` | string[] | 必需字段名数组（缺失时验证失败） |
| `$meta.strictTemplate` | boolean | 严格模板模式（新值必须符合template结构） |
| `$meta.strictSet` | boolean | 严格设置模式（禁止添加template以外的字段） |
| `$meta.concatTemplateArray` | boolean | 数组模板时是否拼接（true）还是替换（false） |

来源：`[运行时源码]`（bundle.js CDN 摘要读取） | 置信度：字段名称存在：**medium·单源孤证**；各字段具体行为：**medium·推导未验证**（生产卡不用 $meta，无真机验证）

---

### 3.4 MvuData.schema / SchemaNode

**核心结论**：bundle.js 中未找到独立的 `MvuData.schema(SchemaNode)` 公开 API。实际使用路径是通过 `registerMvuSchema` 注册 Zod schema 对象，bundle 内部通过 `mag_command_parsed_for_zod` 事件处理验证和路径操作。

`SchemaNode` 是 Zod schema 对象树的节点类型（ZodObject、ZodString、ZodNumber等），无独立自定义类型。

来源：`[运行时源码]`（bundle.js CDN 摘要）+ `[推断]` | 置信度：**medium·推导未验证**

---

### 3.5 z 的来源与离线加载

**核心结论**：**在卡的 zod_schema.js 中，`z` 是预置全局变量**，由 TavernHelper（JS-Slash-Runner v3.3.2+）在前端/脚本环境中自动注入，不需要显式 import z。

```javascript
// 正确做法（星月/交错实战卡）：直接用 z.object() 等
const Schema = z.preprocess(normalizeRoot, z.object({...}));

// 只需 import registerMvuSchema 这个 helper
let registerMvuSchema;
try {
  ({ registerMvuSchema } = await import(
    'https://cdn.jsdelivr.net/gh/StageDog/tavern_resource/dist/util/mvu_zod.js'
  ));
} catch (e) {
  ({ registerMvuSchema } = await import(
    'https://testingcf.jsdelivr.net/gh/StageDog/tavern_resource/dist/util/mvu_zod.js'
  ));
}
```

来源：`[本地实战]`（星月2.5.0 / 交错2.6.0 均直接使用全局 z）+ `[文档·单源]`（TavernHelper官方更新日志，v3.3.2 明确记录"为前端和脚本默认置入了 zod 库"）| 置信度：**high**

---

## 4. zod 体系与跨实例风险

### 4.1 三套 schema 体系对比

**核心结论（已回源坐实）**：「变量 schema」在 ST 生态里有**三套完全独立、互不调用**的体系，重建时务必认清当前用的是哪套（混淆是 zod_schema.js bug 的根源领域）：

| 体系 | API / 形态 | 作用 | 运行时是否拦截写入 |
|---|---|---|---|
| **JS-Slash-Runner** | `registerVariableSchema(schema, {type})` 单函数，type ∈ 5 种作用域 | 仅变量管理器 UI 校验显示 | 否（纯 UI） |
| **MagVarUpdate (MVU)** | 内嵌 `MvuData.schema`（自定义 `SchemaNode` 类型，**非 zod**），写在变量结构里 | 指导路径校验 / 模板填充 / extensible 控制 | 是（运行时起效） |
| **ST-Prompt-Template** | `setVariableSchema()`（EJS 路线，已废弃） | EJS 写入前拦截校验 | 是 |

来源：`[运行时源码]`（本地 variables.d.ts JSDoc 逐字核实）| 置信度：JS-Slash-Runner 侧 **high**；MVU SchemaNode 侧 **medium·单源孤证**（MVU 私有仓，本地无佐证）

---

### 4.2 registerVariableSchema 纯 UI 不拦截写入

**核心结论（已回源坐实）**：`registerVariableSchema` **仅影响变量管理器 UI 的校验显示**，对代码层面零影响——不影响 `parseMessage` / `replaceMvuData` 的实际写入，不会在运行时拦截非法写入。

依据 `@types/function/variables.d.ts` 的 JSDoc 原文：「这只是方便使用变量管理器这一 UI 查看和管理变量，对于代码层面没有任何影响」

它是**单一函数**靠 `option.type`（global/preset/character/chat/message）切换作用域，**不存在** `registerGlobalVariableSchema` 等多个独立函数。

```typescript
// 签名
function registerVariableSchema(
  schema: z.ZodType<any>,
  option: { type: 'global' | 'preset' | 'character' | 'chat' | 'message' }
): void;
```

来源：`[运行时源码]`（本地 variables.d.ts 行190 JSDoc + TavernHelper CHANGELOG v4.3.7）| 置信度：**high**

---

### 4.3 跨 zod 实例风险（三实例地雷）

**核心结论**：JS-Slash-Runner 内置 zod `^4.4.3`、MVU（mvu_zod.js）内部从 CDN 加载 zod `v4/core`，卡内若再从 CDN 引第三个 zod → `instanceof` 跨实例必失败、`._def` 取不到、`parse` 静默产出 NaN/undefined。

**铁律：卡内一律复用 `window.z`（JS-Slash-Runner 注入的同一实例），绝不自带 CDN zod。**

```javascript
// 推荐：直接用全局 z（JS-Slash-Runner 挂载的 Zod v4）
const Schema = z.object({ 积分: z.number() });

// 危险：从 CDN 引入独立 Zod（可能是不同 v4 patch 或 v3）
// import { z } from 'https://cdn.jsdelivr.net/npm/zod@3/...'; // 禁止
```

**Zod v4 重大 API 变化**：`._def` 内部属性迁移到 `._zod.def`；`ZodType` 类不再互通。跨版本实例 `instanceof z.ZodType` 必然失败。

**暴露机制**：

```typescript
// src/third_party_object.ts
import * as z_object from 'zod';
globalThis.z = z_object;  // 挂到 window.z

// src/iframe/predefine.js
result = result.merge(_.pick(window.parent, ['EjsTemplate', 'TavernHelper', 'YAML', 'showdown', 'toastr', 'z']));
// iframe 内的 window.z 与 parent.z 是同一对象引用
```

来源：`[运行时源码]`（本地 third_party_object.ts + iframe/predefine.js + package.json 逐字核实）| 置信度：**high**

---

## 5. 生命周期事件与 API

### 5.1 bundle.js 公开事件

**核心结论**：bundle.js（master版）中 v 对象定义了 6 个公开事件，另有 2 个 mvu_zod.js 内部事件；另外 bundle.js 还有 2 个底层调度常量（`mag_invoke_mvu`、`mag_update_variable`）不属于公开生命周期 API。

| 事件名 | 触发时机 | 公开 | 用途 |
|---|---|---|---|
| `mag_variable_initialized` | 变量初始化完成后 | ✅ | 第一次从WI加载InitialVariables完成时 |
| `mag_variable_update_started` | 识别到`<UpdateVariable>`时 | ✅ | 开始解析前的钩子 |
| `mag_command_parsed` | 每个JSONPatch命令被解析后 | ✅ | 可以拦截/修改单条patch |
| `mag_variable_update_ended` | 所有patch执行完成后 | ✅ | 更新完成钩子（**最常用**） |
| `mag_before_message_update` | 消息内容修改前 | ✅ | 预更新钩子 |
| `mag_variable_updated` | 单个变量发生变化时 | ✅ | 细粒度监听单变量变化 |
| `mag_command_parsed_for_zod` | （mvu_zod.js内部）命令解析时 | ❌ | schema验证逻辑触发点 |
| `mag_command_parsed_ended_for_zod` | （mvu_zod.js内部）命令解析结束时 | ❌ | 清理临时数据 |

来源：`[运行时源码]`（bundle.js + mvu_zod.js CDN 实测）+ `[本地实战]`（星月2.5.0 mvu_zod_cn.js） | 置信度：公开6事件 **high**；mvu_zod.js 内部事件名 `mag_command_parsed_ended_for_zod` **medium·单源孤证**（仅 WebFetch 摘要）

---

### 5.2 VARIABLE_INITIALIZED typo 警告

**极重要的拼写 typo**（`high [类型声明]`，本地 `exported.mvu.d.ts` 行57 直读确认）：`VARIABLE_INITIALIZED` 对应的字符串值是 `'mag_variable_initiailized'`（**多一个 'i'**：ini-t-i-**a-i**-lized），这是上游源码定义，不是笔误。**必须用常量引用，绝不能硬编码字符串**。

```typescript
Mvu.events = {
  VARIABLE_INITIALIZED:    'mag_variable_initiailized',  // ← typo，多一个 i
  VARIABLE_UPDATE_STARTED: 'mag_variable_update_started',
  COMMAND_PARSED:          'mag_command_parsed',
  VARIABLE_UPDATE_ENDED:   'mag_variable_update_ended',
  BEFORE_MESSAGE_UPDATE:   'mag_before_message_update',
};
```

> 注意：`.d.ts` 中的 typo 是否就是实际运行时事件字符串，见 §9 悬案 P2。

**事件回调签名**（精确，来自 `exported.mvu.d.ts`）：

```typescript
interface ListenerType {
  [Mvu.events.VARIABLE_INITIALIZED]:    (variables: Mvu.MvuData, swipe_id: number) => void;
  [Mvu.events.VARIABLE_UPDATE_STARTED]: (variables: Mvu.MvuData) => void;
  [Mvu.events.COMMAND_PARSED]:          (variables: Mvu.MvuData, commands: Mvu.CommandInfo[], message_content: string) => void;
  [Mvu.events.VARIABLE_UPDATE_ENDED]:   (variables: Mvu.MvuData, variables_before_update: Mvu.MvuData) => void;
  [Mvu.events.BEFORE_MESSAGE_UPDATE]:   (context: { variables: Mvu.MvuData; message_content: string }) => void;
}
```

来源：`[类型声明]`（本地 exported.mvu.d.ts 逐字核实）| 置信度：**high**

---

### 5.3 Mvu 四个核心函数签名

```typescript
declare const Mvu: {
  // 同步读取指定作用域的变量（类型声明为同步，底层行为需真机确认，见 §9 悬案 P4）
  getMvuData: (options: VariableOption) => Mvu.MvuData;

  // 异步完整替换指定作用域的变量（是完整替换，非 merge）
  replaceMvuData: (mvu_data: Mvu.MvuData, options: VariableOption) => Promise<void>;

  // 异步解析包含命令的字符串并应用到 old_data，返回新 MvuData
  // 有变量被更新→返回新 MvuData；否则→返回 undefined。必须做 null check。
  parseMessage: (message: string, old_data: Mvu.MvuData) => Promise<Mvu.MvuData>;

  // 同步，是否处于「额外分析模型」解析轮次
  isDuringExtraAnalysis: () => boolean;

  events: { /* 见 5.2 节 */ };
};
```

**VariableOption 完整类型**：

```typescript
type VariableOption =
  | { type: 'chat' | 'preset' | 'global' }
  | { type: 'character' }
  | { type: 'message'; message_id?: number | 'latest' }  // 负数=深度索引，-1=最新楼层
  | { type: 'script'; script_id?: string }
  | { type: 'extension'; extension_id: string };         // extension_id 必填
```

**标准三件套：读取-解析-写回**：

```javascript
await waitGlobalInitialized('Mvu');

const old_data = Mvu.getMvuData({ type: 'message', message_id: 'latest' });
const new_data = await Mvu.parseMessage(
  "_.set('角色.好感度', 30);//强制修改",
  old_data
);
if (new_data) {  // 必须做 null check：无变量被更新时返回 undefined
  await Mvu.replaceMvuData(new_data, { type: 'message', message_id: 'latest' });
}
```

**VARIABLE_UPDATE_ENDED 状态栏刷新（最常用挂点）**：

```javascript
await waitGlobalInitialized('Mvu');

eventOn(Mvu.events.VARIABLE_UPDATE_ENDED, (variables, variables_before_update) => {
  // variables.stat_data 是更新后全量变量
  // variables_before_update.stat_data 是更新前快照
  renderStatusBar(variables.stat_data);
});
```

**COMMAND_PARSED 拦截命令（in-place 修改，不能 return）**：

```javascript
await waitGlobalInitialized('Mvu');

eventOn(Mvu.events.COMMAND_PARSED, (variables, commands, message_content) => {
  const filtered = commands.filter(cmd => cmd.type !== 'delete');
  commands.splice(0, commands.length, ...filtered);  // 必须 in-place 修改
  // return 值被忽略，必须 splice 原数组
});
```

来源：`[类型声明]`（本地 exported.mvu.d.ts 逐字核实）| 置信度：函数签名 / 事件名常量 / 回调签名 **high**；运行时行为（同步性 / 返回值 / in-place 语义）**medium·推导未验证**，见 §9 悬案 P3–P5

---

### 5.4 事件注册 API（JS-Slash-Runner）

来源：`@types/iframe/event.d.ts`（TH v4.8.13；本地实测 v4.8.9，内容逐字吻合）| 置信度：**high [运行时源码]**

```typescript
// 注册事件监听（已注册则静默无效）
declare function eventOn<T extends EventType>(event_type: T, listener: ListenerType[T]): EventOnReturn;

// 保证该 listener 最先运行（已注册则调整顺序）
declare function eventMakeFirst<T extends EventType>(event_type: T, listener: ListenerType[T]): EventOnReturn;

// 保证该 listener 最后运行（已注册则调整顺序）
declare function eventMakeLast<T extends EventType>(event_type: T, listener: ListenerType[T]): EventOnReturn;

// 仅触发一次后自动取消
declare function eventOnce<T extends EventType>(event_type: T, listener: ListenerType[T]): EventOnReturn;

type EventOnReturn = { stop: () => void; };

// 清理 API（关闭时自动卸载，通常不需手动调用）
declare function eventRemoveListener<T extends EventType>(event_type: T, listener: ListenerType[T]): void;
declare function eventClearEvent(event_type: EventType): void;
declare function eventClearListener(listener: Function): void;
declare function eventClearAll(): void;
```

**iframe_events vs tavern_events 的 `js_` 前缀区别（禁止混用）**：

```typescript
iframe_events.GENERATION_STARTED = 'js_generation_started';    // 注意有 js_ 前缀
tavern_events.GENERATION_STARTED  = 'generation_started';      // 无前缀
```

**废弃 API**：`eventOnButton` 已标注 `@deprecated`，改用 `eventOn(getButtonEvent('按钮名称'), handler)`。

---

### 5.5 等待 MVU 就绪范式

来源：`[本地实战]`（星月2.5.0 mvu_zod_cn.js / mvu_zod_global.js 实测）| 置信度：**high**

```javascript
// 等待MVU就绪
const waitForMvuReady = async () => {
  if (typeof waitGlobalInitialized === 'function') return waitGlobalInitialized('Mvu');
  const started = Date.now();
  while (!window.Mvu && Date.now() - started < 8000) {
    await new Promise(resolve => setTimeout(resolve, 50));
  }
  return window.Mvu;
};

// 等待世界书WorldbookReady（多卡名兼容，已据星月2.5.0实测源码补全）
const waitXingyueWorldbookReady = async () => {
  const getReadyPromise = () => (
    window.XingyueWorldbookReady ||
    window['星月WorldbookReady'] ||
    window['星月学院WorldbookReady'] ||
    window.CrossedZoneWorldbookReady ||
    window.FusionEraWorldbookReady
  );
  // 轮询最长5s，超时后等1s再继续
};
```

---

## 6. ST 原生变量层对照

### 6.1 存储层分离

来源：`[运行时源码]`（SillyTavern release/public/scripts/variables.js 源码）| 置信度：**high**

| 命令 | 存储位置 | 作用域 |
|---|---|---|
| `/setvar` / `/setchatvar` | `chat_metadata.variables` | 当前对话 |
| `/setglobalvar` | `extension_settings.variables.global` | 跨会话全局 |
| `/let` / `/var` | `SlashCommandScope.variables`（内存） | 当前管线执行帧 |

两套持久存储**完全独立，无自动同步**：`/getvar` 只读 `chat_metadata`，`/getglobalvar` 只读 `extension_settings`，同名变量互不影响。

**注意**：`{{setvar}}` 宏在 greeting 加载时立即执行（**medium·单源孤证**，单一 GitHub issue #3263）——含 `{{setvar::key::value}}` 的 alternate greeting 在用户切换/预览时就立刻把变量写入 `chat_metadata.variables`，不等用户发消息。建议改用 STScript `/setvar` 命令在有条件管线中控制时机。

---

### 6.2 resolveVariable 三层优先级

来源：`[运行时源码]`（本地 variables.js 逐字核实）| 置信度：**high**

```javascript
// 只在特定调用路径生效（如宏替换），/getvar 命令不经过此函数
resolveVariable(name, scope):
  → SlashCommandScope（帧变量）
  → chat_metadata.variables（本地）
  → extension_settings.variables.global（全局）
  → 原样返回 name 字符串（而非 undefined）
```

---

### 6.3 getLocalVariable 返回类型

来源：`[运行时源码]`（本地 variables.js）| 置信度：**high**

```javascript
// 能转数字则转，否则返回原字符串，空值返回空字符串
return (localVariable?.trim?.() === '' || isNaN(Number(localVariable)))
  ? (localVariable || '')
  : Number(localVariable);
// 纯数字变量在 JS 层面是 number 类型，不是 string！
```

---

## 7. 完整可用范式代码

### 7.1 zod_schema.js 标准模板

来源：`<local-user-path>/OneDrive\ST-\角色卡工作区\星月 2.5.0\components\zod_schema.js`（生产验证）| 置信度：**high**

```javascript
// Step 1: import registerMvuSchema（带fallback）
let registerMvuSchema;
try {
  ({ registerMvuSchema } = await import(
    'https://cdn.jsdelivr.net/gh/StageDog/tavern_resource/dist/util/mvu_zod.js'
  ));
} catch (error) {
  ({ registerMvuSchema } = await import(
    'https://testingcf.jsdelivr.net/gh/StageDog/tavern_resource/dist/util/mvu_zod.js'
  ));
}

// Step 2: 辅助函数（normalize、isPlainObject、textOf等）
function isPlainObject(value) {
  return value && typeof value === 'object' && !Array.isArray(value);
}
function textOf(value, fallback = '') {
  if (value === undefined || value === null || value === '') return fallback;
  if (Array.isArray(value)) return value.map(v => textOf(v)).filter(Boolean).join('、');
  if (isPlainObject(value)) return Object.entries(value).map(([k, v]) => k + ':' + textOf(v)).join('；');
  return String(value);
}
const LooseString = (fallback = '') => z.preprocess(
  v => (v === undefined || v === null || v === '' ? fallback : String(v)),
  z.string()
).prefault(fallback);

// Step 3: 子schema定义
const 衣着Schema = z.object({
  上装: z.string().prefault(''),
  下装: z.string().prefault(''),
  内衣: z.string().prefault(''),
  内裤: z.string().prefault(''),
  袜子: z.string().prefault(''),
  鞋子: z.string().prefault(''),
  配饰: z.string().prefault('无'),
}).prefault({});

// Step 4: normalizeRoot预处理函数
function normalizeRoot(raw) {
  const root = cloneData(raw);  // JSON.parse(JSON.stringify(raw))
  if (!isPlainObject(root)) return raw;
  // 归一化别名字段
  if (root.自身) {
    root.user = normalizeSelfAlias(root.自身, root.user);
    delete root.自身;
  }
  return root;
}

// Step 5: 顶层schema定义并导出
export const Schema = z.preprocess(normalizeRoot, z.object({
  环境: z.object({
    日期: z.string().prefault(''),
    时间: z.string().prefault(''),
    经过天数: z.coerce.number()
      .transform(v => _.clamp(v, 0, Number.MAX_SAFE_INTEGER))
      .prefault(0),
    所在位置: z.object({
      城市: z.string().prefault(''),
      // ...
    }).prefault({}),
  }).prefault({}),
  user: z.object({
    状态: z.string().prefault(''),
    // ...
  }).prefault({}),
  // ...其他顶层容器
}));

// Step 6: 注册（必须在$()回调中）
$(() => { registerMvuSchema(Schema); });
```

---

### 7.2 mvu_zod_cn.js 标准模板

来源：`<local-user-path>/OneDrive\ST-\角色卡工作区\星月 2.5.0\components\mvu_zod_cn.js`（生产验证）| 置信度：**high**

```javascript
// MVU加载与默认设置注入
const DEFAULTS = Object.freeze({
  reprocessVariables: true,
  rereadInitialVariables: true,
  retryExtraModelParse: true,
  '重新处理变量': true,
  '重新读取初始变量': true,
  '重试额外模型解析': true,
});

// 等待WorldbookReady
await waitXingyueWorldbookReady();
await new Promise(resolve => setTimeout(resolve, 250));

// 加载MVU bundle（主CDN + testingcf备用）
await import('https://testingcf.jsdelivr.net/gh/MagicalAstrogy/MagVarUpdate/artifact/bundle.js');

// 等待MVU就绪
const Mvu = await waitForMvuReady();

// 注入默认设置
applyMvuDefaults(Mvu);
```

---

### 7.3 典型 JSONPatch 输出示例

来源：`<local-user-path>/OneDrive\ST-\角色卡工作区\星月 2.5.0\components\mvu_update_output_format.txt`（生产验证）| 置信度：**high**

```xml
<UpdateVariable>
<analysis>
正文中时间从午休后推进到下午14:25。
user接取了新任务"调查事件"，任务类型剧情，初始状态进行中。
城市环境：室内，光照人工灯光，噪音低。
</analysis>
<JSONPatch>
[
  {"op":"replace","path":"/环境/时间","value":"14:25"},
  {"op":"replace","path":"/环境/城市环境/光照条件","value":"人工灯光"},
  {"op":"add","path":"/任务/调查事件","value":{
    "任务id":"TASK001",
    "任务类型":"剧情",
    "任务难度":"普通",
    "任务状态":"进行中",
    "任务描述":"调查目标",
    "发布人":"未确认",
    "发布地点":"未确认",
    "当前阶段":"已接取",
    "任务进度":"0%",
    "截止时间":"无明确期限",
    "任务奖励":"未确认",
    "任务惩罚":"未确认"
  }}
]
</JSONPatch>
</UpdateVariable>
```

---

## 8. 高频避坑

来源：`[本地实战]` + `[运行时源码]` | 置信度：**high**

1. **replace路径不存在报错**：`replace` 要求路径已存在。首次写入 record 项必须 `add` 完整对象，不能直接 `replace` 其子字段。

2. **新增record子字段的顺序**：先 `add /顶层容器/recordKey`（完整对象），才能对其子字段使用 `replace`。

3. **VWD格式已废弃**：InitialVariables.json 中不写 `[value, desc]` 二元组，直接写纯值（字符串/数字/空对象）。路径直接是 `/环境/时间` 而非 `/环境/时间/0`。

4. **prefault vs default的选择**：对象字段用 `.prefault({})`（值先经过 transform），不要与 `.default({})` 混用；`.default` 的后备值绕过 transform。

5. **passthrough()的必要性**：LLM 可能写入 schema 中未声明的字段时，需在对象末尾加 `.passthrough()`，否则这些字段会被 stripping 掉。

6. **$meta在生产卡中不用**：生产卡通过 zod schema 约束结构，不用 `$meta` 来控制 extensible 等。

7. **registerMvuSchema 需在`$()`回调中调用**：必须等 DOM 就绪后注册，不能在模块顶层直接调用。

8. **z 是全局变量，无需 import**：TavernHelper（v3.3.2+）自动注入 `z`，直接使用 `z.object()` 等，无需 import zod。卡内绝不从 CDN 另引 zod 实例（三实例地雷，见 §4.3）。

9. **analysis每行必须有对应patch**：`<analysis>` 中每行描述至少对应一个 JSONPatch 操作，禁止分析中提到变化但 patch 中遗漏。

10. **LLM输出不使用 delta/insert**：这两个 op 仅限脚本代码调用；LLM输出的 JSONPatch 只用 `add / replace / remove / move` 四种。

11. **VARIABLE_INITIALIZED 必须用常量引用**：事件字符串值中有 typo（`initiailized`，多一个 i），硬编码极易出错，必须用 `Mvu.events.VARIABLE_INITIALIZED` 引用（见 §5.2）。

12. **iframe_events 有 `js_` 前缀**：iframe 内监听 ST 事件要用 `iframe_events.GENERATION_STARTED`（`'js_generation_started'`），不是 `tavern_events.GENERATION_STARTED`（`'generation_started'`）。

13. **registerVariableSchema 不拦截写入**：调用此函数只影响变量管理器 UI 显示，无法在运行时阻止 LLM 写非法值。真正的运行时拦截靠 zod schema 经 `registerMvuSchema` 注册（见 §4.2）。

---

## 9. 悬案（未决项）

### 悬案 P1 — delta op 是否为 LLM 可用 op

**状态**：已有倾向性结论，但未真机验证  
**描述**：MVU bundle.js 使用 `delta` op（数值增量）但 RFC6902 标准中无此 op。本地 mvu_update_full.txt 规则（星月2.5.0）明确只允许 `add、replace、remove、move`，**未包含 delta**。  
**倾向结论**：LLM输出格式仅用4种op（add/replace/remove/move），`delta` 是内部API或早期版本遗留，不暴露给LLM。  
**验证路径**：查看卡的世界书条目中 `[mvu_update]` 的op限制说明；或在 JSONPatch 中使用 `delta` 后观察 bundle 是否接受。  
置信度：**medium·推导未验证**

---

### 悬案 P2 — VARIABLE_INITIALIZED typo 在运行时是否存在

**状态**：`.d.ts` 确认 typo，运行时是否一致未测  
**描述**：`.d.ts` 中的 `'mag_variable_initiailized'`（typo）是否就是实际运行时事件字符串？  
**验证路径**：`document.addEventListener('mag_variable_initialized', () => console.log('correct'))` 和 `document.addEventListener('mag_variable_initiailized', () => console.log('typo'))` 各触发一次，看哪个收到事件。  
置信度：**medium·推导未验证**

---

### 悬案 P3 — parseMessage 返回 undefined 的触发条件

**状态**：类型文档已明确，未真机验证  
**描述**：当消息字符串不含有效命令时，`Mvu.parseMessage()` 返回 `undefined` 还是原 `old_data`？  
**验证路径**：`await Mvu.parseMessage('这是一段没有命令的文本', old_data)` 打印返回值。  
置信度：**medium·推导未验证**

---

### 悬案 P4 — getMvuData 是否真的同步

**状态**：推断  
**描述**：`.d.ts` 标注为同步（非 Promise），但需读取 ST 聊天数据，底层是否真同步（依赖内存缓存）？  
**验证路径**：`const r = Mvu.getMvuData({ type: 'message' }); console.log(r instanceof Promise)` — 若为 false 则真同步。  
置信度：**medium·推导未验证**

---

### 悬案 P5 — COMMAND_PARSED in-place 修改语义

**状态**：推断，未真机验证  
**描述**：在 `COMMAND_PARSED` 回调中对 `commands` 数组做 `splice` 修改，是否真的影响后续执行？  
**验证路径**：注册回调，用 `commands.splice(0, commands.length)` 清空所有命令，观察变量是否仍被更新。  
置信度：**medium·推导未验证**

---

### 悬案 P6 — z.preprocess 中 prefault 的触发时机

**状态**：推断，官方文档未明确  
**描述**：如 `z.preprocess(normalizeRoot, z.object({...})).prefault({})` 中，prefault 何时触发——是 normalizeRoot 之前还是之后？  
**倾向结论**：外层 `.prefault({})` 先将 `undefined` 换成 `{}`，然后 `{}` 进入内层 `z.preprocess(fn, ...)` 的 fn 函数。实战中 `{}` 是安全初始值，无论顺序如何结果一致，因此该角落场景未被验证。  
置信度：**medium·推导未验证**

---

### 悬案 P7 — passthrough() 在深层 record 中的行为

**状态**：推断，未真机验证  
**描述**：`z.record(z.string(), z.object({...}).passthrough().prefault({})).prefault({})` 中，每个 record 值的 passthrough 是否独立起效？  
**验证路径**：构造含未声明字段的 record 值，通过 MVU patch 写入后读取，观察字段是否保留。  
置信度：**medium**

---

### 悬案 P8 — VWD 兼容路径在 beta 版中是否完全移除

**状态**：未验证  
**描述**：bundle.js master 版仍有 VWD 检测代码。beta 版未知是否清理。  
**验证路径**：向 beta 版卡发送带 VWD 格式的变量 JSON，观察解析行为。  
置信度：**low**（孤证，禁止基于此施工）

---

## 10. 关键字段/API 参考表

| API / 字段 | 来源 | 说明 | 置信度 |
|---|---|---|---|
| `registerMvuSchema(schema)` | `mvu_zod.js`（CDN实测） | 注册 zod schema 到 MVU 变量管理器 | high |
| `registerVariableSchema(schema, opts)` | TavernHelper v4.3.7+（官方更新日志） | 底层注册函数，由 mvu_zod.js 调用；纯 UI，不拦截写入 | high |
| `z.string().prefault('')` | Zod v4 官方文档·多源 + 本地实战 | 字符串字段，undefined 时后备为空字符串 | high |
| `z.coerce.number().transform(v=>_.clamp(v,0,100)).prefault(0)` | 本地实战（星月2.5.0 zod_schema.js） | 数值字段，coerce+clamp+prefault 组合 | high |
| `z.preprocess(fn, schema).prefault({})` | 本地实战 + 推断 | 预处理归一化+prefault 组合；嵌套顺序见 §9 P6 | medium·推导未验证 |
| `z.object({...}).passthrough().prefault({})` | Zod v4 官方文档 + 本地实战 | 允许额外字段的对象 | high |
| `z.record(z.string(), z.object({...})).prefault({})` | 本地实战（星月2.5.0 zod_schema.js） | 动态 key 的 record 类型 | high |
| `mag_variable_initialized` | 运行时源码（bundle.js v对象） | 变量初始化完成 | high |
| `mag_variable_update_ended` | 运行时源码（bundle.js v对象） | 变量更新完成（最常用钩子） | high |
| `mag_command_parsed` | 运行时源码（bundle.js v对象） | 单条 patch 命令解析后 | high |
| `mag_before_message_update` | 运行时源码（bundle.js v对象） | 消息修改前钩子 | high |
| `mag_variable_updated` | 运行时源码（bundle.js v对象） | 单个变量变化时 | high |
| `eventOn(event, listener)` | 本地 event.d.ts | 注册事件监听，已注册则静默无效 | high |
| `eventMakeFirst/Last` | 本地 event.d.ts | 调整监听器执行顺序 | high |
| `iframe_events.GENERATION_STARTED` | 本地 event.d.ts | 值为 `'js_generation_started'`（有 `js_` 前缀） | high |
| `Mvu.getMvuData(opts)` | 本地 exported.mvu.d.ts | 同步读取变量（底层同步性见 §9 P4） | high（签名）/ medium（运行时） |
| `Mvu.replaceMvuData(data, opts)` | 本地 exported.mvu.d.ts | 异步完整替换变量 | high |
| `Mvu.parseMessage(msg, old_data)` | 本地 exported.mvu.d.ts | 异步解析命令并应用；无更新→返回 undefined | high（签名）/ medium（运行时） |
| `$meta.extensible` | 运行时源码（bundle.js 摘要） | 允许 schema 外字段 | medium·单源孤证 |
| `$meta.template` | 运行时源码（bundle.js 摘要） | 数组元素模板 | medium·单源孤证 |
| `reprocessVariables: true` | 本地实战（星月2.5.0 DEFAULTS 对象） | 重新处理变量（生产卡默认开启） | high |

---

## 11. 本地源文件索引

| 文件 | 路径 | 可信等级 |
|---|---|---|
| zod_schema.js（星月） | `<local-user-path>/OneDrive\ST-\角色卡工作区\星月 2.5.0\components\zod_schema.js` | ★★★ 最高 |
| zod_schema.js（交错） | `<local-user-path>/OneDrive\ST-\角色卡工作区\...\v2.6.0_20260611_000000\components\zod_schema.js` | ★★★ 最高 |
| mvu_zod_cn.js | `<local-user-path>/OneDrive\ST-\角色卡工作区\星月 2.5.0\components\mvu_zod_cn.js` | ★★★ 最高 |
| mvu_update_full.txt | `<local-user-path>/OneDrive\ST-\角色卡工作区\星月 2.5.0\components\mvu_update_full.txt` | ★★★ 最高 |
| mvu_update_output_format.txt | `<local-user-path>/OneDrive\ST-\角色卡工作区\星月 2.5.0\components\mvu_update_output_format.txt` | ★★★ 最高 |
| InitialVariables.json（星月） | `<local-user-path>/OneDrive\ST-\角色卡工作区\星月 2.5.0\components\InitialVariables.json` | ★★★ 最高 |
| InitialVariables.json（交错） | `<local-user-path>/OneDrive\ST-\角色卡工作区\...\v2.6.0_20260611_000000\components\InitialVariables.json` | ★★★ 最高 |
| exported.mvu.d.ts | `C:\...\node_modules\@types\iframe\exported.mvu.d.ts` | ★★★ 最高（本地核实） |
| event.d.ts | `C:\...\node_modules\@types\iframe\event.d.ts` | ★★★ 最高（本地核实） |
| variables.d.ts | `C:\...\node_modules\@types\function\variables.d.ts` | ★★★ 最高（本地核实） |

---

## 参考来源

- [GitHub - MagicalAstrogy/MagVarUpdate](https://github.com/MagicalAstrogy/MagVarUpdate)
- [GitHub - KritBlade/MVU_Zod_StatusMenuBuilder](https://github.com/KritBlade/MVU_Zod_StatusMenuBuilder)
- [TavernHelper 更新日志](https://n0vi028.github.io/JS-Slash-Runner-Doc/guide/%E5%85%B3%E4%BA%8E%E9%85%92%E9%A6%86%E5%8A%A9%E6%89%8B/%E6%9B%B4%E6%96%B0%E6%97%A5%E5%BF%97.html)
- [Zod v4 API - Prefault](https://zod.dev/api)
- [DeepWiki - Default and Prefault Values](https://deepwiki.com/colinhacks/zod/4.5-default-and-prefault-values)
- [Medium - From .default() to .prefault() to .catch()](https://haweecodes.medium.com/from-default-to-prefault-b59b7e0af144)
- [GitHub - N0VI028/JS-Slash-Runner CHANGELOG](https://github.com/N0VI028/JS-Slash-Runner/blob/main/CHANGELOG.md)
- [GitHub - StageDog/tavern_resource mvu_zod.js](https://cdn.jsdelivr.net/gh/StageDog/tavern_resource/dist/util/mvu_zod.js)
- SillyTavern v1.14.0 release/public/scripts/variables.js（本地核实）
- JS-Slash-Runner v4.8.9 @types/iframe/exported.mvu.d.ts、event.d.ts、@types/function/variables.d.ts（本地核实）
