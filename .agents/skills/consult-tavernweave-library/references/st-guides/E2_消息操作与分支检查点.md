# 消息操作与分支检查点

> 文档版本：v1.0.0 ｜ 最后更新：2026-07-14

SillyTavern 消息层核心机制全览：Swipe 变体管理、楼层编辑/隐藏、检查点/分支树、变量与消息的绑定关系，以及完整的事件系统参考。供角色卡脚本开发与正则调试使用。

---

## 置信度分级体系

三级评定：
- **high** — 多源交叉验证或本地真机确认，可直接施工
- **medium** — 单一可信来源或合理推断，施工前建议验证
- **low** — 孤证、无来源或纯推断，**禁止直接施工**

依据类型轴：`[运行时源码]` / `[真机]` / `[文档·多源]` / `[文档·单源]` / `[社区]` / `[推断]`

收集日期：2026-06-28  
**对抗复核日期：2026-06-28**（本地 v1.14.0 源码直读 + 生产卡验证）  
回源基准：SillyTavern v1.14.0 + 本地成熟卡（星月 2.5.0 / 交错 2.6.0）

---

## 目录

1. 机制总览
2. Message 对象完整字段参考
   - 2.1 顶层字段
   - 2.2 extra 子字段完整表
   - 2.3 JSONL 文件结构
3. STscript 消息操作命令完整参考
   - 3.1 核心消息操作命令
   - 3.2 分支/检查点命令
   - 3.3 LALib 扩展命令
4. 事件系统完整参考
   - 4.1 扩展 API 事件注册范式
   - 4.2 消息相关事件完整列表
5. 分支/检查点机制详解
   - 5.1 数据模型
   - 5.2 关键实现函数
   - 5.3 Timelines 扩展的树状视图逻辑
6. 变量系统与消息的关联
   - 6.1 三层变量体系
   - 6.2 正则脚本/角色卡内置变量 API
   - 6.3 message-level variables 与 swipe 的关联
7. 完整可用范式代码
   - 7.1 基础消息操作（STscript）
   - 7.2 分支/检查点操作（STscript）
   - 7.3 扩展 API 事件监听范式
   - 7.4 读取消息变量
   - 7.5 LALib 扩展的 Swipe 管理范式
8. 高频避坑
9. 悬案（不确定项 + 验证路径）
10. 来源索引

---

## 1. 机制总览

**来源：** `script.js` + `bookmarks.js` + 官方文档 | **置信度：high [文档·多源] [运行时源码]**

| 机制 | 核心作用 | 置信度 |
|---|---|---|
| **Swipe（划动重生成）** | 同一楼层存多个 AI 回复变体，用 swipe_id 指针导航 | high [文档·多源] |
| **Edit（楼层编辑）** | 直接修改 `chat[id].mes`，保存时写回 JSONL | high [运行时源码] |
| **Hide/Unhide（隐藏）** | 设置 `extra.hidden=true`，该楼不进 LLM prompt | high [运行时源码] |
| **Continue（续写）** | 调用 `Generate('continue')`，扩展最后一条消息 | high [运行时源码] |
| **Delete（删除）** | 不可恢复地从 `chat[]` 删除，写回文件 | high [文档·多源] |
| **Branch/Checkpoint（分支/检查点）** | 克隆聊天文件 + chat_metadata 链接实现非线性树 | high [运行时源码] |
| **Variables（变量）** | 三层变量体系（scoped/local/global），local 存 chat_metadata | high [运行时源码] |

---

## 2. Message 对象完整字段参考

**来源：** `script.js`（raw GitHub）、DeepWiki 5.2、SillyTavern.NET-File-Converter  
**适用版本：** v1.14.0  
**置信度：high [运行时源码] [文档·多源]**

### 2.1 顶层字段

```jsonc
{
  "name": "CharacterName",        // 发送者名称（必填）
  "is_user": false,               // 是否用户消息（必填）
  "is_system": false,             // 是否系统/注释消息（/sys /comment）
  "mes": "消息正文",              // 主文本（必填）
  "send_date": "April 26, 2024 6:06am",  // 时间戳（格式不一致，见避坑 8.1 节）
  "swipes": ["版本A", "版本B"],   // 所有划动变体的文本数组
  "swipe_id": 0,                  // 当前显示的 swipe 索引（0-based）
  "extra": { /* 见 2.2 */ },      // 元数据对象
  "variables": {},                // per-message 变量（来自正则/脚本写入）
  "force_avatar": "",             // 强制使用的头像路径
  "title": ""                     // 消息标题
}
```

### 2.2 extra 子字段完整表

```jsonc
{
  "api": "openai",                // 生成来源 API
  "model": "gpt-4o",             // 使用的模型名
  "display_text": "...",          // 格式化后的显示文本（覆盖 mes 渲染）
  "bias": "",                     // 生成偏置
  "token_count": 123,             // token 数
  "gen_started": "...",           // 生成开始时间戳
  "gen_finished": "...",          // 生成结束时间戳
  "reasoning_duration": 0,        // 推理时长（ms）
  "time_to_first_token": 0,      // TTFT（ms）
  "media": [],                    // 附件数组（图片/视频/音频）
  "media_display": "...",         // 媒体显示模式
  "media_index": 0,               // 当前显示媒体索引
  "files": [],                    // 文件附件（Data Bank）
  "title": "",                    // 附加标题
  "bookmark_link": "chatname",    // 链接到的检查点文件名（由 bookmarks.js 写入）
  "branches": [],                 // 从该楼创建的分支列表
  "type": "",                     // 消息类型标记
  "hidden": false,                // /hide 命令写入，true = 不进 LLM prompt
  "tool_invocations": [],         // 工具调用记录
  "uses_system_ui": false         // 是否使用系统 UI
}
```

**extra.hidden 与 is_system 区别：**

- `extra.hidden = true` — 用户主动隐藏（/hide 命令），消息仍显示在 UI 中（有幽灵图标），不发给 LLM
- `is_system = true` — `/sys` 或 `/comment` 发出的中性叙述/注释，也不进 LLM prompt，但 UI 渲染方式不同

**【重要更正】`/messages` 命令的过滤逻辑：** `slash-commands.js:3428` 直读：`if (!includeHidden && msg.is_system)` —— 实际过滤的是 `is_system=true` 的消息，而 **`extra.hidden=true` 的消息不受影响**（除非该消息同时有 `is_system=true`）。加 `hidden=true` 参数后，`is_system=true` 的消息也会被放行。换言之，"两者都会被 `/messages` 排除"的说法有误：hidden 消息（`extra.hidden=true` 但 `is_system=false`）仍会出现在 `/messages` 输出中。

注意 PR #5040 修复了 hidden 消息 `is_system` 被强制清零的 bug，该 bug 会导致深度检测失效，v1.14.0 已修复。

**来源：** `slash-commands.js`（本地直读 3428行）、PR #5040 | **置信度：high [运行时源码]**（过滤逻辑已直读源码确认）

### 2.3 JSONL 文件结构

**来源：** DeepWiki 5.2 | **置信度：high [文档·多源]**

```
第一行：{ "chat_metadata": {...}, "user_name": "(legacy)", "character_name": "(legacy)" }
第二行起：每行一个 Message 对象（上述结构）
```

chat_metadata 已知字段：

```jsonc
{
  "variables": {},           // local 变量存储（/setvar 等命令写入）
  "main_chat": "chatfile",  // 检查点/分支文件中：指向源聊天文件名
  "note_prompt": "",         // Author's Note 文本
  "note_interval": 1,        // AN 插入间隔
  "note_position": 1         // AN 插入位置
}
```

---

## 3. STscript 消息操作命令完整参考

**来源：** `slash-commands.js`（raw GitHub release 分支）  
**适用版本：** v1.14.0  
**置信度：high [运行时源码]**

### 3.1 核心消息操作命令

| 命令 | 必填参数 | 可选参数 | 限制 | 返回值 |
|---|---|---|---|---|
| `/hide` | 无（默认末尾） | `[id/range]`, `name=角色名` | — | 无 |
| `/unhide` | 无（默认末尾） | `[id/range]`, `name=角色名` | — | 无 |
| `/swipe` | 无 | `direction=right/left`（默认 right）, `await=false` | 仅对最后一条 AI 消息 | 无 |
| `/addswipe [text]` | swipe 文本 | `switch=true/false`（是否切换到新 swipe） | 不能用于用户消息或 hidden 消息 | 无 |
| `/delswipe [1-based-id]` | swipe 编号（1-based） | 无 | 仅最后一条消息 | 新的当前 swipe_id |
| `/continue` | 无 | `[prompt]`（附加提示）, `await=false` | — | 无 |
| `/regenerate` | 无 | `await=false` | — | 无 |
| `/send [text]` | 消息文本 | `at=N`（插入位置）, `name=角色名`, `compact=false`, `return=none`, `raw=true` | — | 无/消息ID |
| `/sendas name=X [text]` | `name` + 消息文本 | `avatar=X`, `at=N`, `compact=false`, `return=none`, `raw=true` | — | 无/消息ID |
| `/cut [range]` | 消息范围 | 无 | 不可恢复 | 无 |
| `/del [N]` | 数量 | 无 | 删除最后 N 条，不可恢复 | 无 |
| `/delname [charname]` | 角色名 | 无 | 删除该角色所有消息 | 无 |
| `/sys [text]` | 文本 | `at=N` | — | 无 |
| `/comment [text]` | 文本 | `at=N` | 不进 prompt | 无 |
| `/messages [range]` | 无 | `names=on/off`, `hidden=true`, `role=system/assistant/user`, `start-finish` | 默认跳过 `is_system=true` 消息（`extra.hidden=true` 不受影响）；加 `hidden=true` 可放行系统消息 | 消息文本 |

**避坑：**
- `/hide` 无参数时默认操作最后一条消息；range 是**包含区间**，`3-7` = 下标 3、4、5、6、7 共 5 条
- `/delswipe` 的 swipe-id 是 **1-based**，与 LALib 的 0-based 不同（见 8.3 节）

### 3.2 分支/检查点命令

**来源：** `bookmarks.js`（raw GitHub）、v1.12.6 release notes  
**置信度：high [运行时源码]**

| 命令 | 参数 | 返回值 | 说明 |
|---|---|---|---|
| `/branch-create` | `[mesId]`（可选，NUMBER） | 新分支名 | 创建并立即切换到新分支 |
| `/checkpoint-create` | `mesId=N`（named）, `[名称]`（unnamed） | 检查点名 | 创建检查点但不切换 |
| `/checkpoint-go` | `[mesId]`（可选） | 检查点名 | 跳转到指定楼的检查点 |
| `/checkpoint-exit` | 无 | 父聊天名 | 从检查点返回主聊天 |
| `/checkpoint-parent` | 无 | 父聊天名 | 获取当前分支的源聊天名 |
| `/checkpoint-get` | `[mesId]`（可选） | 检查点名或空 | 查询指定楼的检查点名 |
| `/checkpoint-list` | `links=true/false` | JSON 数组 | 列出所有检查点的 ID 和名称 |

### 3.3 LALib 扩展命令（社区最完整实现）

**来源：** `LenAnderson/SillyTavern-LALib` README  
**置信度：medium·单源孤证 [社区]**（LALib 是第三方社区扩展，来源为 README 文档而非 ST 核心运行时源码；命令可用性取决于用户是否安装该扩展）

| 命令 | 参数 | 返回值 |
|---|---|---|
| `/message-get [id]` | 消息 ID（支持负数，-1=最后） | 消息字典（含 name/mes/send_date 等） |
| `/message-list [range]` | 范围（如 0-2, -3--1） | 消息字典列表 |
| `/message-edit [message=id] [append=bool] [text]` | 目标 ID, 是否追加, 新文本 | 修改后的消息 |
| `/message-move [up/down/to=N]` | 方向或目标位置 | 无 |
| `/swipes-get [message=id] [swipe-index]` | 消息 ID, swipe 下标（0-based） | swipe 文本 |
| `/swipes-list [message=id]` | 消息 ID | 所有 swipe 文本列表 |
| `/swipes-add [message=id] [text]` | 消息 ID, 新 swipe 文本 | 无 |
| `/swipes-del [message=id] [filter=expr] [index]` | 消息 ID, 可选过滤表达式或下标 | 更新后列表 |
| `/swipes-go [message=id] [index]` | 消息 ID, 目标下标 | 无 |
| `/swipes-count [message=id]` | 消息 ID | 数量（整数） |
| `/swipes-index [message=id]` | 消息 ID | 当前 swipe_id（0-based） |
| `/swipes-swipe` | 无 | 对最后消息触发新 swipe |
| `/role-swap [id/range]` | 消息 ID 或范围（可选，默认全部） | 无 |
| `/chat-parent` | 无 | 父聊天名（若为分支） |
| `/message-on event=X callback=closure [selector]` | 事件类型, 回调闭包, CSS 选择器 | 监听器 ID |
| `/message-off [id] [event] [query]` | 监听器 ID 或事件/选择器 | 无 |

---

## 4. 事件系统完整参考

**来源：** `events.js`（本地 v1.14.0 直读）  
**适用版本：** v1.14.0  
**置信度：medium·单源孤证（事件表整体可信，但含3个 v1.14.0 不存在的条目，见下表划线行）[运行时源码]**

### 4.1 扩展 API 事件注册范式

```javascript
// 方式1：扩展 API（推荐，用于扩展 JS 文件中）
const { eventSource, event_types } = SillyTavern.getContext();
eventSource.on(event_types.MESSAGE_RECEIVED, (messageId) => {
    // messageId 为楼层索引
});

// 方式2：正则脚本/角色卡内置变量（注入到 iframe context，依赖 Tavern Helper 扩展）
// window.tavern_events 和 eventOn 函数由 Tavern Helper 扩展注入，ST 核心不提供
// 生产卡（星月 2.5.0/交错 2.6.0）已真机验证可用
const events = window.tavern_events || {};
const disposer = eventOn(events.MESSAGE_SWIPED, (messageId) => {
    // 处理 swipe 事件
});
// 组件卸载时调用 disposer() 清理
```

### 4.2 消息相关事件完整列表

| 事件常量 | 字符串值 | 触发时机 | 参数 |
|---|---|---|---|
| `MESSAGE_SENT` | `message_sent` | 用户消息记录但未渲染 | messageId |
| `MESSAGE_RECEIVED` | `message_received` | AI 消息生成完成但未渲染 | messageId |
| `USER_MESSAGE_RENDERED` | `user_message_rendered` | 用户消息出现在 UI 中 | messageId |
| `CHARACTER_MESSAGE_RENDERED` | `character_message_rendered` | AI 消息出现在 UI 中 | messageId |
| `MESSAGE_EDITED` | `message_edited` | 消息被编辑 | messageId |
| `MESSAGE_DELETED` | `message_deleted` | 消息被删除 | messageId |
| `MESSAGE_UPDATED` | `message_updated` | 消息更新（含 swipe 切换） | messageId |
| `MESSAGE_SWIPED` | `message_swiped` | 消息划动（选择 swipe） | messageId |
| `MESSAGE_SWIPE_DELETED` | `message_swipe_deleted` | 某个 swipe 被删除 | — |
| `MESSAGE_FILE_EMBEDDED` | `message_file_embedded` | 消息中嵌入文件附件 | — |
| `MESSAGE_REASONING_EDITED` | `message_reasoning_edited` | 推理内容被编辑 | — |
| `MESSAGE_REASONING_DELETED` | `message_reasoning_deleted` | 推理内容被删除 | — |
| `MORE_MESSAGES_LOADED` | `more_messages_loaded` | 加载更多历史消息 | — |
| `CHARACTER_FIRST_MESSAGE_SELECTED` | `character_first_message_selected` | 选择角色开场消息变体 | eventArgs（含 messageId） |
| ~~`MESSAGE_SELECTED`~~ | ~~`message_selected`~~ | **v1.14.0 不存在此事件** — events.js 直读无此条目，来源不明，禁止使用 | — |
| `CHAT_CHANGED` | `chat_id_changed` | 切换聊天（**字符串值为 `'chat_id_changed'`，非 `'chat_changed'`**，events.js 第18行直读） | — |
| ~~`CHAT_LOADED`~~ | ~~`chat_loaded`~~ | **v1.14.0 不存在此事件** — events.js 直读无此条目 | — |
| `CHAT_CREATED` | `chat_created` | 新建聊天 | — |
| `CHAT_DELETED` | `chat_deleted` | 聊天被删除 | — |
| ~~`CHAT_RENAMED`~~ | ~~`chat_renamed`~~ | **v1.14.0 不存在此事件** — events.js 直读无此条目 | — |
| `GENERATION_STARTED` | `generation_started` | 生成开始 | — |
| `GENERATION_ENDED` | `generation_ended` | 生成正常结束 | messageId |
| `GENERATION_STOPPED` | `generation_stopped` | 生成被手动停止 | — |
| `STREAM_TOKEN_RECEIVED` | `stream_token_received` | 流式输出新 token | — |

**避坑：** swipe 切换只触发 `MESSAGE_SWIPED`，不一定触发 `MESSAGE_UPDATED`，需要同时监听两个事件才能覆盖所有消息变化场景。[medium，推断]

---

## 5. 分支/检查点机制详解

**来源：** `bookmarks.js`（raw GitHub）  
**适用版本：** v1.14.0  
**置信度：high [运行时源码]**

### 5.1 数据模型

```
主聊天文件：CharacterName - 2024-01-01@12-00-00.jsonl
├── 第 N 楼 message.extra.bookmark_link = "某自定义名 - January 1, 2024 12:00pm"
└── 第 M 楼 message.extra.branches = ["Branch #M - January 1, 2024 12:01pm"]

检查点文件命名：用户输入名称 + " - " + humanizedDateTime()（bookmarks.js 第100行）
└── 第1行 chat_metadata.main_chat = "CharacterName - 2024-01-01@12-00-00"

分支文件命名：`Branch #${mesId} - ${humanizedDateTime()}`（bookmarks.js 第174行直读，mesId 是消息楼层号）
└── 第1行 chat_metadata.main_chat = "CharacterName - 2024-01-01@12-00-00"
```

- `extra.bookmark_link` — 字符串，指向检查点 JSONL 文件名（不含扩展名）
- `extra.branches` — **纯字符串数组**，每条为一个分支文件名（bookmarks.js 第189行直读：`lastMes.extra['branches'].push(name)`，name 是字符串）；**悬案已解除，见第9章**

### 5.2 关键实现函数

```javascript
// 创建检查点（bookmarks.js）
createNewBookmark(mesId, { forceName = null })
// 1. 获取已有聊天名 getExistingChatNames()
// 2. 生成唯一名 "{mainChat} - Checkpoint #{N}"
// 3. 保存为独立 JSONL 文件（含消息到 mesId 为止）
// 4. 在 chat[mesId].extra.bookmark_link 写入检查点文件名
// 5. saveChat()

// 创建分支（bookmarks.js）
createBranch(mesId)
// 1. 生成名称格式：`Branch #${mesId} - ${humanizedDateTime()}`（bookmarks.js 第174行直读，是 mesId 非递增N）
// 2. 保存为独立 JSONL 文件（含消息到 mesId 为止）
// 3. 在 chat[mesId].extra.branches 追加分支文件名（字符串 push）
// 注意：createBranch() 本身不切换聊天，切换由上层 branchChat() 调用 openCharacterChat()/openGroupChat()

// 返回主聊天
backToMainChat()
// 读取 chat_metadata['main_chat'] → 调用 openCharacterChat()/openGroupChat()
```

**避坑：** 分支命名使用 `Branch #${mesId} - humanizedDateTime()`（mesId 为楼层号），不使用 `getUniqueName()`，无递增编号逻辑。若手动删除分支文件，`extra.branches` 数组中仍保留文件名，切换时会出现死链错误。[high，运行时源码]

**避坑：** `/branch-create` 执行后会立即切换聊天，触发 `CHAT_CHANGED` 事件。角色卡的所有状态需在 `CHAT_CHANGED` 回调中重新初始化，否则旧状态残留。[high，推断+真机]

### 5.3 Timelines 扩展的树状视图逻辑

**来源：** SillyTavern-Timelines 扩展（社区）| **置信度：medium [社区]**

SillyTavern-Timelines 通过以下方式构建可视化分支历史：
1. 跟踪 `extra.bookmark_link` 链接（忽略死链）
2. 合并"相同深度、相同内容"的消息节点
3. 递归构建树状结构

---

## 6. 变量系统与消息的关联

**来源：** `variables.js`（raw GitHub）、`control_center.js`（本地实战卡）  
**适用版本：** v1.14.0  
**置信度：high [运行时源码] [真机]**

### 6.1 三层变量体系

```javascript
// === Scoped 变量（临时，STscript 执行作用域内） ===
// /let key=value → /var key
// 作用域关闭后自动销毁，不持久化

// === Local 变量（聊天级，持久化） ===
// 存储位置：chat_metadata.variables[name]
// STscript 命令：/setvar key=foo value / /getvar foo
// 宏：{{setvar::foo::bar}} / {{getvar::foo}}
// 函数：setLocalVariable(name, value) / getLocalVariable(name)

// === Global 变量（全局，跨聊天） ===
// 存储位置：extension_settings.variables.global[name]
// STscript 命令：/setglobalvar key=foo value / /getglobalvar foo
// 持久化到 settings.json
```

### 6.2 正则脚本/角色卡内置变量 API

**来源：** `control_center.js`（生产卡实战代码，星月 2.5.0/交错 2.6.0）  
**置信度：high [真机]**

```javascript
// getVariables / replaceVariables 是 ST 注入到正则/脚本执行上下文的全局函数

// 读取当前楼层的 message-level 变量（跟随当前 swipe_id）
const msgVars = getVariables({ type: 'message' });

// 读取脚本级变量（按 script_id 隔离）
const scriptVars = getVariables({ type: 'script', script_id: 'my-script-id' });

// 写入（替换）message-level 变量
replaceVariables({ stat_data: { ... } }, { type: 'message' });

// 写入脚本级变量
replaceVariables(newValue, { type: 'script', script_id: 'my-script-id' });

// 读取最近几条消息（负数范围表示从末尾计算）
const recentMessages = getChatMessages('-6--1', { include_swipes: false });
// 返回数组，每条含 message/mes/text 字段（需三重兼容，见 8.7 节）

// 读取单条消息（-1 = 最后一条）
const msg = getChatMessages(-1, { include_swipes: false })?.[0];
const rawText = msg?.message || msg?.mes || msg?.text || '';
```

### 6.3 message-level variables 与 swipe 的关联

**置信度：high [真机] [社区]**

`getVariables({ type: 'message' })` 返回**当前显示的 swipe** 的变量（跟随 swipe_id 切换而切换）。这是 LALib MessageVariables 扩展的核心机制：变量绑定在每个 swipe 上，切换 swipe 会切换到对应 swipe 的变量集。

实际存储位置：`message.variables`（顶层字段，非 extra 内）。具体写入时机见第 9 章悬案。

---

## 7. 完整可用范式代码

### 7.1 基础消息操作（STscript）

**来源：** `slash-commands.js` | **置信度：high [运行时源码]**

```stscript
{{! 隐藏某楼，不让LLM看到 }}
/hide 5

{{! 按角色名隐藏最后一条消息 }}
/hide name=Alice

{{! 批量隐藏一段范围（含区间，3到7共5条） }}
/hide 3-7

{{! 恢复显示 }}
/unhide 5

{{! 划动到下一个变体（会触发新生成） }}
/swipe direction=right

{{! 划动到上一个已有变体（不生成） }}
/swipe direction=left

{{! 添加一个新变体并切换到它 }}
/addswipe switch=true 这是手动添加的变体内容

{{! 删除第2个变体（1-based，注意与LALib区别） }}
/delswipe 2

{{! 续写最后一条AI消息 }}
/continue

{{! 续写并追加额外提示 }}
/continue 继续描述战斗场景

{{! 插入一条隐藏注释（不进prompt） }}
/comment 系统注释：此处发生了跳转

{{! 插入中性叙述（不进prompt） }}
/sys 场景切换到三天后

{{! 读取最后3条消息文本 }}
/setvar key=start {{lastMessageId}} |
/addvar key=start -2 |
/messages names=off {{getvar::start}}-{{lastMessageId}}
```

### 7.2 分支/检查点操作（STscript）

**来源：** `bookmarks.js`、v1.12.6 release notes | **置信度：high [运行时源码]**

```stscript
{{! 在当前最后一条消息创建检查点（不切换） }}
/checkpoint-create 第一章结尾存档

{{! 在指定楼层创建检查点 }}
/checkpoint-create mesId=15 战斗前存档

{{! 列出所有检查点 }}
/checkpoint-list

{{! 跳转到某楼的检查点 }}
/checkpoint-go 15

{{! 从检查点返回主聊天 }}
/checkpoint-exit

{{! 创建分支（立即切换到新分支） }}
/branch-create

{{! 从指定楼创建分支 }}
/branch-create 10

{{! 获取当前聊天的父聊天名（用于判断是否在分支中） }}
/checkpoint-parent |
/if left={{pipe}} right="" else={:
    /sys 当前在主聊天中
:} {:
    /sys 当前在分支中，父聊天：{{pipe}}
:}
```

### 7.3 扩展 API 事件监听范式（角色卡正则脚本上下文）

**来源：** `control_center.js`（生产卡实战代码，真机验证）  
**置信度：high [真机]**

```javascript
function bindEvents() {
    const on = typeof eventOn === 'function' ? eventOn : null;
    const events = window.tavern_events || {};
    const disposers = [];

    // 监听 swipe 事件（用于同步状态）
    if (on && events.MESSAGE_SWIPED) {
        disposers.push(on(events.MESSAGE_SWIPED, (messageId) => {
            // messageId 为被 swipe 的楼层索引
            handleSwipeChange(messageId);
        }));
    }

    // 监听 AI 消息渲染完成
    if (on && events.CHARACTER_MESSAGE_RENDERED) {
        disposers.push(on(events.CHARACTER_MESSAGE_RENDERED, (messageId) => {
            updateStatusBar(messageId);
        }));
    }

    // 监听聊天切换（切换角色/分支时触发）
    if (on && events.CHAT_CHANGED) {
        disposers.push(on(events.CHAT_CHANGED, () => {
            reinitialize();
        }));
    }

    // 监听 MVU 变量初始化完成（自定义变量系统）
    if (Mvu?.events?.VARIABLE_INITIALIZED) {
        disposers.push(eventOn(Mvu.events.VARIABLE_INITIALIZED, () => {
            refreshDisplay();
        }));
    }

    // 清理（组件卸载时调用返回函数）
    return () => disposers.forEach(d => typeof d === 'function' ? d() : d?.off?.());
}
```

### 7.4 读取消息变量（角色卡正则脚本上下文）

**来源：** `control_center.js`（生产卡实战代码，真机验证）  
**置信度：high [真机]**

```javascript
// 读取当前 swipe 的 message-level 变量（与 swipe 绑定）
function readMessageVars() {
    if (typeof getVariables !== 'function') return {};
    return getVariables({ type: 'message' }) || {};
}

// 写入 message-level 变量（写到当前 swipe）
function writeMessageVars(value) {
    if (typeof replaceVariables !== 'function') return;
    replaceVariables(value, { type: 'message' });
}

// 读取脚本级持久化设置（按 script_id 隔离，跨 swipe 持久）
function readScriptSettings(scriptId) {
    if (typeof getVariables !== 'function') return {};
    return getVariables({ type: 'script', script_id: scriptId }) || {};
}

// 读取最近 N 条消息的原始文本（用于状态解析）
function recentMessages(n = 6) {
    if (typeof getChatMessages !== 'function') return '';
    return (getChatMessages(`-${n}--1`, { include_swipes: false }) || [])
        .map(m => m?.message || m?.mes || m?.text || '')  // 三重兼容
        .join('\n')
        .slice(-6000);
}

// 读取单条消息的原始文本（messageId=-1 表示最后一条）
function rawMessageFor(messageId = -1) {
    if (typeof getChatMessages !== 'function') return '';
    const msg = getChatMessages(messageId, { include_swipes: false })?.[0];
    return msg?.message || msg?.mes || msg?.text || '';  // 三重兼容
}
```

### 7.5 LALib 扩展的 Swipe 管理范式

**来源：** LALib README | **置信度：medium·单源孤证 [社区]**（同 3.3 节，命令可用性依赖 LALib 扩展安装）

```stscript
{{! 读取最后一条消息的所有 swipes 数量 }}
/swipes-list | /echo 共有 {{pipe}} 个变体

{{! 读取当前 swipe 索引（0-based） }}
/swipes-index | /setvar key=curSwipe {{pipe}}

{{! 跳转到第2个swipe（0-based索引） }}
/swipes-go 1

{{! 批量删除包含关键词的swipe }}
/swipes-del filter="'错误内容' in swipe.mes"

{{! 给某条历史消息添加新swipe（LALib支持非最后消息） }}
/swipes-add message=-3 这是第三条消息的新变体

{{! 角色互换（swap user/AI角色，用于复盘） }}
/role-swap 5-10
```

---

## 8. 高频避坑

**来源综合：** `slash-commands.js` / `script.js` / `bookmarks.js` / 本地生产卡 / 社区 issue

### 8.1 send_date 格式不一致

**置信度：medium [文档·单源]** | 来源：Issue #2151

ST 的 `send_date` 字段历史上存在多种格式：`"April 26, 2024 6:06am"`、Unix 毫秒时间戳、ISO 8601 等。不要用 `new Date(send_date)` 直接解析，需做格式检测。官方 issue #2151 承认此问题但已 Stale（未修复）。

### 8.2 /addswipe 不能给用户消息或系统消息添加 swipe

**置信度：high [运行时源码]** | 来源：`slash-commands.js`（本地直读 3860-3866行）

【重要更正】`addSwipeCallback` 内部检查的是 `is_user`（行3860）和 `is_system`（行3865），**不是** `extra.hidden`。违反条件时会触发 `toastr.warning` 弹出提示，**并非静默失败**。`extra.hidden=true` 的消息不受此限制。

### 8.3 /delswipe 的 swipe-id 是 1-based，LALib 是 0-based

**置信度：high [运行时源码]** | 来源：`slash-commands.js`（本地直读 1534行 `description: '1-based swipe id'`）

- `/delswipe 1` — 删除第一个 swipe（1-based）
- `/delswipe`（无参数）— 删除**当前**swipe（deleteSwipeCallback 第3910行：无参数时 swipeId=null，deleteSwipe 内部取当前 swipe_id）
- `/swipes-del 0` — 删除第一个 swipe（LALib，0-based，需安装 LALib 扩展）

混用时必须换算，否则删错位置。

### 8.4 extra.hidden 和 is_system 行为差异

**置信度：medium [运行时源码]** | 来源：`slash-commands.js`、PR #5040

两者都排除在 LLM prompt 和 `/messages` 命令之外，但 UI 渲染方式不同。PR #5040 修复了 hidden 消息 `is_system` 被强制清零的 bug（深度检测失效），v1.14.0 已修复。

### 8.5 检查点文件命名：空名自动递增，用户自定义名加时间戳

**置信度：high [运行时源码]** | 来源：`bookmarks.js`（本地直读 82-101行）

【更正】草稿说使用 `getUniqueName()`，实际不是。`getBookmarkName()` 的逻辑：
- 用户输入名称 → 最终名称 = `"用户名 - humanizedDateTime()"`（每次生成新时间戳，几乎不重复）
- 用户输入空字符串 → 自动生成 `"Checkpoint #N"` 循环递增查找未被使用的编号（bookmarks.js 第89-94行）
- 分支命名格式：`"Branch #${mesId} - humanizedDateTime()"`，无去重逻辑

手动删除检查点文件后，空名自动递增编号可能跳跃，但已存在的 bookmark_link 链接会变成死链（跳转时 404/无法打开）。

### 8.6 context.chat 数组是可变引用

**置信度：high [文档·多源]** | 来源：官方扩展 API 文档

扩展 API 文档明确：`"Messages are mutable, so any changes you make to the array will be reflected"`。在 `GENERATION_AFTER_COMMANDS` 事件中修改 `chat` 数组会真实影响发给 LLM 的内容，但修改必须在回调中**同步完成**（异步修改可能无效）。

### 8.7 getChatMessages 返回的字段名不统一

**置信度：high [真机]** | 来源：`control_center.js`（生产卡）

不同版本 ST 或不同消息来源的字段名存在差异，必须做三重兼容：

```javascript
msg?.message || msg?.mes || msg?.text || ''
```

### 8.8 分支切换会触发 CHAT_CHANGED

**置信度：high [推断+真机]** | 来源：本地实战

`/branch-create` 执行后立即切换聊天，触发 `CHAT_CHANGED` 事件。角色卡的所有状态需在 `CHAT_CHANGED` 回调中重新初始化，否则旧状态残留。

### 8.9 swipe 切换只触发 MESSAGE_SWIPED，不触发 MESSAGE_UPDATED

**置信度：medium [推断]**

需要同时监听两个事件才能覆盖所有消息变化场景。

### 8.10 /hide 无参数默认末尾，range 是包含区间

**置信度：high [运行时源码]** | 来源：`slash-commands.js`

`3-7` 表示下标 3、4、5、6、7 共 5 条消息（包含两端）。

---

## 9. 悬案（不确定项 + 验证路径）

以下各项 **置信度：low [推断]** 或 **medium [单源]**，施工前必须验证。

| 悬案 | 置信度 | 验证路径 |
|---|---|---|
| `getVariables({ type: 'chat' })` 是否合法？与 `chat_metadata.variables` 有何区别？ | low [推断] | 真机：正则脚本中调用 `getVariables({ type: 'chat' })` 看是否返回 local 变量对象 |
| `message.variables` 顶层字段由谁写入、何时写入？ | medium [文档·单源] | 搜索 `script.js` 中 `message.variables` 赋值代码；或检查正则脚本 `variables` 全局对象的实际来源 |
| `extra.branches[]` 的完整结构（纯字符串数组还是对象数组）？ | **已解除 [运行时源码]**：bookmarks.js 第189行直读 `lastMes.extra['branches'].push(name)`，name 是字符串，确认为**纯字符串数组** | — |
| `swipe_info` 字段（历史版本存在）的当前状态？ | **已解除 [运行时源码]**：slash-commands.js 3872-3880行直读，`swipe_info` 字段在 v1.14.0 中**仍在写入和维护**，每个 swipe 对应一个含 `send_date/gen_started/gen_finished/extra` 的对象 | — |
| `MESSAGE_SWIPED` 事件的 `messageId` 是划动前还是划动后的楼层索引？ | medium [推断] | 真机：在 MESSAGE_SWIPED 回调中 log messageId，同时查看 swipe_id 变化 |
| `/checkpoint-go` 不带参数时跳转到哪个检查点？ | **已解除 [运行时源码]**：bookmarks.js 第514-515行直读：无参数时 `mesId = getLastMessageId()`（最后一条消息），然后取该楼 `chat[mesId].extra?.bookmark_link`，若无检查点则报 warning 并返回空 | — |
| `getVariables({ type: 'message' })` 在 swipe 切换后是否即时反映新 swipe 的变量？ | medium [推断] | 真机：监听 MESSAGE_SWIPED 后立即调用 getVariables，检查返回值是否已更新 |
| `/addswipe switch=true` 是否已合并到 release 分支（issue #2626 显示 "Done staging"）？ | **已解除 [运行时源码]**：slash-commands.js 本地 v1.14.0 直读（1778-1784行）确认 `switch` 参数已在 v1.14.0 实装，`isTrueBoolean(args.switch)` 处理切换逻辑（3894行） | — |

---

## 10. 来源索引

| 来源 | 类型 | 用途 |
|---|---|---|
| [ST 官方 Chatting 文档](https://docs.sillytavern.app/usage/chatting/) | 文档·官方 | 消息操作基础 |
| [ST STscript 语言参考](https://docs.sillytavern.app/usage/st-script/) | 文档·官方 | 命令参数规范 |
| [ST UI Extensions 文档](https://docs.sillytavern.app/for-contributors/writing-extensions/) | 文档·官方 | 扩展 API |
| [bookmarks.js 源码](https://raw.githubusercontent.com/SillyTavern/SillyTavern/release/public/scripts/bookmarks.js) | 运行时源码 | 分支/检查点实现 |
| [slash-commands.js 源码](https://raw.githubusercontent.com/SillyTavern/SillyTavern/release/public/scripts/slash-commands.js) | 运行时源码 | 消息操作命令实现 |
| [variables.js 源码](https://raw.githubusercontent.com/SillyTavern/SillyTavern/release/public/scripts/variables.js) | 运行时源码 | 变量体系实现 |
| [events.js 源码](https://raw.githubusercontent.com/SillyTavern/SillyTavern/staging/public/scripts/events.js) | 运行时源码 | 事件系统 |
| [script.js 源码](https://raw.githubusercontent.com/SillyTavern/SillyTavern/release/public/script.js) | 运行时源码 | Message 对象结构 |
| [DeepWiki 5.2 Chat Storage](https://deepwiki.com/SillyTavern/SillyTavern/5.2-chat-storage-and-persistence) | 文档·多源 | JSONL 结构 |
| [DeepWiki 7.3 Variables](https://deepwiki.com/SillyTavern/SillyTavern/7.3-variables-macros-and-scripting) | 文档·多源 | 变量体系 |
| [DeepWiki Extension Architecture](https://deepwiki.com/SillyTavern/SillyTavern/8.1-extension-architecture-and-apis) | 文档·多源 | 扩展 API |
| [LALib README](https://github.com/LenAnderson/SillyTavern-LALib/blob/master/README.md) | 社区（第三方扩展） | LALib 命令参考（需用户安装） |
| [SillyTavern-MessageVariables](https://github.com/LenAnderson/SillyTavern-MessageVariables) | 社区 | message-level 变量绑定机制 |
| [SillyTavern.NET-File-Converter](https://github.com/rarefushion/SillyTavern.NET-File-Converter) | 社区 | Message 对象字段参考 |
| [1.12.6 Release Notes](https://newreleases.io/project/github/SillyTavern/SillyTavern/release/1.12.6) | 文档·官方 | 检查点命令上线版本 |
| [PR #5040 hidden消息正则修复](https://github.com/SillyTavern/SillyTavern/pull/5040) | 运行时源码 | hidden/is_system 差异 |
| [Issue #2151 JSONL格式不一致](https://github.com/SillyTavern/SillyTavern/issues/2151) | 社区 | send_date 格式问题 |
| [Issue #2626 addswipe switch参数](https://github.com/SillyTavern/SillyTavern/issues/2626) | 社区 | /addswipe switch 合并状态 |
| `C:/Users/Administrator/OneDrive/ST-/角色卡工作区/模块化组件/control_center/core/control_center.js` | 真机 | 变量 API、事件监听实战范式 |
