# 提示词与预设（Prompts & Presets）参考手册

> 文档版本：v1.0.0 ｜ 最后更新：2026-07-14

> SillyTavern 提示词系统、Prompt Manager、预设文件结构与宏的完整参考。

---

## 置信度分级体系

本手册采用三级置信度标注，格式为 `置信度 [依据类型]`：

- **high**：多源一致 + 源码直接验证，可直接施工
- **medium**：单源或推断，可参考施工，宜在真机验证后再大规模应用
- **low**：孤证或纯推断，**禁止直接施工**，须先验证

依据类型轴：`[运行时源码]` / `[文档·多源]` / `[文档·单源]` / `[社区]` / `[推断]` / `[真机/生产卡]`

收集日期：2026-06-28  
回源基准：SillyTavern v1.14.0–v1.18.0 + 本地成熟卡（星月 2.9.7 / 交错宙域 1.9.x）  
交叉验证：跨 7 个独立来源（docs.sillytavern.app / SillyTavern release branch 源码 / character-card-spec-v3 / 本工作区生产卡 / 本工作区 ST 指南数据库）

---

## 目录

1. [机制总览：Chat Completion vs Text Completion 双轨](#1-机制总览chat-completion-vs-text-completion-双轨)
2. [Prompt Manager（Chat Completion 专用）](#2-prompt-managerchat-completion-专用)
   - 2.1 [系统预设 Prompt 完整列表](#21-系统预设-prompt-完整列表)
   - 2.2 [每个 Prompt 条目的完整字段](#22-每个-prompt-条目的完整字段)
   - 2.3 [injection_position 详解](#23-injection_position-详解)
   - 2.4 [injection_trigger 生成类型过滤](#24-injection_trigger-生成类型过滤)
   - 2.5 [Preset 文件完整结构](#25-preset-文件完整结构)
3. [角色卡与 Prompt 的交汇字段](#3-角色卡与-prompt-的交汇字段)
   - 3.1 [CCV2/CCV3 中的 prompt 相关字段](#31-ccv2ccv3-中的-prompt-相关字段)
   - 3.2 [system_prompt / post_history_instructions 生效条件](#32-system_prompt--post_history_instructions-生效条件)
4. [Post-History Instructions（PHI）机制](#4-post-history-instructionsphi机制)
5. [Context Template（Text Completion 专用）](#5-context-templatetext-completion-专用)
   - 5.1 [Story String（Handlebars 语法）](#51-story-stringhandlebars-语法)
   - 5.2 [其他字段](#52-其他字段)
6. [Instruct Mode（Text Completion 专用）](#6-instruct-modetext-completion-专用)
   - 6.1 [核心序列字段](#61-核心序列字段)
   - 6.2 [`{{name}}` 宏（Instruct 专用）](#62-name-宏instruct-专用)
7. [宏系统（Macros）完整参考](#7-宏系统macros完整参考)
   - 7.1 [核心语法规则](#71-核心语法规则)
   - 7.2 [宏速查表](#72-宏速查表)
   - 7.3 [`{{if}}` 条件宏语法](#73-if-条件宏语法)
   - 7.4 [`{{outlet::Name}}` 使用约束](#74-outletname-使用约束)
8. [世界书条目与 Prompt 注入（atDepth 路径）](#8-世界书条目与-prompt-注入atdepth-路径)
   - 8.1 [WI 注入到 prompt 的位置枚举](#81-wi-注入到-prompt-的位置枚举)
   - 8.2 [atDepth 条目的 role 字段](#82-atdepth-条目的-role-字段)
   - 8.3 [atDepth 四步注入流水线](#83-atdepth-四步注入流水线)
9. [扩展插件 prompt 注入 API](#9-扩展插件-prompt-注入-api)
10. [Claude API 的特殊处理（prompt-converters.js）](#10-claude-api-的特殊处理prompt-converterjs)
11. [完整可用范式代码](#11-完整可用范式代码)
12. [高频避坑](#12-高频避坑)
13. [悬而未决（真机验证前勿据此施工）](#13-悬而未决真机验证前勿据此施工)
14. [来源索引](#14-来源索引)

---

## 1. 机制总览：Chat Completion vs Text Completion 双轨

**high `[文档·多源]`**

ST 的 prompt 构建管线分为完全独立的两套：

```
API 类型                    Prompt 构建控制面板
──────────────────────────────────────────────────
Chat Completion             Prompt Manager（本文档主讲）
  (OpenAI / Claude / etc.)
Text Completion             Advanced Formatting 面板
  (KoboldAI / tabbyAPI 等)   → Context Template (Story String)
                             → Instruct Mode（仅此模式需要）
──────────────────────────────────────────────────
```

两套不互通。角色卡开发基本针对 Chat Completion（OpenAI 兼容接口 + Claude API），故本文档重点覆盖 Prompt Manager 路径。

来源：docs.sillytavern.app/prompts + docs.sillytavern.app/advanced-formatting 交叉验证  
适用版本：v1.14.0–v1.18.0

---

## 2. Prompt Manager（Chat Completion 专用）

### 2.1 系统预设 Prompt 完整列表

**high `[运行时源码]`**

以下 identifier 为 ST 硬编码的系统预设，顺序参照 `PromptManager.js` 中 `promptManagerDefaultPromptOrder`（注意：Default.json 预设文件的 character_id=100000 顺序与此略有差异，且 Default.json 的 character_id=100000 中无 `personaDescription`，仅 character_id=100001 的全局 group 默认顺序才含此项）：

| identifier | 显示名 | 类型 | 默认 role | 默认 enabled |
|---|---|---|---|---|
| `main` | Main Prompt | 内容型 | system | ✅ |
| `worldInfoBefore` | World Info (↑Char) | 标记型（marker） | — | ✅ |
| `personaDescription` | Persona Description | 标记型 | — | ✅（仅在 promptManagerDefaultPromptOrder 中；Default.json character_id=100000 无此项） |
| `charDescription` | Character Description | 标记型 | — | ✅ |
| `charPersonality` | Character Personality | 标记型 | — | ✅ |
| `scenario` | Character Scenario | 标记型 | — | ✅ |
| `worldInfoAfter` | World Info (↓Char) | 标记型 | — | ✅ |
| `dialogueExamples` | Chat Examples | 标记型 | — | ✅ |
| `nsfw` | Auxiliary Prompt | 内容型 | system | ✅ |
| `enhanceDefinitions` | Enhance Definitions | 内容型 | system | ❌（默认关） |
| `chatHistory` | Chat History | 标记型 | — | ✅ |
| `jailbreak` | Post-History Instructions | 内容型 | system | ✅ |

说明：
- `marker=true` 的条目是占位符，内容由 ST 运行时动态填充，用户**不能编辑正文**。
- `system_prompt=true` 的条目在 PromptManager.js 内受到特殊保护，不允许被角色卡 override 覆盖。
- Default.json 实际默认顺序（character_id=100000）为：main → worldInfoBefore → charDescription → charPersonality → scenario → enhanceDefinitions(关) → nsfw → worldInfoAfter → dialogueExamples → chatHistory → jailbreak。

来源：本地 v1.14.0 `PromptManager.js` `chatCompletionDefaultPrompts` + `promptManagerDefaultPromptOrder`（直读）+ `default/content/presets/openai/Default.json`（直读）  
适用版本：v1.14.0–v1.18.0

---

### 2.2 每个 Prompt 条目的完整字段

**high `[运行时源码]`**

来源：`PromptManager.js` release branch

```json
{
  "identifier": "my_custom_prompt",    // string，自定义用 UUID 或自命名
  "name": "显示名称",                   // UI 显示用，不发给模型
  "content": "实际注入内容，支持{{宏}}",
  "role": "system",                    // "system" | "user" | "assistant"
  "enabled": true,                     // bool，是否启用
  "system_prompt": false,              // bool，系统预设才为 true；禁止用户新建时设为 true
  "marker": false,                     // bool，标记型占位符；用户不能设为 true
  "injection_position": 0,             // 0=RELATIVE, 1=ABSOLUTE(In-Chat)
  "injection_depth": 4,                // injection_position=1 时生效，深度值
  "injection_order": 100,              // 同 role+depth 时的次序（数值越小越靠前）
  "forbid_overrides": false,           // bool，禁止角色卡 override
  "extension": false,                  // bool，是否由扩展插件注入
  "injection_trigger": []              // string[]，限制触发生成类型（见 2.4）
}
```

适用版本：v1.14.0–v1.18.0

---

### 2.3 injection_position 详解

**high `[运行时源码]`**

来源：`PromptManager.js` INJECTION_POSITION 枚举

| 值 | 常量名 | 行为 |
|---|---|---|
| `0` | `RELATIVE` | 按 UI 拖拽顺序放置，不进入聊天历史区 |
| `1` | `ABSOLUTE`（In-Chat） | 按 `injection_depth` 插入聊天历史中，忽略拖拽顺序 |

**深度语义**（high `[文档·多源]`）：

```
depth=0 → 插在最后一条消息之后（对话末尾）
depth=1 → 插在最后一条消息之前（倒数第1条的上方）
depth=2 → 插在倒数第2条消息之前
depth=N → 插在倒数第N条消息之前
```

默认值 `injection_depth=4`（与世界书 DEFAULT_DEPTH 一致）。

**同 depth 的多条 prompt 排序**（**high `[运行时源码 + 文档·多源]`**）：  
按 `injection_order` **数值越小越靠前**（在最终输出中更早出现）。官方文档原文："The lower the number, the earlier it is sent."。源码机制：`openai.js` `populationInjectionPrompts()` 中先以 order 降序(`b - a`)处理（大数值先 splice），再整体 `messages.reverse()`，净效果是小数值 order 的 prompt 在最终 prompt 序列中靠前。同 injection_order 时，roles 按 `system → user → assistant` 顺序推入（注释"most important go lower"，反转后靠后即更接近生成末端）。

来源：本地 v1.14.0 `openai.js` L760-791（直读）+ docs.sillytavern.app/prompt-manager（多源交叉）  
适用版本：v1.14.0–v1.18.0

---

### 2.4 injection_trigger 生成类型过滤

**high `[文档·多源]`**

来源：docs.sillytavern.app/prompt-manager + 本工作区A3_世界书优化.md 19.1 节交叉验证

| 类型字符串 | 含义 |
|---|---|
| `"normal"` | 常规消息生成 |
| `"continue"` | 点击"继续"按钮 |
| `"impersonate"` | 点击"扮演"按钮 |
| `"swipe"` | 划动重生成 |
| `"regenerate"` | 点击"重新生成"（群聊走 normal） |
| `"quiet"` | 后台静默生成（扩展/STscript 触发） |

**注意**：空数组 `[]` = 所有类型均触发，相当于不过滤。若要限制特定类型，必须显式填写类型列表。

适用版本：v1.14.0–v1.18.0

---

### 2.5 Preset 文件完整结构

**high `[运行时源码 + 文档·多源]`**

来源：Default.json + PromptManager.js

```json
{
  "name": "预设名称",

  // ── Sampling 参数 ──
  "temperature": 1,
  "top_p": 1,
  "top_k": 0,
  "frequency_penalty": 0,
  "presence_penalty": 0,
  "repetition_penalty": 1,
  "openai_max_context": 4095,
  "openai_max_tokens": 300,   // Default.json 实际默认 300；注意：0 的语义为"使用最大上下文值"（medium·推导未验证，待确认）

  // ── Prompt 定义列表 ──
  "prompts": [
    { /* prompt 条目，见 2.2 */ }
  ],

  // ── Prompt 排列顺序（按角色定制） ──
  "prompt_order": [
    {
      "character_id": 100000,   // 100000=全局默认，其他值=特定角色
      "order": [
        { "identifier": "main",              "enabled": true },
        { "identifier": "worldInfoBefore",   "enabled": true },
        { "identifier": "charDescription",   "enabled": true },
        { "identifier": "charPersonality",   "enabled": true },
        { "identifier": "scenario",          "enabled": true },
        { "identifier": "worldInfoAfter",    "enabled": true },
        { "identifier": "dialogueExamples",  "enabled": true },
        { "identifier": "nsfw",              "enabled": true },
        { "identifier": "chatHistory",       "enabled": true },
        { "identifier": "jailbreak",         "enabled": true }
      ]
    }
  ]
}
```

**重要**：若 preset 与角色卡同名，ST 会在打开该角色时自动切换到该 preset——通常为非预期行为。命名建议加前缀区分（如 `[通用]写实风格`）。

来源：SillyTavern/default/content/presets/openai/Default.json + PromptManager.js  
适用版本：v1.14.0–v1.18.0

---

## 3. 角色卡与 Prompt 的交汇字段

### 3.1 CCV2/CCV3 中的 prompt 相关字段

**high `[运行时源码 + 文档·多源 + 真机/生产卡]`**

来源：character-card-spec-v3 SPEC_V3.md + 本地生产卡 `星月私立高等学院 MVU 2.9.7.json`

```json
{
  "spec": "chara_card_v3",
  "spec_version": "3.0",
  "data": {
    "name": "角色名",
    "description": "角色描述（注入到 charDescription marker）",
    "personality": "性格（注入到 charPersonality marker）",
    "scenario": "场景（注入到 scenario marker）",
    "mes_example": "示例对话（注入到 dialogueExamples marker）",

    // ── Prompt 覆盖字段 ──
    "system_prompt": "",           // 若非空，且用户开启「Prefer Char. Prompt」则覆盖 main
    "post_history_instructions": "", // 若非空，且用户开启「Prefer Char. Instructions」则覆盖 jailbreak

    "extensions": {
      // ── depth_prompt：深度注入（角色卡内嵌深度 prompt）──
      "depth_prompt": {
        "prompt": "注入内容（支持宏）",
        "depth": 4,              // 默认 4，插入位置同 injection_depth
        "role": "system"         // "system" | "user" | "assistant"
      },

      // ── 绑定的世界书 ──
      "world": "世界书文件名（不含 .json）"
    }
  }
}
```

来源（depth_prompt 字段）：本地生产卡 `星月私立高等学院 MVU 2.9.7.json` L33-37，`depth_prompt.depth=4, role="system"` **high `[真机/生产卡]`**

适用版本：v1.14.0–v1.18.0

---

### 3.2 system_prompt / post_history_instructions 生效条件

**medium·单源孤证 `[文档·单源 + 运行时源码（默认值）]`**

- 角色卡的 `data.system_prompt` 字段非空时，**仅在用户 ST 设置中开启「Prefer Character Prompt」**后才覆盖 preset 的 main prompt；**该选项默认为开启**（`[运行时源码]` 本地 v1.14.0 `power-user.js` L208：`prefer_character_prompt: true`）。
- 角色卡的 `data.post_history_instructions` 字段非空时，同理需要开启「Prefer Character Instructions」（`prefer_character_jailbreak`）；**该选项默认同样为开启**（`power-user.js` L209）。
- 若卡的这两个字段为空字符串，则完全不影响用户 preset 设置。
- `{{original}}` 宏可在角色卡 `system_prompt` 中引用全局 preset 的 main prompt 内容（见 7.2 节宏表）。

来源：docs.sillytavern.app/prompt-manager（单源）+ 本地 v1.14.0 `power-user.js` L208-209（默认值）  
仍需真机验证：override 的精确内容替换还是追加行为（见悬案 P2）。

---

## 4. Post-History Instructions（PHI）机制

**high `[运行时源码]`**（role 字段源码直读；"优先级最高"为推断，单独标注见下）

PHI 在 Prompt Manager 中对应 `identifier="jailbreak"` 的条目（尽管 UI 显示名是"Post-History Instructions"），其特殊之处：

- 作为 **system role 注入**（`[运行时源码]` 本地 v1.14.0 `chatCompletionDefaultPrompts` 直读），按 UI 拖拽顺序位于 chatHistory 之后，插在 prompt 末尾（最近的 assistant 回复请求之前）
- 优先级最高——因为紧靠生成请求，模型最后读到它（**medium·推导未验证 `[推断]`**：此处为逻辑推断，不同模型权重分布可能不同）
- 可包含宏（`{{char}}`、`{{user}}` 等）

> **[运行时源码] 校准**：本地 v1.14.0 `openai.js` L2034-2038 与 `PromptManager.js` L2034-2038 均明确 `jailbreak` 的 `role` 为 `"system"` 而非 `"user"`。原文"user role 的不可见注入"为错误描述，已更正。用户可自行在 Prompt Manager 中将 role 改为 user，但默认即 system。

标准 Chat Completion prompt 末端结构：

```
[...聊天历史...]
[Post-History Instructions（system role，默认；用户可改为 user/assistant）]
[最后一条 assistant 前缀（或空 assistant 开头消息触发回复）]
```

来源：docs.sillytavern.app/prompt-manager + PromptManager.js `chatCompletionDefaultPrompts`（本地 v1.14.0 直读）  
适用版本：v1.14.0–v1.18.0

---

## 5. Context Template（Text Completion 专用）

**high `[文档·多源]`**

Context Template **仅影响 Text Completion API** 的 prompt 格式化，Chat Completion 完全不用此配置。

### 5.1 Story String（Handlebars 语法）

范式（**medium·单源孤证 `[文档·单源]`**）：

```handlebars
{{#if system}}{{system}}
{{/if}}{{#if description}}{{char}}'s personality: {{description}}
{{/if}}{{#if personality}}{{char}}'s persona: {{personality}}
{{/if}}{{#if scenario}}Scenario: {{scenario}}
{{/if}}{{#if persona}}[{{user}}'s persona: {{persona}}]
{{/if}}
```

可用 Handlebars 参数：

| 参数 | 说明 |
|---|---|
| `{{description}}` | 角色描述字段 |
| `{{scenario}}` | 场景字段 |
| `{{personality}}` | 角色性格字段 |
| `{{system}}` | 系统提示词内容 |
| `{{persona}}` | 用户人设描述 |
| `{{char}}` | 角色名 |
| `{{user}}` | 用户名 |

来源：docs.sillytavern.app/context-template  
适用版本：v1.14.0–v1.18.0

---

### 5.2 其他字段

**high `[文档·多源]`**

| 字段 | 说明 |
|---|---|
| `example_separator` | 示例对话块间的分隔符（替换 `<START>` 标签） |
| `chat_start` | Story String 之后、第一条消息之前的分隔符 |
| `use_stop_strings` | 将分隔符加入 stop strings |
| `names_as_stop_strings` | 将 char/user 名加入 stop strings |

来源：docs.sillytavern.app/context-template  
适用版本：v1.14.0–v1.18.0

---

## 6. Instruct Mode（Text Completion 专用）

**high `[文档·多源]`**

Instruct Mode **仅适用于 Text Completion API**。Chat Completion（OpenAI/Claude）完全不需要也不支持此设置——配置了 Instruct Mode 对这些 API 没有任何作用。

来源：docs.sillytavern.app/instructmode + 源码分析

### 6.1 核心序列字段

**high `[文档·多源]`**

```json
{
  "name": "ChatML",
  "user_alignment_message": "",          // 用户填充消息（user-first 约束时使用）
  "system_prompt": "",                   // 独立系统提示词（已从序列中分离）

  // 消息包裹序列
  "input_sequence": "<|im_start|>user\n",
  "input_suffix": "<|im_end|>\n",
  "output_sequence": "<|im_start|>assistant\n",
  "output_suffix": "<|im_end|>\n",
  "system_sequence": "<|im_start|>system\n",
  "system_sequence_suffix": "<|im_end|>\n",

  // 特殊序列
  "first_output_sequence": "",           // 第一条 assistant 消息前缀（聊天历史专用）
  "last_output_sequence": "",            // 最后一条 assistant 消息前缀（实际生成用）
  "system_instruction_sequence": "",     // 后台静默生成时的系统前缀

  "stop_sequence": "<|im_end|>",         // 停止序列（生成到此截断）
  "wrap": true,                          // 是否用换行包裹序列
  "macro": true,                         // 是否在序列中替换 {{宏}}
  "names": "never",                      // "never" | "always" | "groups_and_personas"
  "activation_regex": ""                 // 模型名匹配 regex，自动激活此模板
}
```

来源：docs.sillytavern.app/instructmode  
适用版本：v1.14.0–v1.18.0

---

### 6.2 `{{name}}` 宏（Instruct 专用）

**high `[文档·多源]`**

在 Instruct 序列中，`{{name}}` 返回当前消息的实际发言者名（而非固定的 `{{char}}` 或 `{{user}}`），适合群聊或 `/sendas` 场景。此宏**仅在 Instruct 序列字段**内有意义，在普通 prompt content 中行为未定义。

来源：docs.sillytavern.app/instructmode  
适用版本：v1.14.0–v1.18.0

---

## 7. 宏系统（Macros）完整参考

**high `[文档·多源]`**

来源：docs.sillytavern.app/macros + 本地生产卡验证

### 7.1 核心语法规则

- 双花括号包裹：`{{macroName}}`
- 大小写不敏感：`{{USER}}` = `{{user}}`
- 支持嵌套：`{{getvar::{{char}}_mood}}`
- **两套语法不互通**：Handlebars 风格（Context Template）与 ST 原生宏（其他位置），不能混用

来源：docs.sillytavern.app/macros  
适用版本：v1.14.0–v1.18.0

---

### 7.2 宏速查表

**high `[文档·多源]`**，`{{outlet::Name}}` 条目 **high `[文档·单源]`**

| 宏 | 说明 | 可用位置 |
|---|---|---|
| `{{char}}` | 当前角色名 | 全部 |
| `{{user}}` | 当前用户/人设名 | 全部 |
| `{{description}}` | 角色描述字段 | Context Template |
| `{{personality}}` | 角色性格字段 | Context Template |
| `{{scenario}}` | 场景字段 | Context Template |
| `{{persona}}` | 用户人设描述 | Context Template |
| `{{system}}` | 系统提示词内容 | Context Template |
| `{{mesExamples}}` | Instruct 格式的示例对话 | 内容型 prompt |
| `{{mesExamplesRaw}}` | 原始格式示例对话（无 Instruct 格式化） | 内容型 prompt |
| `{{lastMessage}}` | 最后一条消息内容 | 全部 |
| `{{lastUserMessage}}` | 最后一条用户消息 | 全部 |
| `{{lastCharMessage}}` | 最后一条角色消息 | 全部 |
| `{{original}}` | 原始内容（角色卡 prompt override 中引用全局设置） | 角色卡 override 字段 |
| `{{time}}` | 当前时间 | 全部 |
| `{{date}}` | 当前日期 | 全部 |
| `{{weekday}}` | 星期 | 全部 |
| `{{datetimeformat::format}}` | 自定义日期格式 | 全部 |
| `{{random::a::b::c}}` | 每次重新随机抽取 | 全部 |
| `{{pick::a::b::c}}` | 稳定随机选取（同位置一致） | 全部 |
| `{{getvar::name}}` | 读取本地变量 | 全部 |
| `{{setvar::name::value}}` | 设置本地变量 | 全部（有副作用） |
| `{{.varName}}` | 简写读本地变量 | 全部 |
| `{{.varName = value}}` | 简写设本地变量 | 全部 |
| `{{$varName}}` | 读全局变量 | 全部 |
| `{{trim}}` | 移除周围换行符 | 全部 |
| `{{outlet::Name}}` | 手动放置世界书 Outlet 插槽（禁止放在 WI 条目内） | prompt 字段 |
| `{{name}}` | Instruct 序列中当前消息发言者名 | Instruct 序列 |
| `{{if condition}}...{{/if}}` | 条件块 | 全部 |
| `{{if condition}}...{{else}}...{{/if}}` | 条件块带回退 | 全部 |
| `{{summary}}` | 聊天摘要（需 Summarize 扩展） | 全部（扩展依赖） |

来源：docs.sillytavern.app/macros + 本地生产卡验证  
适用版本：v1.14.0–v1.18.0

---

### 7.3 `{{if}}` 条件宏语法

**high `[文档·多源]`**

```
{{ if condition }} 内容 {{ /if }}
{{ if !condition }} 内容 {{ /if }}
{{ if condition }} 内容A {{ else }} 内容B {{ /if }}
```

falsy 值：空字符串 `""` / `false` / `0` / `off` / `no`

来源：docs.sillytavern.app/macros  
适用版本：v1.14.0–v1.18.0

---

### 7.4 `{{outlet::Name}}` 使用约束

**high `[文档·单源]`**

- 仅可放在 prompt 字段中（Main Prompt、Post-History Instructions 等内容型 prompt 条目正文）
- **不能**放在世界书条目 content 中（官方文档明确："Placing outlet macros inside World Info entries is not supported"）

来源：docs.sillytavern.app/macros（单源，官方文档直读）  
适用版本：v1.14.0–v1.18.0

---

## 8. 世界书条目与 Prompt 注入（atDepth 路径）

详细字段参照 `ST开发指南DB/A3_世界书优化.md`，本节仅覆盖与 prompt 直接交汇的部分。

### 8.1 WI 注入到 prompt 的位置枚举

**high `[运行时源码]`**

来源：A3_世界书优化.md 6.2 节，已本地 v1.14.0 核实

| position JSON 值 | 位置 |
|---|---|
| `0` | Before Char Defs（charDescription marker 之前） |
| `1` | After Char Defs（默认，charDescription 之后） |
| `2` | Before Example Messages |
| `3` | After Example Messages |
| `4` | At Depth（`injection_depth` 参数生效，同 PM 的 ABSOLUTE 模式） |
| `5` | Top of AN（Author's Note 顶部） |
| `6` | Bottom of AN（Author's Note 底部） |
| `7` | Outlet（手动用 `{{outlet::Name}}` 调用） |

来源：SillyTavern release branch `world-info.js` + A3_世界书优化.md  
适用版本：v1.14.0–v1.18.0

---

### 8.2 atDepth 条目的 role 字段

**high `[运行时源码]`**

来源：A3_世界书优化.md 2.1 节，已本地核实

| role 值 | 含义 |
|---|---|
| `0` | system |
| `1` | user |
| `2` | assistant |

适用版本：v1.14.0–v1.18.0

---

### 8.3 atDepth 四步注入流水线

源码调用链 **high `[运行时源码]`**；最终 splice+reverse 落位 **medium·推导未验证 `[推断]`**

```
1. checkWorldInfo() 收集激活条目 → WIDepthEntries（按 {depth, role} 分组）
2. script.js 调用 setExtensionPrompt(id, content, IN_CHAT=1, depth, false, role)
3. openai.js populationInjectionPrompts() 在循环 depth=i 时读取对应条目
4. splice() 进 newest-first messages 数组 → 最后 reverse()
```

注意：同一 depth+role 的多条 WI 条目在步骤1合并为单字符串（`join('\n')`），不是多条消息——无法通过 WI 在同深度产生多条独立消息。

来源：SillyTavern release branch `world-info.js` + `openai.js` + A3_世界书优化.md 20.3 节  
适用版本：v1.14.0–v1.18.0

---

## 9. 扩展插件 prompt 注入 API

**high `[运行时源码]`**

来源：A3_世界书优化.md 20.6 节 + `script.js` release branch

```javascript
import { setExtensionPrompt, extension_prompt_types, extension_prompt_roles }
  from '../../../script.js';

// IN_CHAT（atDepth）注入
setExtensionPrompt(
  'MY_UNIQUE_KEY',                         // string，扩展用的唯一键名
  '注入内容',
  extension_prompt_types.IN_CHAT,          // 1 = atDepth 模式
  2,                                       // depth=2
  false,                                   // 是否参与 WI 关键词扫描
  extension_prompt_roles.SYSTEM            // 0=system, 1=user, 2=assistant
);

// RELATIVE 注入（跟随 Prompt Manager 顺序）
setExtensionPrompt(
  'MY_UNIQUE_KEY',
  '注入内容',
  extension_prompt_types.BEFORE_PROMPT,    // 0 = RELATIVE 顶部
  0, false, 0
);
```

适用版本：v1.14.0–v1.18.0

---

## 10. Claude API 的特殊处理（prompt-converters.js）

**high `[运行时源码]`**（原描述有两处错误，已就地校准，见下）

来源：`<local-user-path>/OneDrive\ST-\SillyTavern-1.14.0\src\prompt-converters.js`（本地 v1.14.0 直读）

Claude 的 Messages API 对消息角色有严格约束，ST 通过 `convertClaudeMessages()` 处理（路径：server 侧 `src/endpoints/anthropic.js` 调用）：

- **头部 system 消息提取**：messages 数组最前端连续的 `role=system` 消息被提取为 Claude API 的独立 `system` 参数（仅在 `useSysPrompt=true` 时启用）
- **后续 system 消息转换为 user**：messages 数组中位于非头部的 `role=system` 消息一律被**转换为 `role=user`**（源码 L257 `message.role = 'user'`），而非"合并"
- **PROMPT_PROCESSING_TYPE.CLAUDE** 已标注 `@deprecated`，实际 Claude 处理不走 `STRICT mergeMessages`

> **[运行时源码] 校准**：原文"连续同角色消息通过 mergeMessages() 合并（STRICT 模式）"有误——CLAUDE 类型对应的是 `strict: false` 的 merge，且该路径已废弃。实际生效的是 `convertClaudeMessages()` 的"提取头部 system + 后续 system 转 user"双步逻辑。

**实践影响**：atDepth 注入的 system role WI 条目，在 Claude API 下如果位于 messages 数组非头部位置，会被**静默转换为 user role**（非合并），看起来像用户消息混入对话流。是否与真实用户消息合并取决于 `mergeMessages` 后处理（悬案 P3 依然成立）。如需保持 system 语义，应确保该注入位于 messages 头部。

适用版本：v1.14.0–v1.18.0（Claude API 路径）

---

## 11. 完整可用范式代码

### 11.1 自定义 Prompt 条目（Relative 模式，置于 chatHistory 之后）

**high `[运行时源码]`**

```json
{
  "identifier": "my_rules_prompt",
  "name": "写作规则",
  "content": "写作时务必：\n1. 保持第三人称视角\n2. 每次回复至少 300 字\n3. 角色名：{{char}}",
  "role": "system",
  "enabled": true,
  "system_prompt": false,
  "marker": false,
  "injection_position": 0,
  "injection_depth": 4,
  "injection_order": 100,
  "forbid_overrides": false,
  "injection_trigger": []
}
```

---

### 11.2 自定义 Prompt 条目（In-Chat/ABSOLUTE 深度注入，depth=1）

**high `[运行时源码]`**

```json
{
  "identifier": "style_reinforcement",
  "name": "风格强化（深度注入）",
  "content": "请注意：下一条回复请保持角色{{char}}的说话风格。",
  "role": "system",
  "enabled": true,
  "system_prompt": false,
  "marker": false,
  "injection_position": 1,
  "injection_depth": 1,
  "injection_order": 100,
  "forbid_overrides": false,
  "injection_trigger": ["normal", "swipe", "regenerate"]
}
```

---

### 11.3 角色卡 data.extensions.depth_prompt（生产卡实测格式）

**high `[真机/生产卡]`**

来源：`星月私立高等学院 MVU 2.9.7.json` L33-37（本工作区本地验证）

```json
"extensions": {
  "depth_prompt": {
    "prompt": "（此卡中为空；实际用世界书条目承载深度注入）",
    "depth": 4,
    "role": "system"
  },
  "world": "星月私立高等学院 ver2.9.7"
}
```

---

### 11.4 世界书 atDepth 条目 JSON（深度注入变量状态）

**high `[运行时源码]`**

来源：A3_世界书优化.md 20.5 节

```json
{
  "key": [],
  "keysecondary": [],
  "selective": false,
  "selectiveLogic": 0,
  "content": "当前角色状态：\n{{getvar::stat_data}}",
  "constant": true,
  "disable": false,
  "order": 100,
  "group": "",
  "groupOverride": false,
  "groupWeight": 100,
  "probability": 100,
  "sticky": 0,
  "cooldown": 0,
  "delay": 0,
  "extensions": {
    "position": 4,
    "depth": 4,
    "role": 0
  }
}
```

---

### 11.5 Context Template Story String 完整范式（Text Completion 专用）

**medium·单源孤证 `[文档·单源]`**

```handlebars
{{#if system}}{{system}}
{{/if}}[Character: {{char}}]
{{#if description}}Personality: {{description}}
{{/if}}{{#if personality}}{{char}}'s traits: {{personality}}
{{/if}}{{#if scenario}}Scenario: {{scenario}}
{{/if}}{{#if persona}}[{{user}} is: {{persona}}]
{{/if}}
```

来源：docs.sillytavern.app/context-template（单源）  
适用版本：v1.14.0–v1.18.0

---

## 12. 高频避坑

**high `[运行时源码 + 真机/生产卡 + 文档·多源]`**，个别条目见下方来源标注

1. **Preset 与角色卡同名陷阱**：Preset 若与某角色卡同名，打开该角色时会自动切换到该 preset——通常为非预期行为。Preset 命名建议加前缀区分（如 `[通用]写实风格`）。

2. **Claude API system 消息转换（非合并）**：messages 数组头部连续的 `role=system` 消息被提取为独立 system 参数；头部之后的 `role=system` 消息被**逐条转换为 `role=user`**（非合并成一条），会混入对话流（见悬案 P3）。原"合并"描述不准确——实为类型转换。`[运行时源码]` 本地 v1.14.0 `src/prompt-converters.js` L257 直读。

3. **depth=0 的歧义**：In-Chat depth=0 是"最末尾"（最新消息**之后**），而非"最顶部"。Post-History Instructions 本质上就是 depth=0 的 user role 注入。

4. **marker 标记型条目不能编辑正文**：`chatHistory`、`charDescription`、`worldInfoBefore` 等 `marker=true` 的系统条目，只能设置 enabled/disabled，不能修改其 content——内容由 ST 运行时自动填充。

5. **AN 频率=0 时 Top/Bottom of AN 的 WI 条目被静默跳过**：不报错，仅无效。使用 At Depth（position=4）代替。

6. **WI atDepth 同 depth+role 合并**：同一 depth+role 的多个 WI 条目在 prompt 中只产生一条消息（内容用换行拼接）——无法通过 WI 在同深度产生多条独立消息。

7. **`{{original}}` 宏只在角色卡 override 上下文有效**：在角色卡的 `system_prompt` 字段里使用 `{{original}}` 可引用当前 preset 的 main prompt 内容；在其他地方行为未定义。**high `[文档·单源]`**

8. **`{{outlet::Name}}` 禁止放在 WI 内容里**：文档明确不支持，仅在 Prompt Manager 的内容型条目正文中使用。**high `[文档·单源]`**

9. **Instruct Mode 仅对 Text Completion 生效**：Chat Completion（OpenAI/Claude）完全不走 Instruct 逻辑，配置了 Instruct Mode 对这些 API 没有任何作用。

10. **`injection_trigger` 为空 `[]` ≠ 任何类型都不触发**：空数组是"全部类型均触发"，相当于不过滤。若要只在特定类型触发，必须显式填写类型列表。

11. **`system_prompt=true` 的条目不允许用户新建时设为 true**：PromptManager.js 内对此有保护；新建的自定义条目务必保持 `"system_prompt": false`。

来源：PromptManager.js + prompt-converters.js + docs.sillytavern.app + A3_世界书优化.md  
适用版本：v1.14.0–v1.18.0

---

## 13. 悬而未决（真机验证前勿据此施工）

| ID | 问题 | 置信度 | 验证路径 |
|---|---|---|---|
| ~~P1~~ | ~~`injection_order` 值在多条同 depth+role prompt 中的精确排序方向（升序/降序哪个靠前？）~~ | **已解决 `[运行时源码 + 文档·多源]`** | 本地 v1.14.0 `openai.js` L760：`sort((a, b) => +b - +a)` 大数值先 splice + 末尾 `messages.reverse()`，净效果是**数值越小越靠前**（更早出现在最终 prompt）。官方文档："The lower the number, the earlier it is sent."双源一致。注意：同 injection_order 时 role 顺序 `system → user → assistant`（反转后越靠近末端），仍需真机验证观察是否与预期完全吻合。|
| P2 | 角色卡 `system_prompt` 字段中 `{{original}}` 宏的精确行为（是替换整个 main prompt 还是追加） | medium·单源孤证 | 设置角色卡 `system_prompt="{{original}}\n追加内容"`，开启 Prefer Char Prompt，观察发送的 prompt |
| P3 | Claude API 下 `role=user` 的 In-Chat 注入是否与真实用户消息合并（mergeMessages 的 STRICT 模式） | medium·推导未验证 | 设置 depth=2 role=user 的 In-Chat prompt，用 Claude 后端查看 Network 请求的 messages 数组 |
| ~~P4~~ | ~~`injection_order` 的默认值是否为 100，以及是否与世界书 `order` 字段存在计算关联~~ | **已解决 `[运行时源码]`** | 本地 v1.14.0 `PromptManager.js` L31-32：`const DEFAULT_ORDER = 100`，构造函数 `this.injection_order = injection_order ?? DEFAULT_ORDER`。默认值确为 100，与世界书 `order` 无直接关联（两者独立字段，openai.js 中 `extensionPromptsOrder` 也硬编码为 `'100'` 以便 WI 扩展提示在同 order 组插入）。|
| P5 | 同一角色卡的 `depth_prompt` 与 WI atDepth 条目在同 depth 时的合并行为 | medium·推导未验证 | 设置 `depth_prompt` + 同 depth WI 条目，观察是否合并为一条消息 |
| ~~P6~~ | ~~`Prefer Character Prompt` / `Prefer Character Instructions` 的默认开/关状态~~ | **已解决 `[运行时源码]`** | 本地 v1.14.0 `power-user.js` L208-209：`prefer_character_prompt: true, prefer_character_jailbreak: true`。两者默认均为**开启**。注意：用户历史 settings.json 可能覆盖此默认值；且"各 ST 版本是否一致"的跨版本问题未验证，如需跨版本确认仍需真机测试。|

---

## 14. 来源索引

- [Prompts | docs.sillytavern.app](https://docs.sillytavern.app/usage/prompts/)
- [Prompt Manager | docs.sillytavern.app](https://docs.sillytavern.app/usage/prompts/prompt-manager/)
- [Context Template | docs.sillytavern.app](https://docs.sillytavern.app/usage/prompts/context-template/)
- [Instruct Mode | docs.sillytavern.app](https://docs.sillytavern.app/usage/core-concepts/instructmode/)
- [Macros | docs.sillytavern.app](https://docs.sillytavern.app/usage/core-concepts/macros/)
- [Advanced Formatting | docs.sillytavern.app](https://docs.sillytavern.app/usage/core-concepts/advancedformatting/)
- [World Info | docs.sillytavern.app](https://docs.sillytavern.app/usage/core-concepts/worldinfo/)
- [character-card-spec-v3/SPEC_V3.md](https://github.com/kwaroran/character-card-spec-v3/blob/main/SPEC_V3.md)
- SillyTavern/SillyTavern release branch：`PromptManager.js`、`openai.js`、`prompt-converters.js`、`world-info.js`、`script.js`
- **本地 v1.14.0 直读（回源依据）**：`<local-user-path>/OneDrive\ST-\SillyTavern-1.14.0\public\scripts\PromptManager.js`、`openai.js`、`power-user.js`；`src\prompt-converters.js`；`default\content\presets\openai\Default.json`
- Default.json preset：`SillyTavern/default/content/presets/openai/Default.json`
- 本地生产卡：`ST开发指南DB\本地证据\星月历史\2.9.7\星月私立高等学院 MVU 2.9.7.json`
- 本工作区指南数据库：`<local-user-path>/OneDrive\ST-\角色卡工作区\ST开发指南DB\A3_世界书优化.md`
