# 前端应用 — 状态栏与控制中心

> 文档版本：v1.1.0 ｜ 最后更新：2026-07-14
>
> 原《状态栏与HUD实现范式.md》与《控制中心与弹窗模态架构.md》**重度合并去重版**（2026-06-28）。
>
> 回源环境：SillyTavern v1.14.0 + TavernHelper (JS-Slash-Runner) + MVU；生产卡：星月 2.5.0 / 2.9.9 + 交错宙域 2.6.0。
>
> 基础 API（getVariables / replaceVariables / eventOn / injectPrompts / window.parent 等）详见姐妹文档：**《C1_前端基础-TavernHelper与iframe.md》**
> 本文档不重复那些内容，直接引用节号。
>
> 2026-07-14 增补导航：本册继续负责 MVU 状态栏/控制中心的**内容与数据链**；入口与宿主外壳拆到 **《C11_悬浮球与功能入口.md》**、**《C12_抽屉式状态栏-移动端方案.md》**、**《C13_浮窗式状态栏.md》**；一次性开局初始化拆到 **《C10_开局页与自定义开局.md》**；宿主主题美化见 **《C9_酒馆界面美化.md》**；TH 脚本与魔棒协议见 **《E5_创意工坊与扩展生态.md》**。**《C4_数据库表格模板.md》**与 **《C7_数据库二创前端.md》**负责 SP·数据库及正常多楼层数据库 UI；**《C5_同层前端.md》**负责首楼内闭合全部游玩体验；**《C6_独立前端.md》**负责角色卡即 Web/App/PC 产品；实验兼容仅见 **《C8_数据库兼容同层前端-待验证.md》**。

---

## 目录

1. 置信度分级体系（全手册适用）
2. 机制总览
   - 2.1 状态栏 / HUD 四层实现链路
   - 2.2 控制中心执行上下文与双窗口穿透
   - 2.3 正则注入 iframe 机制
   - 2.4 控制中心面板架构演进（v2.5.0 vs v2.9.9）
   - 2.5 入口层、外壳层与内容层选择
3. 数据结构
   - 3.1 MvuData 顶层结构
   - 3.2 stat_data 字段结构（星月 2.5.0 实例）
   - 3.3 statRoot 兼容层与 safeGet 范式
   - 3.4 displayValue 美化逻辑
4. 关键 API 参考
   - 4.1 变量读写 API（状态栏场景）
   - 4.2 MVU 事件系统（详见基础文档 §11）
   - 4.3 跨 iframe API 发现链（控制中心）
   - 4.4 TavernHelper 扩展 API（控制中心专用）
   - 4.5 Prompt 注入 API（控制中心场景，详见基础文档 §7）
   - 4.6 世界书条目结构（updateWorldbookWith 格式）
   - 4.7 ST 官方扩展 API（扩展开发参考）
   - 4.8 世界书条目位置类型参考
5. 完整实现范式
   - 5.1 状态栏 iframe 初始化完整流程
   - 5.2 变量读取三级降级链
   - 5.3 防抖刷新调度器（schedulePopulate）
   - 5.4 DOM 绑定范式（setCardValue / setAlwaysCardValue）
   - 5.5 Tab 分区懒渲染范式
   - 5.6 多 iframe 去重机制
   - 5.7 控制中心面板创建与生命周期管理
   - 5.8 世界书事务写入范式
   - 5.9 MVU 变量读写三段式范式
   - 5.10 开局页挂载摘要（完整方案转 C10）
   - 5.11 custom- 前缀对策——MutationObserver 动态补齐
   - 5.12 Prompt 注入范式（GENERATION 事件钩子，v2.5.0 时代）
6. CSS 布局与主题变量
   - 6.1 主容器与分区结构
   - 6.2 CSS 变量系统
7. 高频避坑
   - 7.1 custom- 前缀导致动态注入内容丢样式
   - 7.2 整面板空白（分区渲染抛错未隔离）
   - 7.3 window.parent 访问失败
   - 7.4 CHAT_CHANGED 后魔杖入口丢失
   - 7.5 first_mes 塞大 HTML 引发上下文爆炸
   - 7.6 事件监听器不清理
   - 7.7 世界书写入用错 API
   - 7.8 开局页 script 标签不执行
   - 7.9 waitGlobalInitialized 必须 await
   - 7.10 getCurrentMessageId 仅限消息楼层 iframe
   - 7.11 数据签名防止无效重渲染
   - 7.12 iframe 重复问题
8. 悬案（未决项 + 真机验证路径）
9. 关键本地文件路径

---

## 1. 置信度分级体系（全手册适用）

与《C1_前端基础-TavernHelper与iframe.md》第 1 章**完全相同**，不重复展开。

**三级**：high（可直接施工）/ medium（谨慎施工，须防御性分支）/ low（禁止施工，先验证）

**依据类型轴**：`[运行时源码]` / `[真机]` / `[文档·多源]` / `[文档·单源]` / `[类型声明]` / `[CHANGELOG·单源]` / `[推断]`

**铁规**：同一知识点在两份文档出现时，置信度取**下确界**（两份中较低者）。

---

## 2. 机制总览

### 2.1 状态栏 / HUD 四层实现链路

**核心结论**

```
正则匹配变量块（Find Regex 匹配 AI 输出中的变量块标记）
    ↓
TavernHelper 检测代码块语法（```html 开头）→ 渲染为 iframe（srcdoc = HTML 内容）
    ↓
iframe 内 script[type=module] 调用 getVariables({type:'message'}) 或 Mvu.getMvuData()
读取当前楼层的 stat_data 变量
    ↓
DOM 操作渲染分区 → 监听 Mvu.events.VARIABLE_UPDATE_ENDED 事件驱动刷新
```

| 层 | 执行位置 | 关键接口 | 置信度 |
|----|---------|---------|--------|
| 正则注入 | ST 消息渲染层 | 正则 Replace With | high `[运行时源码]` |
| iframe 容器 | TavernHelper 扩展 | TH 内部端点 + srcdoc | high `[运行时源码]` |
| 变量读取 | iframe 内 JS | `getVariables` / `Mvu.getMvuData` | high `[运行时源码]` |
| 事件刷新 | iframe 内 JS | `Mvu.events.VARIABLE_UPDATE_ENDED` | high `[运行时源码]` |

来源：`status_bar_regex_beauty_r5.html`（生产卡，262KB）+ `status_bar_regex.html` ｜ 置信度：high `[运行时源码]`

### 2.2 控制中心执行上下文与双窗口穿透

**核心结论**

ST 里的卡内 JS（通过正则脚本注入）运行在消息渲染 iframe 内。控制中心采用**双窗口穿透**策略——始终优先访问 `window.parent`（ST 主窗口），失败则回落到 `window` 自身。真实 DOM（扩展菜单栏、聊天输入框等）在父窗口中，必须通过 `hostWindow()` / `hostDocument()` 访问。

```javascript
// 来自 control_center.js v2.9.9 第 49-54 行（最高可信）
function hostWindow() {
  try {
    if (window.parent && window.parent !== window && window.parent.document) return window.parent;
  } catch (_) {}
  return window;
}
function hostDocument() {
  try { return hostWindow().document || document; } catch (_) { return document; }
}
```

**核心原则**：所有需要访问 ST 主窗口 DOM 或全局 API 的操作，均通过 `hostWindow()` / `hostDocument()`，不直接用 `window` / `document`。

详见《C1_前端基础-TavernHelper与iframe.md》§10（window.parent 防御访问模式）。

来源：`control_center.js` v2.9.9 第 49-54 行 ｜ 置信度：high `[运行时源码]`

### 2.3 正则注入 iframe 机制

**核心结论**

TavernHelper 扩展检测消息中的代码块语法（` ```html ` 开头），将其内容渲染为内联 `<iframe>`，iframe 的 `src` 指向 TH 内部端点，`srcdoc` 为代码块内容。

`status_bar_regex.html` / `status_bar_regex_beauty_r5.html` 文件格式：文件首行为 ` ```html `，末行为 ` ``` `。这是**正则的 Replace With 内容**，已预先编译为完整 HTML。

```
# 正则配置结构
Find Regex: /\[变量块标记\]/g   （具体 pattern 卡内定制，见悬案 C）
Replace With:
```html
<!doctype html>...完整 HTML...
```
```

来源：`status_bar_regex_beauty_r5.html` 文件头部结构 ｜ 置信度：high `[运行时源码]`；（Find Regex 具体 pattern 见悬案 C）

### 2.4 控制中心面板架构演进（v2.5.0 vs v2.9.9）

**核心结论**

| 维度 | v2.5.0（376 行） | v2.9.9（3272 行） |
|---|---|---|
| 面板位置 | `position:fixed; right:22px; top:82px` | 支持拖拽 + resize，`savePanelRect` 持久化 |
| 切换机制 | `panel.hidden = !panelOpen` | 同，加拖拽持久化 |
| 内容渲染 | 单次 `innerHTML` 全量替换 | 分区 `safe()` 兜底渲染（任一分区抛错不影响其它） |
| 状态存储 | `localStorage`（v2 key） | `localStorage`（v291 key） |
| 面板入口 | 魔杖菜单 `#extensionsMenu` | 同，加 sidebar 侧边球 |
| 公开 API | `window.CrossedZoneControlCenter` | `window.XingyueControlCenter` + `window.CrossedZoneControlCenter`，并同步挂到 hostWindow |
| 变量写入 | `injectPrompts` 注入 | MVU `generateRaw → parseMessage → replaceMvuData` 三段式 |
| 开局页 | `first_mes` 内嵌大 HTML（历史 bug） | `first_mes` 只放短标记，remote fetch 注入（见范式 5.10） |

来源：两版本 `control_center.js` 对比 ｜ 置信度：high `[运行时源码]`

### 2.5 入口层、外壳层与内容层选择

2026-07-14 起，状态栏/控制中心按三层拆分，避免把“按钮长什么样”“面板在哪里”和“状态怎么读”混成一份巨型实现：

```text
入口层：Wand / 悬浮球 / 页面按钮
                 ↓ command registry
HUD Host Core：phase / generation / abort / contextRevision / dirty
                 ↓ ShellAdapter
             移动抽屉 / 桌面浮窗
                 ↓
     iframe + bridge + 状态栏内容与数据链

消息楼层生命周期 → 每楼/末楼 inline iframe → 同一内容协议/数据适配
```

| 选择 | 适合 | 不适合 | 专册 |
|---|---|---|---|
| 消息内联状态栏 | 每楼或末楼紧贴剧情，天然跟随消息 | 需要长期悬浮、跨楼操作 | 本册 |
| Wand 入口 | 最稳妥的既有功能菜单入口 | 想常驻屏幕和拖动 | E5 |
| 悬浮球入口 | 常驻、可吸边、多功能分发 | 直接承载完整状态内容 | C11 |
| 抽屉外壳 | 窄屏、粗指针、软键盘环境 | 任意拖动/缩放 | C12 |
| 浮窗外壳 | 桌面边聊边看、可拖动缩放 | 手机默认方案 | C13 |

内容层仍由本册负责：变量读取、事件刷新、Tab/分区渲染、写回与控制中心业务。C12/C13 应复用同一个顶层 HUD Host Core 与 iframe 真身，只替换 ShellAdapter；消息内联通常是每楼/末楼自己的 iframe 生命周期，只共享内容协议和数据适配，不默认加入同一个活动 singleton session。C11 的入口只 dispatch 命令，不复制业务处理器。

星月 3.4.9 已把抽屉与浮窗纳入共享 `hudSession`。抽屉是已落地的移动端生产路线，不得因旧模块化 fragment 未同步而标成待验证。具体视口、手势、几何和异步竞态分别由 C12/C13 维护。

---

## 3. 数据结构

### 3.1 MvuData 顶层结构

**核心结论**

`Mvu.getMvuData()` 返回 `MvuData` 对象，核心字段为 `stat_data`：

```typescript
type MvuData = {
  stat_data: Record<string, any>;                    // 实际数据对象（核心）
  initialized_lorebooks: Record<string, any[]>;      // Mvu 初始化的世界书条目
  [key: string]: any;
};
```

来源：`@types/iframe/exported.mvu.d.ts`（类型声明）+ `status_bar_regex_beauty_r5.html` L4680（`Mvu.getMvuData` 实际调用并访问 `.stat_data`）｜置信度：high `[运行时源码]` + `[文档·多源]`

### 3.2 stat_data 字段结构（星月 2.5.0 实例）

**核心结论**：以下为生产卡（星月 2.5.0）的 `stat_data` 完整顶层键结构。

```javascript
stat_data: {
  环境: { 日期, 时间, 经过天数, 所在位置, 城市环境, 近期节日 },
  user: {
    身份背景, 外貌身材, 当前穿着, 状态,
    基本属性: { 等级, EXP进度, 可分配属性点, 技能点, 精神韧性 },
    核心属性: { /* ... */ },
    体力, 饱腹值, 水分值, 洁净值, 校卡余额, 现金
  },
  天赋与技能: { 天赋: { /* 天赋词条 */ }, 技能: { /* 技能词条 */ } },
  持有物: { [物品名]: { 数量, 物品效果, 物品携带位置 } },
  仓库: { /* 同持有物结构 */ },
  规则变化: { 世界变化: { /* ... */ }, 校规变化: { /* ... */ } },
  雷达系统: { /* ... */ },
  任务: { [任务名]: { 任务状态, 当前阶段, 任务进度 } },
  近期事件: { [事件名]: { 时间, 地点, 类型, 摘要, 重要度 } },
  当前互动角色: { [角色名]: { 身份, 好感度, 堕落值, 服从度, 状态, 位置 } },
  关系网: { /* ... */ },
  羁绊角色: { [角色名]: { 身份, 变量冻结, 状态 } },
  载具: { /* ... */ },
  居所: { /* ... */ },
  莉莉丝的商店: { 积分, [商品名]: { 价格, 类型, 效果 } }
}
```

来源：`mvu_context.ejs` L132-L186 ｜ 置信度：high `[运行时源码]`

### 3.3 statRoot 兼容层与 safeGet 范式

**核心结论**：生产卡统一使用 `statRoot` 处理两种传入格式，`safeGet` 自动剥离 `stat_data.` 路径前缀。

```javascript
// stat_data 兼容层：允许传入完整 MvuData 或裸 stat_data
function statRoot(all) {
  if (!all || typeof all !== 'object') return {};
  return all.stat_data || all;
}

// 安全路径读取：自动剥离 'stat_data.' 前缀 + 空值处理
function safeGet(obj, path, def) {
  var root = statRoot(obj);
  var p = String(path || '');
  if (p.indexOf('stat_data.') === 0) p = p.slice(10);  // 兼容带前缀的 path
  var val = _.get(root, p);
  if (!shouldRenderValue(val)) return def;
  return displayValue(val, def);
}
```

**调用惯例**：外部调用建议统一传不带前缀的路径（如 `'user.等级'`），两种格式均支持。

来源：`status_bar_regex_beauty_r5.html` L1916-1918, L2324-2330 ｜ 置信度：high `[运行时源码]`

### 3.4 displayValue 美化逻辑

**核心结论**：生产卡无独立 `display_data` 字段（见悬案 A），美化逻辑内联在 `displayValue` 函数中。

```javascript
function displayValue(val, emptyText) {
  if (val === undefined || val === null || val === '') return emptyText || '--';
  if (Array.isArray(val)) return val.length ? val.map(/* ... */).join(', ') : '[]';
  if (typeof val === 'object') {
    var preferred = ['人员', '人数', '职责', '身份', '状态', '所在位置' /* ... */];
    // 先找预设字段，fallback 取前 5 个 key 拼接
    return parts.join(' | ');
  }
  return String(val);
}
```

来源：`status_bar_regex_beauty_r5.html` L1972-1988 ｜ 置信度：high `[运行时源码]`

---

## 4. 关键 API 参考

### 4.1 变量读写 API（状态栏场景）

> 完整 API 签名见《C1_前端基础-TavernHelper与iframe.md》§5。此处仅列状态栏场景特有说明。

**变量读取三级降级链（状态栏生产模式）**：

| 优先级 | 函数 | 说明 |
|---|---|---|
| 第一 | `getVariables({ type: 'message' })` | TavernHelper 原生 API，最快；无 message_id 时行为见悬案 B |
| 第二 | `Mvu.getMvuData({ type: 'message', message_id: getCurrentMessageId() })` | 明确指定楼层 ID，更稳定 |
| 降级 | 返回上次有效数据 `lastGoodHudData` 或空壳 `{ stat_data: {} }` | API 全不可用时保底 |

**变量写入（状态栏场景，更新当前楼层 stat_data）**：

```javascript
const msgId = getCurrentMessageId();
await updateVariablesWith(function(vars) {
  vars = vars || {};
  vars.stat_data = vars.stat_data || {};
  vars.stat_data.user = vars.stat_data.user || {};
  vars.stat_data.user['基本属性']['等级'] = newValue;
  return vars;
}, { type: 'message', message_id: msgId });
```

来源：`status_bar_regex_beauty_r5.html` L2986-L3017 + JS-Slash-Runner `src/function/variables.ts` ｜ 置信度：high `[运行时源码]`

### 4.2 MVU 事件系统

> 完整 API 见《C1_前端基础-TavernHelper与iframe.md》§11。

状态栏最常用的两个事件：

| 事件 | 字符串值 | 触发时机 | 置信度 |
|---|---|---|---|
| `Mvu.events.VARIABLE_INITIALIZED` | `'mag_variable_initiailized'`（⚠️ 双 i） | 变量首次初始化完成 | high `[运行时源码]` |
| `Mvu.events.VARIABLE_UPDATE_ENDED` | `'mag_variable_update_ended'` | 变量更新完成（**HUD 刷新主挂点**） | high `[运行时源码]` |

**使用前必须 `await waitGlobalInitialized('Mvu')`（或降级轮询），否则 `Mvu is not defined` 运行时错误。**

### 4.3 跨 iframe API 发现链（控制中心）

**核心结论**：卡内代码不能直接 `import` ST 扩展 API，只能通过全局对象发现。每个外部依赖走"本窗口 → 父窗口"双检。

```javascript
// Tavern Helper（核心依赖）
function helperHost() {
  const candidates = [window, hostWindow()];
  for (const target of candidates) {
    if (target?.TavernHelper) return target.TavernHelper;
    if (target?.generateRaw || target?.getChatMessages) return target;
  }
  return null;
}

// MVU（变量读写）
function mvuHost() {
  return window.Mvu || hostWindow().Mvu || null;
}

// 媒体库（同级模块）
function mediaLibrary() {
  return window.XingyueMediaLibrary || hostWindow().XingyueMediaLibrary ||
         window.CrossedZoneMediaLibrary || hostWindow().CrossedZoneMediaLibrary || null;
}
```

来源：`control_center.js` v2.9.9 ｜ 置信度：high `[运行时源码]`

### 4.4 TavernHelper 扩展 API（控制中心专用）

**核心结论**

| API | 用法 | 说明 |
|---|---|---|
| `TavernHelper.generateRaw(opts)` | `async → string` | 静默生成，不影响 chat history |
| `TavernHelper.getChatMessages(range)` | `→ Message[]` | 获取消息楼层，`-1` = 最新楼，整数 = 楼层 id |
| `Mvu.getMvuData({type:'message', message_id:'latest'})` | `→ MvuData` | 读取 MVU 变量数据 |
| `Mvu.replaceMvuData(newData, {type, message_id})` | `async` | 写回 MVU 变量（不改消息正文）｜ medium·单源孤证（无官方文档） |
| `Mvu.parseMessage(rawStr, oldData)` | `async → MvuData` | 从含 `<UpdateVariable>` 的原始文本解析变量更新 ｜ medium·单源孤证 |
| `TavernHelper.getCharWorldbookNames('current')` | `→ {primary, ...}` | 取当前角色卡绑定世界书名 |
| `TavernHelper.getWorldbookNames()` | `→ string[]` | 取所有世界书名列表 |
| `TavernHelper.updateWorldbookWith(name, fn, opts)` | `async` | 事务式更新世界书条目（fn 接收当前条目数组，返回新数组） |

来源：`control_center.js` v2.9.9 ｜ 置信度（generateRaw / getChatMessages / updateWorldbookWith / getCharWorldbookNames）：high `[运行时源码]`；（replaceMvuData / parseMessage）：medium·单源孤证（生产卡大量使用，但无官方文档，接口可能随版本变动，见悬案 F）

### 4.5 Prompt 注入 API

> 完整说明见《C1_前端基础-TavernHelper与iframe.md》§7。此处仅补充控制中心场景备注。

**v2.5.0 时代用法（已被 v2.9.9 MVU 三段式取代）**：`injectPrompts` + `GENERATION_AFTER_COMMANDS` 钩子；详见范式 5.12。

### 4.6 世界书条目结构（updateWorldbookWith 格式）

**核心结论**

```javascript
// makeConstantEntry 产出的完整条目结构
{
  name: '条目显示名',           // 等价于 TavernHelper 里的 comment
  enabled: true,
  strategy: {
    type: 'constant',            // constant = 常驻，keyword = 关键词触发
    keys: [],
    keys_secondary: { logic: 'and_any', keys: [] },
    scan_depth: 'same_as_global',
  },
  position: {
    type: 'before_author_note',  // 注入位置类型（完整列表见 §4.8）
    role: 'system',
    depth: 4,                    // chat depth；仅 type='at_depth' 时有效
    order: 100,                  // 数字大 → 更靠近 context 末尾
  },
  content: '世界书正文',
  probability: 100,
  recursion: { prevent_incoming: false, prevent_outgoing: false, delay_until: null },
  effect: { sticky: null, cooldown: null, delay: null },
  extra: {                       // 自定义元数据，TavernHelper 透传存储
    source: 'xingyue-opening-wizard',
    version: '2.9.9',
    kind: 'identity',
  },
}
```

来源：`control_center.js` v2.9.9 第 441-458 行 ｜ 置信度：high `[运行时源码]`

### 4.7 ST 官方扩展 API（扩展开发参考）

> 卡内 JS（非扩展）通常无法 `import` 这些 API；卡内等效路径见 §4.3 / §4.4。

**来源**：ST 官方文档 `docs.sillytavern.app/for-contributors/writing-extensions/` ｜ 置信度：medium·单源孤证 `[文档·单源]`

| API | 签名/用法 | 说明 |
|---|---|---|
| `getContext()` | `SillyTavern.getContext()` | 所有扩展 API 的主入口 |
| `context.chat` | `ChatMessage[]` | 当前对话消息（可变） |
| `context.chatMetadata` | `object` | 绑定当前 chat 的持久元数据 |
| `context.extensionSettings` | `object` | 全局扩展设置 |
| `saveSettingsDebounced()` | `() => void` | 持久化 extensionSettings |
| `saveMetadata()` | `async () => void` | 持久化 chatMetadata |
| `Popup.show.confirm(title, msg)` | `async → bool` | 确认弹窗 |
| `Popup.show.input(title, prompt, default)` | `async → string` | 文本输入弹窗 |
| `new Popup(html, POPUP_TYPE.TEXT, '', opts)` | 自定义弹窗 | `opts: {wide, okButton, customButtons, customInputs}` |
| `loader.show({message, blocking, onStop})` | `{hide()}` | 加载遮罩 |

### 4.8 世界书条目位置类型参考

**核心结论**

来源：JS-Slash-Runner `src/function/worldbook.ts` + `@types/function/worldbook.d.ts`（ST 内部数字枚举与 API 字符串名双向映射均已回源确认）｜置信度：high `[运行时源码]`

> ⚠️ `before_char_defs`/`after_char_defs`/`in_chat` 均为错误名称，不存在于 JS-Slash-Runner worldbook.ts 中。

| `position.type` 值 | ST 内部枚举 | 含义 |
|---|---|---|
| `before_character_definition` | 0 | 角色定义之前 |
| `after_character_definition` | 1 | 角色定义之后 |
| `before_example_messages` | 5 | 示例对话之前 |
| `after_example_messages` | 6 | 示例对话之后 |
| `before_author_note` | 2 | Author's Note 之前 |
| `after_author_note` | 3 | Author's Note 之后 |
| `at_depth` | 4 | 嵌入 chat 上下文指定深度，`depth:0` = 最新楼 |
| `outlet` | 7 | 手动用 `{{outlet::Name}}` 宏放置 |

**`order` 字段**：同一 `position.type` 下，数字大 → 更靠近 context 末尾（更接近最新消息）。

---

## 5. 完整实现范式

### 5.1 状态栏 iframe 初始化完整流程

**核心结论**：生产卡 `init()` 函数的完整流程，涵盖去重、设置、Mvu 就绪等待、首次刷新、事件监听。

```javascript
// <script type="module"> 内
async function init() {
  // 1. 去重（同一楼层多 iframe 时保留一个；详见范式 5.6）
  normalizeDuplicateHudInMessage();
  setTimeout(normalizeDuplicateHudInMessage, 120);
  setTimeout(normalizeDuplicateHudInMessage, 500);

  // 2. 应用本地设置
  applyHudSettings(DEFAULT_SETTINGS);
  bindHudSettingsControls();
  refreshPersistentHudSettings().then(function () { schedulePopulate(20, true); });

  // 3. 等待 Mvu 就绪（关键：必须 await，否则后续 Mvu.events.xxx 调用失败）
  await waitGlobalInitialized('Mvu');

  // 4. 首次刷新（二次兜底应对 TavernHelper 延迟注入变量）
  schedulePopulate(40, true);
  setTimeout(function() { schedulePopulate(40, true); }, 600);

  // 5. 挂事件监听（核心刷新挂点）
  eventOn(Mvu.events.VARIABLE_UPDATE_ENDED, function() { schedulePopulate(80); });
  eventOn(Mvu.events.VARIABLE_INITIALIZED, function() { schedulePopulate(80); });

  // 6. 额外事件（窗口 resize、媒体库更新等）
  $(window).on('resize', updateAllWideThresholdGrids);
  $(window).on('crossed-zone-media-library-updated', function() {
    lastPopulateSignature = '';
    schedulePopulate(40, true);
  });
}

init();
```

**关键次序**：`waitGlobalInitialized('Mvu')` **必须在 `eventOn(Mvu.events.xxx)` 之前 await**。去重在 init 开始时多次调用（0ms / 120ms / 500ms），覆盖不同渲染时序。

来源：`status_bar_regex_beauty_r5.html` L4830-4848 ｜ 置信度：high `[运行时源码]`

### 5.2 变量读取三级降级链

**核心结论**

```javascript
function readCurrentHudVariables() {
  var current = null;

  // 第一优先：TavernHelper 原生 API（最快）
  try {
    current = getVariables({ type: 'message' }) || null;
    if (hasStatData(current)) return current;
  } catch (e) {}

  // 第二优先：指定 message_id 的 Mvu 接口（更稳定）
  try {
    current = Mvu.getMvuData({
      type: 'message',
      message_id: getCurrentMessageId()
    }) || null;
    if (hasStatData(current)) return current;
  } catch (e) {}

  // 最终降级：返回上次有效数据或空壳
  return lastGoodHudData || { stat_data: {} };
}

function hasStatData(data) {
  return !!(data && data.stat_data && typeof data.stat_data === 'object'
    && Object.keys(data.stat_data).length > 0);
}
```

来源：`status_bar_regex_beauty_r5.html` L4671-4683 ｜ 置信度：high `[运行时源码]`

### 5.3 防抖刷新调度器（schedulePopulate）

**核心结论**：`schedulePopulate` + 数据签名机制防止短时间内频繁重渲染。

```javascript
var lastPopulateSignature = '';
var populateTimer = null;
var populateRaf = null;

function schedulePopulate(delay, force) {
  if (populateTimer) clearTimeout(populateTimer);
  var raf = window.requestAnimationFrame || function(cb) { return setTimeout(cb, 16); };
  var caf = window.cancelAnimationFrame || clearTimeout;
  populateTimer = setTimeout(function() {
    if (populateRaf) caf(populateRaf);
    populateRaf = raf(function() {
      populateRaf = null;
      populate(null, { force: !!force });
      updateAllWideThresholdGrids();
    });
  }, delay || 50);
}

function populate(all, options) {
  options = options || {};
  all = all && hasStatData(all) ? all : readCurrentHudVariables();
  if (hasStatData(all)) lastGoodHudData = cloneHudValue(all);

  // 签名：stat_data + media + settings 三者联合签名
  var sig = JSON.stringify({ stat: statRoot(all), media: getMediaData(), settings: settings });
  if (!options.force && sig === lastPopulateSignature) return;  // 签名未变则跳过
  lastPopulateSignature = sig;

  // 活跃 tab 立即同步渲染，其余 tabs requestIdleCallback 延迟渲染
  var activeId = activeTabId();
  renderTabIfNeeded(activeId, all, sig, true);
  renderAllTabsDeferred(all, activeId);
}
```

**注意**：用户触发操作后若数据已更新但签名缓存未变，须传 `{ force: true }` 强制刷新，否则 UI 不刷新（见避坑 7.11）。

来源：`status_bar_regex_beauty_r5.html` L4793-4827 ｜ 置信度：high `[运行时源码]`

### 5.4 DOM 绑定范式（setCardValue / setAlwaysCardValue）

**核心结论**：两个语义区分的辅助函数管理卡片显示/隐藏，自动处理空值。

```javascript
// 空值时隐藏整个卡片（可选字段用此函数）
function setCardValue(selector, obj, path, def) {
  var root = statRoot(obj);
  var p = String(path || '');
  if (p.indexOf('stat_data.') === 0) p = p.slice(10);
  var raw = _.get(root, p);
  var $value = $(selector);
  var $card = $value.closest('.card, .card-full');
  if (!shouldRenderValue(raw)) { $card.hide(); return false; }
  $card.show().toggleClass('is-empty', isEmptyValue(raw));
  $value.text(displayValue(raw, def || '--'));
  return true;
}

// 永远显示（即使为空），用于关键字段
function setAlwaysCardValue(selector, obj, path, def) {
  // 结构同上，但无条件 $card.show()
}

// 使用示例
function renderEnv(all) {
  setAlwaysCardValue('#环境-日期', all, 'stat_data.环境.日期', '--');   // 关键字段，永远显示
  setAlwaysCardValue('#环境-时间', all, 'stat_data.环境.时间', '--');
  setCardValue('#环境-节日', all, 'stat_data.环境.近期节日', '');        // 可选字段，空时隐藏
}
```

来源：`status_bar_regex_beauty_r5.html` L2332-2356 ｜ 置信度：high `[运行时源码]`

### 5.5 Tab 分区懒渲染范式

**核心结论**：活跃 Tab 立即同步渲染，其余 Tab 用 `requestIdleCallback` 分批延迟渲染（每批最多 2 个 Tab），避免阻塞主线程。

```javascript
var TAB_RENDERERS = window.CZ_STATUS_TAB_RENDERERS || {
  'tab-env': renderEnv,
  'tab-user': renderUser,
  // ... 其余 tab
};

function renderAllTabsDeferred(all, activeId) {
  cancelDeferredTabRender();
  var ids = Object.keys(TAB_RENDERERS).filter(function(id) { return id !== activeId; });
  var i = 0;
  function nextBatch() {
    var count = 0;
    while (i < ids.length && count < 2) {
      renderTabIfNeeded(ids[i++], all, sig, false);
      count++;
    }
    if (i < ids.length) scheduleDeferredTabRender(nextBatch);
  }
  // requestIdleCallback({timeout:180}) 或降级 setTimeout(45)
  scheduleDeferredTabRender(nextBatch);
}
```

**`requestIdleCallback` 兜底**：不支持时降级 `setTimeout(45)`，须显式做特性检测（见避坑 7.11）。

来源：`status_bar_regex_beauty_r5.html` L4736-4756 ｜ 置信度：high `[运行时源码]`

### 5.6 多 iframe 去重机制

**核心结论**：每次楼层重渲染（swipe、edit）都会注入新 iframe。去重机制**优先保留当前 iframe 自身**，只有当自身不在 hudFrames 列表中时才降级为保留最后一个，并移除其余。

```javascript
function normalizeDuplicateHudInMessage() {
  var frame = window.frameElement;
  var message = frame && frame.closest ? frame.closest('.mes') : null;
  if (!frame || !message) return 0;

  var frames = Array.prototype.slice.call(message.querySelectorAll('iframe'));
  var hudFrames = frames.filter(function(item) {
    var doc = item.contentDocument;
    return !!(doc && doc.querySelector('.hud-container .hud-title'));
  });

  if (hudFrames.length <= 1) return 0;

  var keep = frame;  // 优先保留自身
  if (hudFrames.indexOf(keep) < 0) keep = hudFrames[hudFrames.length - 1];
  hudFrames.forEach(function(item) {
    if (item !== keep) item.remove();
  });
}

// 调用时序：init 开始时多次调用，覆盖不同渲染完成时序
normalizeDuplicateHudInMessage();
setTimeout(normalizeDuplicateHudInMessage, 120);
setTimeout(normalizeDuplicateHudInMessage, 500);
```

来源：`status_bar_regex_beauty_r5.html` L1930-1944 ｜ 置信度：high `[运行时源码]`

### 5.7 控制中心面板创建与生命周期管理

**核心结论**（综合 v2.5.0 + v2.9.9 最佳实践）

```javascript
(() => {
  const PANEL_ID    = 'my-card-panel';
  const STYLE_ID    = 'my-card-style';
  const WAND_ID     = 'my-card-wand';
  const STORAGE_KEY = 'my-card-settings-v1';
  const HUD_EVENT   = 'my-card-settings-updated';
  const DEFAULT     = { featureA: true, someValue: 50 };

  function hostWindow() {
    try {
      if (window.parent && window.parent !== window && window.parent.document) return window.parent;
    } catch (_) {}
    return window;
  }
  function hostDocument() {
    try { return hostWindow().document || document; } catch (_) { return document; }
  }

  // 防重入销毁（避免事件监听器叠加）
  if (window.MyCardControlCenter?.destroy) {
    try { window.MyCardControlCenter.destroy(); } catch (_) {}
  }

  const disposers = [];
  let panelOpen = false;
  let settings = loadSettings();

  function loadSettings() {
    try {
      return { ...DEFAULT, ...JSON.parse(localStorage.getItem(STORAGE_KEY) || '{}') };
    } catch (_) { return { ...DEFAULT }; }
  }

  function saveSettings(partial) {
    settings = { ...settings, ...(partial || {}) };
    try { localStorage.setItem(STORAGE_KEY, JSON.stringify(settings)); } catch (_) {}
    broadcastSettings();
    renderPanel();
    return { ...settings };
  }

  function broadcastSettings() {
    const detail = { settings: { ...settings }, source: 'my-card' };
    [window, hostWindow()].forEach(target => {
      try { target.dispatchEvent(new CustomEvent(HUD_EVENT, { detail })); } catch (_) {}
    });
  }

  function escapeHtml(s) {
    return String(s || '').replace(/&/g, '&amp;').replace(/</g, '&lt;').replace(/>/g, '&gt;');
  }

  function ensurePanel() {
    const doc = hostDocument();
    // 注入样式（ID 选择器不受 custom- 前缀影响，推荐）
    if (!doc.getElementById(STYLE_ID)) {
      const style = doc.createElement('style');
      style.id = STYLE_ID;
      style.textContent =
        '#my-card-panel { position:fixed; z-index:2147483000; right:22px; top:82px; width:420px;' +
        '  background:#1a1a2e; border:1px solid #444; border-radius:8px; color:#eee; }' +
        '#my-card-panel[hidden] { display:none!important }';
      (doc.head || doc.documentElement).appendChild(style);
    }
    let panel = doc.getElementById(PANEL_ID);
    if (!panel) {
      panel = doc.createElement('div');
      panel.id = PANEL_ID;
      panel.hidden = true;
      doc.body.appendChild(panel);
      // 事件委托——单个监听器管理全部交互
      panel.addEventListener('click', event => {
        const action = event.target?.getAttribute?.('data-action');
        const key = event.target?.getAttribute?.('data-toggle');
        if (action === 'close') togglePanel(false);
        if (key) saveSettings({ [key]: !settings[key] });
      });
    }
    return panel;
  }

  function renderPanel() {
    const panel = ensurePanel();
    // 分区 safe() 兜底：任一分区抛错只影响该区，不拖累整个面板
    const safe = (label, fn) => {
      try { return fn(); } catch (e) {
        return '<div class="muted">【' + label + '】渲染失败：' + escapeHtml(e?.message || String(e)) + '</div>';
      }
    };
    panel.innerHTML =
      '<div class="head"><div class="title">控制中心</div>' +
      '<button data-action="close">关闭</button></div>' +
      '<div class="grid">' +
      safe('分区A', renderSectionA) +
      safe('分区B', renderSectionB) +
      '</div>';
    panel.hidden = !panelOpen;  // ⚠️ 必须在 innerHTML 之后单独设置
  }

  function togglePanel(force) {
    panelOpen = typeof force === 'boolean' ? force : !panelOpen;
    renderPanel();
  }

  function ensureWandEntry() {
    const doc = hostDocument();
    try {
      const host = doc.querySelector('#extensionsMenu') ||
                   doc.querySelector('.extensionsMenu') || doc.body;
      let wrap = doc.getElementById(WAND_ID);
      if (!wrap) {
        wrap = doc.createElement('span');
        wrap.id = WAND_ID;
        const button = doc.createElement('button');
        button.textContent = '我的控制中心';
        button.addEventListener('click', e => { e.preventDefault(); e.stopPropagation(); togglePanel(); });
        wrap.appendChild(button);
      }
      if (wrap.parentNode !== host) host.appendChild(wrap);
      return true;
    } catch (_) { return false; }
  }

  function destroy() {
    while (disposers.length) { try { disposers.pop()?.(); } catch (_) {} }
    try { hostDocument().getElementById(PANEL_ID)?.remove(); } catch (_) {}
    try { hostDocument().getElementById(WAND_ID)?.remove(); } catch (_) {}
    try { hostDocument().getElementById(STYLE_ID)?.remove(); } catch (_) {}
    try { if (window.MyCardControlCenter === api) delete window.MyCardControlCenter; } catch (_) {}
    try {
      const host = hostWindow();
      if (host && host !== window && host.MyCardControlCenter === api)
        delete host.MyCardControlCenter;
    } catch (_) {}
  }

  function bindEvents() {
    const eventOnHost = window.eventOn || hostWindow().eventOn;
    const events = window.tavern_events || hostWindow().tavern_events || {};
    if (typeof eventOnHost !== 'function') return;
    // CHAT_CHANGED 后延时 250ms 重建魔杖入口（等 ST 完成 UI 重建）
    [events.CHAT_CHANGED, events.CHARACTER_MESSAGE_RENDERED].filter(Boolean).forEach(name => {
      try {
        const d = eventOnHost(name, () => setTimeout(ensureWandEntry, 250));
        if (d?.stop) disposers.push(() => d.stop());
      } catch (_) {}
    });
  }

  const api = { version: '1.0.0', getSettings: () => ({ ...settings }), saveSettings, togglePanel, destroy };
  window.MyCardControlCenter = api;
  try { const host = hostWindow(); if (host && host !== window) host.MyCardControlCenter = api; } catch (_) {}

  ensurePanel();
  ensureWandEntry();
  setTimeout(ensureWandEntry, 1000);
  bindEvents();
})();
```

来源：`control_center.js` v2.9.9（综合提取）｜ 置信度：high `[运行时源码]`

### 5.8 世界书事务写入范式

**核心结论**

```javascript
// upsert 逻辑：按 entry.name 去重，保留旧 uid
async function writeEntriesToWorldbook(entries) {
  const apiHost = worldbookApiHost(); // 见 §4.3 API 发现链
  if (!apiHost) throw new Error('Tavern Helper 世界书 API 不可用');

  // 定位角色卡绑定世界书（优先 primary，降级按名称匹配）
  let name;
  try {
    const c = apiHost?.getCharWorldbookNames?.('current');
    if (c?.primary) name = c.primary;
  } catch (_) {}
  if (!name) {
    const all = apiHost?.getWorldbookNames?.();
    if (Array.isArray(all)) {
      name = all.find(n => /我的卡名/.test(String(n)));
    }
  }
  if (!name) throw new Error('未能定位角色卡绑定的世界书');

  await apiHost.updateWorldbookWith(name, worldbook => {
    const existing = Array.isArray(worldbook) ? worldbook : [];
    const isManagedEntry = e => e?.extra?.source === 'my-card';
    const preserved = new Map(
      existing.filter(e => isManagedEntry(e)).map(e => [e.name, e])
    );
    const base = existing.filter(e => !isManagedEntry(e));
    return [
      ...base,
      ...entries.map(e =>
        preserved.has(e.name) ? { ...e, uid: preserved.get(e.name).uid } : e
      ),
    ];
  }, { render: 'debounced' });
}
```

来源：`control_center.js` v2.9.9 第 466-593 行 ｜ 置信度：high `[运行时源码]`

### 5.9 MVU 变量读写三段式范式（v2.9.9）

**核心结论**（v2.9.9 生产验证标准写法，取代 v2.5.0 的 injectPrompts 方案）

```javascript
// 读取当前楼变量
function getCurrentMvuData() {
  const Mvu = window.Mvu || hostWindow().Mvu;
  if (!Mvu?.getMvuData) throw new Error('MVU 未就绪');
  return Mvu.getMvuData({ type: 'message', message_id: 'latest' });
}

// 三段式写回：generateRaw → parseMessage → replaceMvuData
async function writeVariableFix(instruction) {
  const helper = window.TavernHelper || hostWindow().TavernHelper;
  const generateRaw = helper?.generateRaw || window.generateRaw || hostWindow().generateRaw;
  if (typeof generateRaw !== 'function') throw new Error('generateRaw 不可用');

  const oldData = getCurrentMvuData();
  const raw = String(await generateRaw({
    should_silence: true,
    max_chat_history: 0,
    ordered_prompts: [
      { role: 'system', content: '只输出一个 <UpdateVariable> 块（内含 JSONPatch）。' },
      { role: 'user', content: '当前变量：' + JSON.stringify(oldData) + '\n修正：' + instruction },
    ],
  }) || '').trim();

  if (!/<UpdateVariable/i.test(raw)) throw new Error('结果未含 <UpdateVariable> 块');

  const Mvu = window.Mvu || hostWindow().Mvu;
  const nextData = await Mvu.parseMessage(raw, oldData);
  if (!nextData) throw new Error('未能解析变量更新');

  await Mvu.replaceMvuData(nextData, { type: 'message', message_id: 'latest' });
}
```

来源：`control_center.js` v2.9.9 第 824-963 行 ｜ 置信度（generateRaw / 三段式流程）：high `[运行时源码]`；（parseMessage / replaceMvuData 接口）：medium·单源孤证（见悬案 F）

### 5.10 开局页挂载摘要（完整方案转 C10）

历史 v2.9.9 已验证“`first_mes` 短标记 + 本地脚本拉远程页面 + binder 绑定”可减小卡体积，也能避开静态消息渲染阶段的 class 改名。但“拿到 HTML 后直接 `innerHTML`”只能视为历史最小样本，不是当前安全生产范式。

当前生产要求：

```text
短标记
→ 本地可见 loading/error/retry
→ 多源 fetch + 超时
→ SHA-256 / 页面协议 / 必需结构校验
→ 净化并删除远程 script/iframe/object/embed/base/link/meta/form
→ 注入声明式 HTML
→ 由本地受信 binder 绑定
→ 切聊天/Swipe/卸载时 abort + destroy
```

开局页的草稿隔离、预览、身份/世界配置事务、正常发送链与 C5 同层前端边界，统一见 **《C10_开局页与自定义开局.md》**。本册只保留控制中心如何发现 mount、调用 binder 和管理生命周期的接口点。

来源：星月 v2.9.9 历史实现 + `rolecard-diy-workshop/runtime/xingyue/3.4.9/control-center.js` 当前生产链 ｜ 置信度：high `[运行时源码]`

### 5.11 custom- 前缀对策——MutationObserver 动态补齐

**核心结论**：检测到 `custom-` 前缀存在后，用 MutationObserver 实时给新增节点补全 `custom-` 版本类名。

```javascript
function applyCustomPrefix(root) {
  const XY_PX = /(^|\s)custom-xy-/.test(root.className || '') ? 'custom-' : '';
  if (!XY_PX) return; // 离线预览模式，无需处理

  const prefixEl = (el) => {
    if (!el || el.nodeType !== 1 || !el.className) return;
    el.className.split(/\s+/).forEach(token => {
      if (!token || token.indexOf(XY_PX) === 0) return;
      const prefixed = XY_PX + token;
      if (!el.classList.contains(prefixed)) el.classList.add(prefixed);
    });
  };
  const prefixTree = (el) => {
    prefixEl(el);
    if (el.querySelectorAll) el.querySelectorAll('[class]').forEach(prefixEl);
  };

  prefixTree(root);  // 处理现有节点

  if (!root.__xyPrefixObs && typeof MutationObserver === 'function') {
    root.__xyPrefixObs = new MutationObserver(muts => {
      muts.forEach(m => {
        if (m.type === 'attributes') { prefixEl(m.target); return; }
        m.addedNodes.forEach(n => { if (n.nodeType === 1) prefixTree(n); });
      });
    });
    root.__xyPrefixObs.observe(root, {
      childList: true, subtree: true,
      attributes: true, attributeFilter: ['class'],
    });
  }
}
```

来源：`control_center.js` v2.9.9 第 1758-1789 行 ｜ 置信度：high `[运行时源码]`

### 5.12 Prompt 注入范式（GENERATION 事件钩子，v2.5.0 时代）

> **注意**：v2.9.9 已改用 MVU 三段式（§5.9），此范式仅作历史参考 / 兼容旧卡。

```javascript
const injectionIds = ['my-prompt-news', 'my-prompt-radar'];

function injectGenerationPrompts() {
  if (typeof uninjectPrompts === 'function') uninjectPrompts(injectionIds);
  const prompts = [];
  if (settings.newsPolicyEnabled) prompts.push({
    id: 'my-prompt-news',
    position: 'in_chat',
    depth: 0,
    role: 'system',
    content: '...策略文本...',
    should_scan: false,
  });
  if (prompts.length && typeof injectPrompts === 'function') {
    injectPrompts(prompts, { once: true });
  }
}

function bindGenerationEvents() {
  const on = typeof eventMakeFirst === 'function'
    ? eventMakeFirst
    : (typeof eventOn === 'function' ? eventOn : null);
  const events = window.tavern_events || hostWindow().tavern_events || {};
  if (!on) return;
  [events.GENERATION_AFTER_COMMANDS, events.GENERATE_BEFORE_COMBINE_PROMPTS, events.CHAT_COMPLETION_PROMPT_READY]
    .filter(Boolean).forEach(name => {
      const disposer = on(name, () => injectGenerationPrompts());
      if (disposer) disposers.push(disposer);
    });
}
```

来源：`control_center.js` v2.5.0 第 217-341 行 ｜ 置信度：high `[运行时源码]`

---

## 6. CSS 布局与主题变量

### 6.1 主容器与分区结构

**核心结论**（星月 2.5.0 生产 CSS）

```css
/* 主容器：切角设计，固定深色不跟随 ST 主题 */
.hud-container {
  width: 100%;
  max-width: var(--hud-base-width);
  margin: 0 auto;
}

/* 数据卡片 */
.card { /* 隐藏时 display:none */ }
.card-full { width: 100%; }
.card-label, .card-value { /* 标签 + 值双列布局 */ }

/* 响应式网格 */
.stat-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(120px, 1fr));
}
.card-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(180px, 1fr));
}

/* 移动端适配 */
@media (max-width: 480px) {
  .stat-grid { grid-template-columns: repeat(2, 1fr); }
}
```

**注意**：生产 CSS 放在静态 HTML 中（受 custom- 前缀处理），控制中心面板 CSS 通过 JS `createElement('style')` 注入（不受前缀影响）。

来源：`status_bar_regex_beauty_r5.html` L1-100 ｜ 置信度：high `[运行时源码]`

### 6.2 CSS 变量系统

**核心结论**：变量系统分两组——`--theme-*` 随 Tab 切换变色，`--hud-*` 全局固定色。

| 变量名 | 含义 | 变化时机 |
|--------|------|---------|
| `--theme-primary` | 当前 Tab 主色 | Tab 切换时更新 |
| `--theme-glow` | 主色辉光派生色 | Tab 切换时更新 |
| `--theme-accent` | 主色强调派生色 | Tab 切换时更新 |
| `--theme-soft` | 主色柔化派生色 | Tab 切换时更新 |
| `--hud-bg-1` | 容器背景主色（固定深色） | 不变 |
| `--hud-bg-2` | 容器背景次色 | 不变 |
| `--hud-text` | 主文字色 | 不变 |
| `--hud-mute` | 次要/静默文字色 | 不变 |

设计原则：`--hud-*` 不跟随 ST 全局主题，保持 HUD 视觉独立性；`--theme-*` 给 Tab 提供区分色，增强分区辨识度。

来源：`status_bar_regex_beauty_r5.html` CSS 变量定义段 ｜ 置信度：high `[运行时源码]`

---

## 7. 高频避坑

### 7.1 custom- 前缀导致动态注入内容丢样式

**来源**：`public/scripts/chats.js` DOMPurify 钩子直读 + `control_center.js` v2.9.9 ｜ 置信度：high `[运行时源码]`

**机制（已回源 `public/scripts/chats.js` `addDOMPurifyHooks` / `encodeStyleTags` / `decodeStyleTags` 确认）**：
- **`class` 属性**：ST DOMPurify 钩子对每个 class token 加 `custom-` 前缀（`fa-`、`note-` 开头及 `monospace` 豁免）。
- **`<style>` 标签**：被替换为 `<custom-style>编码内容</custom-style>`，CSS 选择器中的 class 名加 `custom-` 前缀；**`id` 选择器不受前缀处理**。
- **`style=""` 属性**：未观察到被重命名为 `custom-style=""`；是否保留受 ST/DOMPurify 版本与配置影响，不能写成全版本“必然剥除”。
- **JS 注入**：不经过消息静态 class 前缀管线，但仍必须区分受信本地模板与不可信远程 HTML。

**对策（按可靠性排序）**：
1. **受信本地控制面板**：可由本地脚本注入受审模板，CSS 写裸类名；禁止把用户/模型文本直接拼入 HTML。
2. **远程开局页面**：必须走 C10 的“完整性验证 → DOM 净化 → 结构校验 → 注入 → 本地 binder”，不能直接 `innerHTML = remoteHtml`。
3. **消息静态内容**：使用已核 `<style>`/class 规则，或 MutationObserver 动态补齐（见范式 5.11）。
4. **宿主面板兜底**：使用稳定 ID 限定作用域（`#my-panel .xy-row { ... }`）。

### 7.2 整面板空白（分区渲染抛错未隔离）

**来源**：`control_center.js` v2.5.0 vs v2.9.9 对比 ｜ 置信度：high `[运行时源码]`

v2.5.0 的 `renderPanel` 是一次性 `innerHTML` 拼接，任一分区函数调用抛错则整个面板不显示。

**对策**：用 `safe(label, fn)` 隔离每个分区；`panel.hidden` 赋值**必须在 `innerHTML` 设置之后单独执行**（不能放在 `safe` 内）。

```javascript
const safe = (label, fn) => {
  try { return fn(); } catch (e) { return '【' + label + '】渲染失败：' + e.message; }
};
panel.innerHTML = safe('分区A', renderA) + safe('分区B', renderB);
panel.hidden = !panelOpen;  // 必须在 innerHTML 之后
```

### 7.3 window.parent 访问失败导致 API 不可用

详见《C1_前端基础-TavernHelper与iframe.md》§14 避坑 3。所有 `window.parent` 访问包在 `try/catch` 里，失败回落到 `window` 自身。

### 7.4 CHAT_CHANGED 后魔杖入口丢失

**来源**：`control_center.js` v2.9.9 第 3172-3183 行 ｜ 置信度：high `[运行时源码]`

ST 切换对话时会重新渲染顶部 UI，之前挂到 `#extensionsMenu` 的按钮被销毁。**对策**：订阅 `CHAT_CHANGED` 事件，延时 250ms 重新调用 `ensureWandEntry()`（见范式 5.7）。

### 7.5 first_mes 塞大 HTML 引发上下文爆炸

**来源**：`control_center.js` v2.9.9（注释明确标注为历史 bug）｜ 置信度：high `[运行时源码]`

`first_mes` 内容会被发给 LLM。若塞入完整开局页 HTML（可能达 250KB+），导致上下文爆炸 + 页面卡顿。**对策**：`first_mes` 只放短标记 `<div data-xy-opening-remote></div>`，控制中心 fetch 远程完整 HTML 注入（见范式 5.10）。

### 7.6 事件监听器不清理导致内存泄漏和重复触发

详见《C1_前端基础-TavernHelper与iframe.md》§14 避坑 10。防御步骤：① 初始化时 destroy 旧实例；② disposers 模式管理所有 eventOn 返回值。

### 7.7 世界书写入用错 API（旧方法另建聊天世界书）

**来源**：`control_center.js` v2.9.9（注释明确标注为历史 bug）｜ 置信度：high `[运行时源码]`

旧方法 `getOrCreateChatWorldbook` 会另建"聊天世界书"而非写入角色卡绑定的世界书，且会撞名报错。**对策**：优先使用 `TavernHelper.getCharWorldbookNames('current').primary`（见范式 5.8）。

### 7.8 开局页 script 标签不执行

**来源**：`control_center.js` v2.9.9（注释明确标注）｜ 置信度：high `[运行时源码]`

ST 渲染消息楼层时不执行楼层内嵌的 `<script>` 标签。依赖 `<script>` 的初始化逻辑永不执行。**对策**：所有逻辑移入控制中心 JS，用 MutationObserver 或事件驱动探测开局页 DOM 节点就绪后再操作（见范式 5.10 `bindOpeningPage`）。

### 7.9 waitGlobalInitialized('Mvu') 必须 await

**来源**：`mvu_zod_cn.js` L27-34 + `status_bar_regex_beauty_r5.html` L4830 ｜ 置信度：high `[运行时源码]`

`eventOn(Mvu.events.xxx, ...)` 等所有 Mvu API 调用，必须在 `Mvu` 对象可用后才能执行。漏 `await` 导致 `Mvu is not defined`（见范式 5.1）。

### 7.10 getCurrentMessageId() 仅限消息楼层 iframe

**来源**：官方文档明述 + `status_bar_regex_beauty_r5.html` L2985 ｜ 置信度：high `[文档·多源]` + `[运行时源码]`

在全局脚本 iframe 或非 TavernHelper 管理的 iframe 中调用会抛出错误。所有调用处须用 `try/catch` 包裹。

### 7.11 数据签名防止无效重渲染

**来源**：`status_bar_regex_beauty_r5.html` L4817-4822 ｜ 置信度：high `[运行时源码]`

`populate()` 用联合签名（stat_data + media + settings）跳过无效渲染。用户操作后若数据更新但签名缓存未更新，须传 `{ force: true }` 强制刷新，否则 UI 不刷新。

### 7.12 iframe 重复问题

每次楼层重渲染（swipe、edit）都可能注入新 iframe。必须在 init 时多次调用 `normalizeDuplicateHudInMessage()`（见范式 5.6 和 5.1）。

---

## 8. 悬案（未决项 + 真机验证路径）

| 编号 | 问题 | 置信度 | 真机验证路径 |
|---|---|---|---|
| A | `display_data` 字段的来源（本地生产卡未发现此字段，可能是某版本旧称） | low | `window.Mvu.getMvuData({ type: 'message', message_id: -1 })` 查看顶层键 |
| B | `getVariables({type:'message'})` 无 message_id 时的精确行为（返回最新楼还是 iframe 所在楼） | medium·推导未验证 | 对比 `getVariables({ type: 'message' })` 与 `getVariables({ type: 'message', message_id: getCurrentMessageId() })` 结果 |
| C | 正则 Find Regex 的标准变量块 pattern（可能存储在 registry.json 或打包的世界书中） | medium·推导未验证 | ST → 角色卡 → 正则脚本列表 → 找 `status_bar` 相关规则 → 查看 Find Regex 字段 |
| D | `waitGlobalInitialized` 的官方实现来源（降级轮询可用已验证；官方实现在 dist 压缩包，未直读） | medium·推导未验证 | F12 控制台 `waitGlobalInitialized.toString()` |
| E | `eventMakeFirst` vs `eventOn` 多个扩展并发时的真实执行顺序 | medium·推导未验证 | F12 控制台同时注册两个 eventMakeFirst，观察回调执行顺序 |
| F | `Mvu.parseMessage` / `Mvu.replaceMvuData` 官方接口文档与完整 API 列表 | high（存在）/ low（文档） | `console.log(Object.keys(window.Mvu))` 查看完整 API；各方法 `.toString()` 查看签名 |

---

## 9. 关键本地文件路径

| 文件 | 路径 | 关键内容 |
|---|---|---|
| 控制中心（v2.9.9） | `ST开发指南DB/本地证据/星月历史/2.9.9/components/control_center.js` | 3272 行，最高可信；双窗口穿透、世界书写入、MVU 三段式、开局页注入、custom- 前缀处理 |
| 控制中心（v2.5.0） | `C:/Users/Administrator/OneDrive/ST-/角色卡工作区/星月/星月 2.5.0/components/control_center.js` | 376 行；injectPrompts / GENERATION 范式、事件 disposers 模式 |
| 状态栏 r5 | `C:/Users/Administrator/OneDrive/ST-/角色卡工作区/星月/星月 2.5.0/components/status_bar_regex_beauty_r5.html` | 262KB；完整 HUD 实现，init 流程、三级降级链、schedulePopulate、Tab 懒渲染、去重机制 |
| 状态栏基础版 | `C:/Users/Administrator/OneDrive/ST-/角色卡工作区/星月/星月 2.5.0/components/status_bar_regex.html` | 简化版状态栏实现 |
| MVU 上下文模板 | `C:/Users/Administrator/OneDrive/ST-/角色卡工作区/星月/星月 2.5.0/components/mvu_context.ejs` | L132-L186：stat_data 字段结构定义 |
| MVU Zod 初始化 | `C:/Users/Administrator/OneDrive/ST-/角色卡工作区/星月/星月 2.5.0/components/mvu_zod_cn.js` | L27-34：waitGlobalInitialized 降级轮询实现 |
| Mvu 类型定义 | https://github.com/N0VI028/JS-Slash-Runner/blob/main/@types/iframe/exported.mvu.d.ts | events 枚举字段及 initiailized 拼写 bug |
| 事件类型定义 | https://github.com/N0VI028/JS-Slash-Runner/blob/main/@types/iframe/event.d.ts | IframeEventType + TavernEventType 完整枚举 |
| ST chats.js | `public/scripts/chats.js`（ST v1.14.0 本体） | DOMPurify 钩子，custom- 前缀处理机制 |
| ST events.js | `public/scripts/events.js` L1-95（ST v1.14.0 本体） | 所有 TavernEventType 枚举字段字符串值 |
