# 音频媒体与多模态参考手册

> 文档版本：v1.0.0 ｜ 最后更新：2026-07-14

## SillyTavern TTS · 表情立绘 · 图片生成 · 图像输入 · STT · Dynamic Audio · 本地媒体库

> **适用对象**：基于 SillyTavern v1.14.0+ 的音频、语音、表情、图像多模态能力建设参考；兼适卡内 JS 脚本与 STscript 开发者。

---

## 〈置信度分级体系〉

每条结论采用 **置信度等级 + 依据类型** 双轴标注。

**收集日期统一声明**：本手册全部条目收集于 **2026-06-28**。回源核实基于本地 SillyTavern **v1.14.0** + 本地成熟卡（星月 2.5.0 / 交错宙域 2.6.0）生产源码；在线文档来源均为 docs.sillytavern.app 与 deepwiki.com 原始页面（具体 URL 见末尾「来源汇总」）。

### 置信度三级

| 级别 | 含义 | 判定标准 |
|------|------|----------|
| **high** | 已坐实，可直接施工 | 有运行时证据（真机实测 / 读到运行时实现源码 / 生产卡验证）；非 beta 未合并特性；结论不含跨步推导；不在悬案清单 |
| **medium** | 待验证，施工前须实测 | 强制二选一子标签：`medium·单源孤证`（单一来源、无第二源、无实测）/ `medium·推导未验证`（逻辑推导成立但未跑通，须随附真机验证路径） |
| **low** | 孤证无源，禁止据此施工 | 零来源猜测 / 纯历史快照（须标年份 + 窄缩适用范围） |

### 依据类型标签（与置信度正交，强制标注）

`[运行时源码]` `[真机]` `[文档·多源]` `[文档·单源]` `[社区]` `[推断]`

### 两条铁规

1. **仅由 `[推断]` 支撑的「运行时行为 / 可用性」结论，最高只能 `medium·推导未验证`**——除非另有 `[运行时源码]` 或 `[真机]` 佐证才升 high。
2. **节级标注 = 节内条目下确界**——禁止整节标 high 却内含悬案条目；冲突即拆节。

---

## 目录

1. [TTS（文字转语音）系统](#1-tts文字转语音系统)
   - 1.1 [机制总览与双队列架构](#11-机制总览与双队列架构)
   - 1.2 [支持的 TTS 提供商（14+ 个）](#12-支持的-tts-提供商14-个)
   - 1.3 [Provider 接口规范与 Manager 公开函数](#13-provider-接口规范与-manager-公开函数)
   - 1.4 [TTS 事件系统](#14-tts-事件系统)
   - 1.5 [STscript 命令](#15-stscript-命令)
   - 1.6 [关键配置选项](#16-关键配置选项)
   - 1.7 [TTS 与立绘同步](#17-tts-与立绘同步)
2. [表情立绘（Expression/Sprite）系统](#2-表情立绘expressionsprite系统)
   - 2.1 [机制总览](#21-机制总览)
   - 2.2 [28 个默认情绪分类（完整列表）](#22-28-个默认情绪分类完整列表)
   - 2.3 [文件命名规范与存储路径](#23-文件命名规范与存储路径)
   - 2.4 [API 参考表](#24-api-参考表)
   - 2.5 [STscript 命令](#25-stscript-命令)
   - 2.6 [服务端 API](#26-服务端-api)
3. [图片生成（Image Generation）](#3-图片生成image-generation)
   - 3.1 [支持的提供商（24+ 个）](#31-支持的提供商24-个)
   - 3.2 [生成模式与触发机制](#32-生成模式与触发机制)
   - 3.3 [提示词架构](#33-提示词架构)
   - 3.4 [服务端 API 端点](#34-服务端-api-端点)
   - 3.5 [ComfyUI 工作流变量](#35-comfyui-工作流变量)
   - 3.6 [关键配置项](#36-关键配置项)
4. [图像输入与多模态视觉（Captioning & Vision）](#4-图像输入与多模态视觉captioning--vision)
   - 4.1 [机制总览：两种图像输入方式](#41-机制总览两种图像输入方式)
   - 4.2 [Captioning 支持后端](#42-captioning-支持后端)
   - 4.3 [Caption 集成模板](#43-caption-集成模板)
   - 4.4 [STscript 命令](#44-stscript-命令)
   - 4.5 [图像数据传输与自动压缩](#45-图像数据传输与自动压缩)
   - 4.6 [Screen Share 扩展](#46-screen-share-扩展)
5. [语音输入（STT - Speech Recognition）](#5-语音输入stt---speech-recognition)
   - 5.1 [支持的 STT 提供商](#51-支持的-stt-提供商)
   - 5.2 [消息处理模式](#52-消息处理模式)
   - 5.3 [Phrases Mapping（语音快捷命令）](#53-phrases-mapping语音快捷命令)
   - 5.4 [触发词过滤（Streaming STT 专属）](#54-触发词过滤streaming-stt-专属)
6. [Dynamic Audio（背景音乐与环境音）](#6-dynamic-audio背景音乐与环境音)
   - 6.1 [机制总览](#61-机制总览)
   - 6.2 [文件结构与命名规范](#62-文件结构与命名规范)
   - 6.3 [STscript 命令](#63-stscript-命令)
   - 6.4 [表情联动触发](#64-表情联动触发)
7. [本地实战：media_library.js 架构分析](#7-本地实战media_libraryjs-架构分析)
   - 7.1 [设计要点与 Tavern Helper 接口](#71-设计要点与-tavern-helper-接口)
   - 7.2 [资产类型系统](#72-资产类型系统)
   - 7.3 [图像处理规格](#73-图像处理规格)
   - 7.4 [图像裁剪比例逻辑](#74-图像裁剪比例逻辑)
   - 7.5 [公开 API 接口](#75-公开-api-接口)
   - 7.6 [meta 对象格式](#76-meta-对象格式)
8. [Talkinghead（历史参考，已废弃）](#8-talkinghead历史参考已废弃)
9. [完整可用范式代码](#9-完整可用范式代码)
10. [高频避坑](#10-高频避坑)
11. [悬案（不确定项 + 真机验证路径）](#11-悬案不确定项--真机验证路径)
12. [来源汇总](#12-来源汇总)

---

## 1 TTS（文字转语音）系统

### 1.1 机制总览与双队列架构

**核心结论**

ST 的 TTS 系统采用**提供商插件化架构**：中央 TTS Manager 负责文本解析、队列管理、播放控制，各 Provider 实现统一接口对接不同语音引擎。

**双队列机制：**

| 队列 | 作用 |
|------|------|
| `ttsJobQueue` | 等待语音合成的文本消息队列 |
| `audioJobQueue` | 已完成合成、等待播放的音频 blob 队列 |

两队列串行处理：`文本 → 语音合成 → audioJobQueue → 播放`

来源：[运行时源码] | 适用版本：ST v1.14.0+ | 置信度：**high**

---

### 1.2 支持的 TTS 提供商（14+ 个）

**核心结论**

**【校准说明】** 草稿仅列 15 个提供商，v1.14.0 `tts/index.js` `ttsProviders` 对象实际注册了 **25 个**（含废弃）。补全如下：

| 提供商（源码 key） | 费用 | 本地/云端 | 特性 |
|--------|------|-----------|------|
| AllTalk | 免费 | 本地 | 多引擎（XTTS/F5/Piper），RVC 变声，Narrator 模式，API 端口 7851 |
| Azure | 付费 | 云端 | Microsoft Edge 同款语音 |
| Chatterbox | — | 本地 | — |
| Coqui | （已废弃） | 本地 | 需要旧版 Extras API |
| CosyVoice (Unofficial) | — | 本地 | — |
| Edge | 免费 | 云端 | 通过 Azure |
| ElevenLabs | 付费 | 云端 | Stability/Similarity Boost 参数 |
| Electron Hub | — | 云端 | — |
| Google Translate | 免费 | 云端 | 非官方，依赖 Google Translate TTS |
| Google Gemini TTS | 付费 | 云端 | — |
| GSVI | — | 本地 | — |
| GPT-SoVITS-V2 (Unofficial) | — | 本地 | — |
| Kokoro | 免费 | 本地 | — |
| MiniMax | 付费 | 云端 | 中文优化，语音克隆，speed/volume/pitch 参数 |
| Novel | — | 云端 | — |
| OpenAI | 付费 | 云端 | — |
| OpenAI Compatible | — | 两者 | 自定义兼容 OpenAI 接口的 TTS 后端 |
| Pollinations | 免费 | 云端 | — |
| SBVits2 | — | 本地 | — |
| Silero | 免费 | 本地 | — |
| SpeechT5 | — | 本地 | — |
| System | 免费 | 本地 | 浏览器原生 TTS |
| TTS WebUI | — | 本地 | — |
| VITS | — | 本地 | — |
| XTTSv2 | 免费 | 本地 | 基于 Coqui，零样本克隆 |

来源：[运行时源码]（v1.14.0 `tts/index.js` `ttsProviders` 对象完整枚举） | 适用版本：ST v1.14.0 | 置信度：**high**

**避坑**：Coqui-TTS 依赖已停维护的 Extras API，新卡禁止基于此路径构建。

---

### 1.3 Provider 接口规范与 Manager 公开函数

**核心结论**

每个 Provider 必须实现的接口：

| 方法 | 签名 | 说明 |
|------|------|------|
| `loadSettings` | `(settings) => void` | 初始化 Provider 设置 |
| `fetchTtsVoiceObjects` | `() => Promise<VoiceObject[]>` | 获取可用语音列表 |
| `generateTts` | `(text, voiceId) => Promise<Response>` | 生成 TTS 音频，返回 HTTP Response |
| `processText` | `(text) => string`（可选） | 文本预处理/清洗 |

TTS Manager 公开函数：

| 函数 | 用途 |
|------|------|
| `registerTtsProvider()` | 注册自定义 TTS 提供商 |
| `playFullConversation()` | 播放整个对话历史 |
| `tts_preview()` | 预览单个语音样本 |
| `getPreviewString()` | 获取语言特定预览文本 |

Voice Map 结构：

```javascript
voiceMap = {
  "CharacterName": "voiceId",          // 正常映射
  "CharacterName": "[Default Voice]",  // 使用默认语音
  "CharacterName": "disabled"          // 禁用该角色 TTS
}
```

来源：[运行时源码] | 适用版本：ST v1.14.0+ | 置信度：**high**

**避坑**：Voice Map 必须配置，否则 TTS 不知道用哪个语音。`[Default Voice]` 和 `disabled` 是两个特殊标记值，不可写错。

---

### 1.4 TTS 事件系统

**【降级：high → low·孤证无源】校准说明**：本地 v1.14.0 运行时源码（`public/scripts/extensions/tts/index.js`）全文 grep 未找到 `TTS_JOB_STARTED`、`TTS_AUDIO_READY`、`TTS_JOB_COMPLETE` 任何一个事件类型定义；同文件也不在 `event_types` 上注册这三个名称。草稿标注"ST v1.17.0+ 新增"但手册适用范围声明为 v1.14.0+，属于超版本声明且无来源佐证。本节全部内容降为 low，禁止据此施工。

**⚠️ 待核实：** 若需在更新版本（v1.17.0+）使用 TTS 事件，须读取对应版本 `extensions/tts/index.js` 实际 `event_types` 枚举或查官方 changelog 确认。

v1.14.0 TTS 扩展实际监听的事件（运行时源码确认）：
- `event_types.MESSAGE_SWIPED` → 重置 TTS 播放
- `event_types.CHAT_CHANGED` → 切换聊天
- `event_types.MESSAGE_DELETED` → 消息删除
- `event_types.GROUP_UPDATED` → 群组更新
- `event_types.GENERATION_STARTED` / `GENERATION_ENDED` → 生成开始/结束
- `event_types.CHARACTER_MESSAGE_RENDERED` / `USER_MESSAGE_RENDERED` → 消息渲染后触发 TTS

（此列表为正面替换参考，来源：[运行时源码]，适用版本：ST v1.14.0）

下方代码块为草稿原始内容，**置信度：low**（超版本声明，v1.14.0 无此事件，来源：[推断]）：

```javascript
// ⚠️ 以下为 v1.17.0+ 才可能存在的事件，v1.14.0 不可用，禁止施工
const { eventSource, event_types } = getContext();

eventSource.on(event_types.TTS_JOB_STARTED, () => {
  // TTS 合成任务开始
});
eventSource.on(event_types.TTS_AUDIO_READY, (audioData) => {
  // 音频数据就绪，即将播放
});
eventSource.on(event_types.TTS_JOB_COMPLETE, () => {
  // 单个 TTS 任务完成
});
```

来源：[推断] | 适用版本：未知（v1.14.0 不存在，可能为 v1.17.0+） | 置信度：**low**

**悬案交叉引用**：事件回调的具体 payload 格式（消息 ID？文本内容？）见 11 节悬案 P1。

---

### 1.5 STscript 命令

**核心结论**

```
/speak voice="CharacterName" 要朗读的文字   # 触发 TTS（别名：/narrate, /tts）
/beep                                       # 播放通知音
```

`voice` 参数匹配 voice map 中的角色名（不是 voiceId），参数引号内为角色名，后续内容为要朗读的文本。

**【校准说明】** `/speak` 命令（含 `/narrate`、`/tts` 别名）在 v1.14.0 `tts/index.js` 中有完整 `SlashCommand.fromProps` 注册实现，源码已验证，升级为 high。`/beep` 命令官方文档有记载但 v1.14.0 本地源码未找到注册代码，保持单源孤证。

来源：`/speak` [运行时源码]；`/beep` [文档·单源] | 适用版本：ST v1.14.0+ | 置信度：`/speak` **high**；`/beep` **medium·单源孤证**

---

### 1.6 关键配置选项

**核心结论**

| 配置项 | 效果 |
|--------|------|
| "Only narrate 'quotes'" | 仅朗读引号内对话 |
| "Ignore \*text inside asterisks\*" | 忽略星号内的动作描述 |
| "Auto-generate" | 每条新消息自动触发 TTS |
| 正则表达式应用 | TTS 文本可先经正则处理后再合成 |

来源：[文档·多源] | 适用版本：ST v1.14.0+ | 置信度：**high**

---

### 1.7 TTS 与立绘同步

**核心结论**

当 TTS 播放时，系统调用：

```javascript
globalThis.vrmLipSync(audioBlob, character)
```

用于 VRM 模型口型同步（须安装对应 VRM 扩展）。

表情自动更新通过情绪分类事件驱动，**流式传输时**以 `STREAMING_UPDATE_INTERVAL = 10000ms` 为节流间隔，防止过频率 API 调用。

来源：[运行时源码] | 适用版本：ST v1.14.0+ | 置信度：**high**

**避坑**：表情分类在 streaming 期间以 10 秒为节流间隔，不是每 token 触发——这是性能保护措施，不是 bug。

---

## 2 表情立绘（Expression/Sprite）系统

### 2.1 机制总览

**核心结论**

ST 的表情系统通过 NLP 情绪分类驱动角色立绘切换，支持本地模型运行或调用 LLM API 进行分类。

分类器可配置为：

| 模式 | 模型 | 情绪数量 |
|------|------|----------|
| Local | `distilbert-base-uncased-go-emotions-onnx`（约 100MB） | 28 个 |
| Local | `bert-base-uncased-emotion-onnx`（via config.yaml） | 6 个 |
| Main API (LLM) | 主模型做分类，可加自定义 prompt | 可变 |
| WebLLM | 本地推理扩展 | — |
| Extras | （已废弃） | — |

来源：[文档·多源] + [运行时源码] | 适用版本：ST v1.14.0+ | 置信度：**high**

---

### 2.2 28 个默认情绪分类（完整列表）

**核心结论**

基于 GoEmotions 数据集，28 个情绪英文名如下（文件命名须与此完全一致）：

```
admiration    amusement    anger        annoyance    approval
caring        confusion    curiosity    desire       disappointment
disapproval   disgust      embarrassment excitement   fear
gratitude     grief        joy          love         nervousness
optimism      pride        realization  relief       remorse
sadness       surprise     neutral
```

来源：[运行时源码] | 适用版本：ST v1.14.0+ | 置信度：**high**

**避坑**：Dynamic Audio 的 BGM 情绪命名必须与上表完全一致，大小写敏感，拼写错误将导致静默失败。

---

### 2.3 文件命名规范与存储路径

**核心结论**

```
# 基础格式
joy.png

# 多张同表情（随机选一张）
joy-1.png
joy-2.png
joy.expressive.png    # 点分隔符也支持

# 支持格式：PNG（推荐透明背景）、WebP、GIF
```

存储路径：`DATA_ROOT/characters/{character_name}/` 下的对应文件

来源：[文档·多源] | 适用版本：ST v1.14.0+ | 置信度：**high**

---

### 2.4 API 参考表

**核心结论**

| 函数 | 说明 |
|------|------|
| `sendExpressionCall(folder, expression, options)` | 主入口，更新角色表情 |
| `chooseSpriteForExpression(folder, expression)` | 从缓存选择精灵图资源 |
| `setImage(imgElement, spriteFile.imageSrc)` | 更新 DOM 中的精灵图 |
| `updateVisualNovelMode()` | 更新视觉小说模式多角色显示 |
| `visualNovelUpdateLayers()` | 管理 z-index 和位置 |
| `lastExpression` | 对象，按角色记录最近表情 |

来源：[运行时源码] | 适用版本：ST v1.14.0+ | 置信度：**high**

**悬案交叉引用**：`sendExpressionCall()` 能否从角色卡世界书脚本直接调用见 11 节悬案 P2。

---

### 2.5 STscript 命令

**核心结论**

```
/expression-set (name)           # 手动设置表情
/expression-set type=sprite      # 指定特定精灵图
/emote (sprite)                  # 模糊匹配表情名
/costume (folder_name)           # 覆盖精灵文件夹
/costume \subfolder              # 前置反斜杠 = 子目录
```

来源：[文档·多源] | 适用版本：ST v1.14.0+ | 置信度：**high**

**悬案交叉引用**：`/expression-set` 在 Group Chat 多角色场景下的目标角色确定逻辑见 11 节悬案 P7。

---

### 2.6 服务端 API

**核心结论**

```
POST /api/sprites/upload-zip     # 批量上传精灵包
```

来源：[运行时源码] | 适用版本：ST v1.14.0+ | 置信度：**high**

---

## 3 图片生成（Image Generation）

### 3.1 支持的提供商（24+ 个）

**核心结论**

**本地提供商：** ComfyUI、AUTOMATIC1111（SD WebUI）、SD.Next、Draw Things、stable-diffusion.cpp

**云端提供商：** OpenAI DALL-E、Stability AI、Black Forest Labs（FLUX）、Google Vertex AI、HuggingFace Serverless、Stable Horde、NovelAI 等

来源：[文档·多源] | 适用版本：ST v1.14.0+ | 置信度：**high**

---

### 3.2 生成模式与触发机制

**核心结论**

**【校准说明】** 草稿描述的模式 ID 含混/错误。v1.14.0 `stable-diffusion/index.js` `generationMode` 对象完整枚举如下：

| 模式 ID | 常量名 | 说明 |
|---------|--------|------|
| -2 | TOOL | Function Tool 提示词描述 |
| -1 | MESSAGE | Chat Message 模板 |
| 0 | CHARACTER | 角色立绘 |
| 1 | USER | 用户人设 |
| 2 | SCENARIO | 场景描述 |
| 3 | RAW\_LAST | 最近消息（原始） |
| 4 | NOW | 最近消息 |
| 5 | FACE | 面部特写 |
| 6 | FREE | 自由模式（用户自定义提示词） |
| 7 | BACKGROUND | 背景图 |
| 8 | CHARACTER\_MULTIMODAL | 角色（多模态） |
| 9 | USER\_MULTIMODAL | 用户（多模态） |
| 10 | FACE\_MULTIMODAL | 面部（多模态） |
| 11 | FREE\_EXTENDED | 自由模式（LLM 扩展） |

草稿原"5=用户自定义主体"有误（5=FACE 面部特写，6=FREE 才是用户自定义）。

来源：[运行时源码]（v1.14.0 `stable-diffusion/index.js`） | 置信度：**high**

**三种触发机制：**

1. **魔法棒菜单（Wand Menu）**：手动从 UI 触发
2. **Slash 命令**：`/sd [argument]`，`/imagine (prompt)`
3. **Interactive Mode**：自动识别自然语言模式：
   ```
   动词: send|mail|imagine|generate|make|create|draw|paint|render|show
   名词: pic|picture|image|drawing|painting|photo|photograph
   ```

来源：[文档·多源] | 适用版本：ST v1.14.0+ | 置信度：**high**

---

### 3.3 提示词架构

**核心结论**

```
Common Prefix（通用前缀）
+ Character-Specific（角色专属，仅 1v1 对话）
+ Negative Prompts（负向提示）
+ {{charPrefix}} 宏（自由模式中嵌入角色数据）
```

来源：[文档·多源] | 适用版本：ST v1.14.0+ | 置信度：**high**

---

### 3.4 服务端 API 端点

**核心结论**

核心端点：

```
POST /api/sd/ping             # 健康检查
GET  /api/sd/models           # 获取模型列表（checkpoint）
GET  /api/sd/samplers         # 采样器元数据
GET  /api/sd/upscalers        # 放大模型
GET  /api/sd/vaes             # VAE 模型
GET  /api/sd/schedulers       # 调度器选项
POST /sdapi/v1/txt2img        # 实际生成请求（代理至 SD WebUI）
```

多提供商图像字幕路由：

```
/api/openai/caption-image
/api/google/caption-image
/api/anthropic/caption-image
/api/backends/text-completions/ollama/caption-image
```

来源：[运行时源码] | 适用版本：ST v1.14.0+ | 置信度：**high**

---

### 3.5 ComfyUI 工作流变量

**核心结论**

```json
{
  "%prompt%": "正向提示词",
  "%negative_prompt%": "负向提示词",
  "%model%": "模型文件名",
  "%sampler%": "采样器",
  "%steps%": "步数",
  "%seed%": "随机种子",
  "%char_avatar%": "角色头像（base64）",
  "%user_avatar%": "用户头像（base64）"
}
```

来源：[文档·多源] | 适用版本：ST v1.14.0+（ComfyUI 集成版） | 置信度：**high**

**悬案交叉引用**：`%char_avatar%` 的 base64 编码格式（有无 data URL 头？JPEG 还是 WebP？）见 11 节悬案 P6。

---

### 3.6 关键配置项

**核心结论**

**【校准说明】** `sd_minimal_prompt_processing` 在 v1.14.0 `stable-diffusion/index.js` 中**未找到**此配置项，疑为文档推导或未来版本特性，已降级处理。

| 配置项 | 来源 | 说明 |
|--------|------|------|
| `sd_refine_mode` | [运行时源码] | 手动编辑提示词后再生成 |
| `sd_multimodal_captioning` | [运行时源码] | 基于头像生成提示词 |
| `sd_free_extend` | [运行时源码] | LLM 扩展主题 |
| `sd_minimal_prompt_processing` | [推断] **⚠️ 存疑** | 保留 LLM 结构化输出——v1.14.0 源码未验证 |
| "Send inline images" | [文档·多源] | 让多模态 LLM 在对话中"看见"生成图像（默认关闭） |

来源：前四项 [运行时源码] 已验证部分置信度 **high**；`sd_minimal_prompt_processing` 降级为 **medium·推导未验证**（v1.14.0 未找到，见 [推断]） | 适用版本：ST v1.14.0+

**避坑**：`Send inline images` 默认关闭，必须手动在 Chat Completion 设置中开启，且后端模型必须是视觉模型，否则图片会被忽略。

---

## 4 图像输入与多模态视觉（Captioning & Vision）

### 4.1 机制总览：两种图像输入方式

**核心结论**

ST 支持两种图像输入方式：

| 方式 | 机制 | 适用场景 |
|------|------|----------|
| Image Captioning | 生成文字描述再注入对话上下文 | 不支持视觉的文本模型 |
| Multimodal Chat（Send Inline Images） | 图像直接作为消息内容发给视觉模型 | GPT-4o、Claude 3.5 等视觉模型 |

来源：[文档·多源] | 适用版本：ST v1.14.0+ | 置信度：**high**

---

### 4.2 Captioning 支持后端

**核心结论**

**零配置本地：** transformers.js（ST 服务端内置，无需额外安装，推荐首选）

**本地大模型后端：** Ollama、llama.cpp、KoboldCpp、Text Generation WebUI、vLLM

**云端 API：** OpenAI、Anthropic、Google、MistralAI、Groq 等 15+ 家

来源：[文档·多源] | 适用版本：ST v1.14.0+ | 置信度：**high**

---

### 4.3 Caption 集成模板

**核心结论**

默认注入模板：

```
[{{user}} sends {{char}} a picture that contains: {{caption}}]
```

`{{caption}}` 宏被替换为图像描述，注入到对话上下文中。

来源：[文档·多源] | 适用版本：ST v1.14.0+ | 置信度：**high**

---

### 4.4 STscript 命令

**核心结论**

```
/caption [quiet=true|false]? [mesId=number]? [prompt]
  # quiet: 静默处理不显示弹窗
  # mesId: 对已有消息重新字幕
  # prompt: 自定义字幕提示词

/editcaption mesId mediaIndex   # 重新字幕聊天历史中的图像
/caption-prompt prompt           # 配置默认字幕提示词
```

来源：[运行时源码] | 适用版本：ST v1.14.0+ | 置信度：**high**

---

### 4.5 图像数据传输与自动压缩

**核心结论**

图像附件存储在 `ChatMessage.extra.media` 数组中，每条消息可有多个附件。

自动压缩管线（via `shared.js`）：
- 自动符合 API payload 上限（如 OpenRouter 的 2MB 限制）
- 格式转换（Oobabooga 等后端需要 JPEG）

来源：[运行时源码] | 适用版本：ST v1.14.0+ | 置信度：**high**

---

### 4.6 Screen Share 扩展

**核心结论**

- 通过"魔法棒菜单"初始化屏幕共享
- 每条后续用户消息自动附带屏幕截图
- 图像不保存到本地，直接发送到 API 提供商
- 需要 `Send inline images` 已开启

来源：[文档·单源] | 适用版本：ST v1.14.0+ | 置信度：**medium·单源孤证**

**避坑**：依赖浏览器 ImageCapture API，**Firefox 不支持**，Safari 支持有限，主要在 Chrome 上可靠运行。

---

## 5 语音输入（STT - Speech Recognition）

### 5.1 支持的 STT 提供商

**核心结论**

| 类型 | 提供商 |
|------|--------|
| 浏览器原生 | Browser Web Speech API |
| 云端 API | OpenAI、MistralAI、Groq、Chutes、Z.AI、ElevenLabs |
| 本地（已废弃） | Vosk、Whisper（需旧版 Extras API + ffmpeg） |

Extras 已于 **2024 年 4 月停止维护**，Vosk/Whisper 旧路径不再推荐，新卡禁止依赖。

来源：[文档·多源] | 适用版本：ST v1.14.0+ | 置信度：**high**

---

### 5.2 消息处理模式

**核心结论**

| 模式 | 行为 |
|------|------|
| Append | 追加到当前输入框文字末尾 |
| Replace | 替换当前输入框全部文字 |
| Auto send | 识别完成后自动发送（免手动点击） |

来源：[文档·多源] | 适用版本：ST v1.14.0+ | 置信度：**high**

---

### 5.3 Phrases Mapping（语音快捷命令）

**核心结论**

```
# 格式: "语音词组 = /slash命令"
command delete = /del2      # 说"command delete"执行删除
```

来源：[文档·多源] | 适用版本：ST v1.14.0+ | 置信度：**high**

---

### 5.4 触发词过滤（Streaming STT 专属）

**核心结论**

仅以指定触发词开头的语音消息才会被发送，防止噪音/无意义语音意外触发。此功能仅在 Streaming STT 模式下有效。

**【降级：high → medium·单源孤证】** 来源仅为单一官方文档页，且 Streaming STT（Vosk/Whisper 路径）本身已废弃（依赖 2024-04 停维的 Extras API）。v1.14.0 本地扩展目录无 speech-recognition 子目录，无法从运行时源码验证。功能本身来源可信，但适用范围存疑。

来源：[文档·单源] | 适用版本：Streaming STT（已废弃路径） | 置信度：**medium·单源孤证**

---

## 6 Dynamic Audio（背景音乐与环境音）

### 6.1 机制总览

**核心结论**

Extension-Audio（即 Dynamic Audio 扩展）为聊天添加沉浸式 BGM 和环境音效，通过角色情绪分类事件驱动音乐自动切换。

来源：[文档·多源] | 适用版本：ST v1.14.0+ | 置信度：**high**

---

### 6.2 文件结构与命名规范

**核心结论**

```
# 角色专属 BGM
\SillyTavern\data\<user-handle>\characters\[CharacterName]\bgm\
  anger_0.mp3
  anger_1.mp3    # 同表情多曲目，随机选一
  joy_0.mp3

# 全局默认 BGM
\SillyTavern\data\<user-handle>\assets\bgm\

# 环境音（与背景图片对应）
\SillyTavern\data\<user-handle>\assets\ambient\
  bedroom-clean.mp3   # 对应背景图 "bedroom clean.jpg"（空格→连字符）
```

支持格式：MP3、OGG、WAV

来源：[文档·多源] | 适用版本：ST v1.14.0+ | 置信度：**high**

**避坑**：情绪命名必须与 2.2 节表中 28 个英文名完全一致，大小写敏感，拼写错误导致静默失败。

---

### 6.3 STscript 命令

**核心结论**

```
/music (filename)    # 强制切换 BGM（按文件名）
/ambient (filename)  # 强制切换环境音（按文件名）
```

来源：[文档·多源] | 适用版本：ST v1.14.0+ | 置信度：**high**

**悬案交叉引用**：`/music` 命令是否支持播放外部 URL 见 11 节悬案 P3。

---

### 6.4 表情联动触发

**核心结论**

开启"Expression BGM Switch"后，BGM 随角色情绪自动切换，文件命名规则：`[emotion]_[number].mp3`，直接对应 28 个情绪分类名称。同一情绪多个曲目时随机选一播放。

来源：[文档·多源] | 适用版本：ST v1.14.0+ | 置信度：**high**

---

## 7 本地实战：media_library.js 架构分析

> **来源**：`C:/Users/Administrator/OneDrive/ST-/角色卡工作区/星月/星月 2.5.0/components/media_library.js`（本地生产源码）

### 7.1 设计要点与 Tavern Helper 接口

**核心结论**

这是角色卡内置**自管理图像资产库**，不依赖 ST 内置表情系统，通过 Tavern Helper 的变量接口自管理持久化存储：

```javascript
// 与 Tavern Helper 的接口对接（关键！）
function scriptVariableOption() {
  return { type: 'script', script_id: SCRIPT_ID };  // 'crossed-zone-media-library'
}

function readRawLibrary() {
  if (typeof getVariables !== 'function') return defaultLibrary();
  return getVariables(scriptVariableOption()) || {};
}

function writeRawLibrary(value) {
  if (typeof replaceVariables !== 'function') throw new Error('酒馆助手变量接口不可用');
  replaceVariables(value, scriptVariableOption());
}
```

**关键发现**：`getVariables` / `replaceVariables` 是 **Tavern Helper 扩展暴露的全局函数**，通过 `{ type: 'script', script_id: string }` option 对象区分变量命名空间。这是本角色卡访问持久化变量的核心机制。

来源：[运行时源码] | 适用版本：星月 2.5.0 + Tavern Helper | 置信度：**high**

**避坑**：`getVariables` / `replaceVariables` 是 Tavern Helper 扩展接口，不是 ST 原生接口。卡内脚本调用前必须检查 `typeof getVariables !== 'function'`，否则在未安装 Tavern Helper 的环境中会报错。

---

### 7.2 资产类型系统

**核心结论**

```javascript
const ASSET_TYPES = ['bond', 'mecha', 'mothership', 'self'];
const ASSET_SLOTS = ['avatar', 'portrait'];
const BOND_VARIANTS = ['normal', 'nude'];

// 资产键格式：type|encodedName|slot|variant
// 示例："bond|%E6%98%9F%E6%9C%88|portrait|normal"
```

来源：[运行时源码] | 适用版本：星月 2.5.0 | 置信度：**high**

---

### 7.3 图像处理规格

**核心结论**

| 场景 | 最大边 | 压缩格式 | 质量 |
|------|--------|----------|------|
| avatar（头像） | 256px | WebP 优先 | 0.88 |
| portrait（立绘） | 1200px | WebP 优先 | 0.86 |
| GIF | 不压缩（保留动画帧） | 原格式 | — |
| GIF 上限 | — | 3MB / 4MB dataUrl | 拒绝导入 |

**整库大小上限：** 24MB（超出则拒绝写入并提示导出备份）

来源：[运行时源码] | 适用版本：星月 2.5.0 | 置信度：**high**

**避坑**：媒体库整库大小上限 24MB；单张 data URL 上限 4MB；GIF 额外限制 3MB。超出需清理未使用资产或改用外链 URL（`external: true`）。

**避坑**：GIF 图片不进行裁剪/压缩（保留动画帧），但超大 GIF 会被拒绝导入——这是主动防卡死措施，移植时需保留此逻辑。

---

### 7.4 图像裁剪比例逻辑

**核心结论**

```javascript
// mothership（居所）：两种横幅比例（弹窗让用户选）
{ label: '横幅 21:9', ratio: 21/9 }
{ label: '横幅 16:9', ratio: 16/9 }

// bond/mecha/self portrait：固定竖屏
{ label: '竖屏 9:16', ratio: 9/16 }

// avatar：固定正方形
{ label: '方形头像', ratio: 1 }
```

来源：[运行时源码] | 适用版本：星月 2.5.0 | 置信度：**high**

---

### 7.5 公开 API 接口

**核心结论**

```javascript
window.CrossedZoneMediaLibrary = {
  getAsset(meta),                 // 查找资产（bond 支持 variant fallback）
  getExactAsset(meta),            // 精确查找
  upsertAsset(meta, source),      // 插入/更新资产
  removeAsset(meta),              // 删除资产
  importLocalAsset(meta, file),   // 从本地文件导入
  requestLocalImport(meta),       // 弹出文件选择框
  importUrlAsset(url, meta),      // 从 URL 导入（支持 data URL 和 http URL）
  requestUrlImport(meta),         // 弹出 URL 输入框
  exportLibrary(),                // 导出整库 JSON
  importLibrary(input),           // 合并导入 JSON（不覆盖现有）
  cleanupUnused(activeNames),     // 清理未使用资产
  getStatus(),                    // 获取库状态和摘要
}

// 状态变更事件
window.addEventListener('crossed-zone-media-library-updated', (e) => {
  // e.detail = { version, ready, lastError, lastAction, libraryType, summary }
});
```

来源：[运行时源码] | 适用版本：星月 2.5.0 | 置信度：**high**

**避坑**：`importUrlAsset` 失败时（CORS 被拒）会自动降级为外链模式（`external: true`），不会报错，但导出库时会有 `warnings` 列表，外链资产在离线场景无效。

---

### 7.6 meta 对象格式

**核心结论**

```javascript
const meta = {
  type: 'bond' | 'mecha' | 'mothership' | 'self',
  slot: 'avatar' | 'portrait',
  name: '角色名称',                    // 必填，非空
  variant: 'normal' | 'nude',          // 仅 bond 有效
};
```

来源：[运行时源码] | 适用版本：星月 2.5.0 | 置信度：**high**

---

## 8 Talkinghead（历史参考，已废弃）

**核心结论**

ST v1.12.13 之后已放弃支持，此章仅作历史记录。

| 属性 | 值 |
|------|-----|
| 废弃版本 | ST v1.12.13 |
| 技术原理 | 基于 Talking Head Anime 3，从单张静态图片生成 Live2D 风格动作 |
| 输入要求 | 512×512 PNG，带 Alpha 通道 |
| 性能 | CPU 约 1FPS，RTX3060 约 9–10FPS |
| VRM 支持 | 不支持 |

`animator.json` 可配置字段（历史参考）：`target_fps`、`pose_interpolator_step`、`blink_interval_min/max`、`talking_fps`、`sway_morphs`、`breathing_cycle_duration`、`postprocessor_chain`。

来源：[文档·多源] | 置信度：**high**（废弃结论）

**悬案交叉引用**：是否有社区 fork 维持在 ST v1.17+ 的兼容性见 11 节悬案 P8。

---

## 9 完整可用范式代码

### 9.1 从世界书/卡内脚本监听 TTS 事件

**【降级：整段 high → low / medium】**
- `TTS_JOB_STARTED` / `TTS_JOB_COMPLETE`：v1.14.0 源码**不存在**（见 1.4 节降级说明），本范式代码在 v1.14.0 下无效。
- `window.setSpriteSlashCommand`：v1.14.0 `expressions/index.js` 中 `setSpriteSlashCommand` 是模块内部函数，**未挂载到 `window`**（grep 确认无 `window.setSpriteSlashCommand` 赋值），此调用在任何版本均无效。

正确替代路径：监听 `CHARACTER_MESSAGE_RENDERED` / `GENERATION_ENDED` 事件，用 `/expression-set` STscript 命令或 ST 官方 `/api/sprites` 接口控制表情。

以下代码块**仅供参考结构，整体不可用，禁止施工**（置信度：**low**）：

```javascript
// ⚠️ 以下代码在 v1.14.0 中无效：TTS_JOB_STARTED 不存在，setSpriteSlashCommand 未全局暴露
(function() {
  if (typeof getContext !== 'function') return;

  const ctx = getContext();
  const { eventSource, event_types } = ctx;

  // TTS 开始时切换到"speaking"表情
  eventSource.on(event_types.TTS_JOB_STARTED, () => {
    if (window.setSpriteSlashCommand) {
      window.setSpriteSlashCommand({ expression: 'joy' });
    }
  });

  // TTS 结束时恢复
  eventSource.on(event_types.TTS_JOB_COMPLETE, () => {
    if (window.setSpriteSlashCommand) {
      window.setSpriteSlashCommand({ expression: 'neutral' });
    }
  });
})();
```

来源：[推断] | 适用版本：不适用（v1.14.0） | 置信度：**low**（TTS 事件不存在 + `setSpriteSlashCommand` 未全局暴露，双重失效）

---

### 9.2 STscript 中的音频媒体完整范式

```
// 语音朗读
/speak voice="星月" 这里是需要朗读的文字 |
/speak voice="旁白" *这里是旁白内容* |

// 图片生成（自动选择模式）
/imagine a beautiful anime girl with silver hair |

// 强制表情
/expression-set joy |
/emote surprise |

// 切换立绘组
/costume \school_uniform |

// BGM 控制
/music battle_intense |
/ambient school_classroom |

// 多行 STscript 条件 BGM
/setvar key=mood value=happy |
/if left={{getvar::mood}} rule=eq right=happy else={: /ambient peaceful :} {: /music joy_0 :}
```

来源：[文档·多源] | 置信度：**high**

---

### 9.3 使用 CrossedZoneMediaLibrary 读取立绘（本地生产代码）

```javascript
// 读取羁绊角色立绘
const asset = window.CrossedZoneMediaLibrary?.getAsset({
  type: 'bond',
  slot: 'portrait',
  name: '角色名',
  variant: 'normal'
});

if (asset?.src) {
  imgElement.src = asset.src;  // dataUrl 或外链 URL
}

// 监听库更新
window.addEventListener('crossed-zone-media-library-updated', (e) => {
  const { summary, ready, lastError } = e.detail;
  console.log(`媒体库：${summary.total}个资产，${summary.embedded}个内嵌`);
});
```

来源：[运行时源码] | 适用版本：星月 2.5.0 | 置信度：**high**

---

### 9.4 Caption + 视觉模型完整链路

```
# 用户发送图片 → 自动字幕 → 注入对话 → LLM"看见"图片
# 步骤一：在 Extensions > Captioning 选择后端（推荐：transformers.js 零配置）
# 步骤二：在 API Connections 配置视觉模型（如 GPT-4o、Claude 3.5 Sonnet）
# 步骤三：在 Chat Completion 设置中开启 "Send inline images"
# 步骤四：用户发送含图片的消息后，LLM 直接看到图像内容（非 caption 文字）

# 手动字幕
/caption quiet=true a detailed description of this image
```

来源：[文档·多源] | 置信度：**high**

---

## 10 高频避坑

**[high][文档·多源]** Extras API 已于 2024 年 4 月停止维护，**不要再基于 Extras 构建新卡功能**（Coqui-TTS、Vosk、Whisper 旧路径、talkinghead 均已死亡）。

**[high][运行时源码]** `getVariables` / `replaceVariables` 是 Tavern Helper 扩展接口，不是 ST 原生接口。卡内脚本调用前必须检查 `typeof getVariables !== 'function'`，否则在未安装 Tavern Helper 的环境中会报错。

**[high][运行时源码]** 媒体库整库大小上限 24MB；单张 data URL 上限 4MB；GIF 额外限制 3MB。超出需清理未使用资产或改用外链 URL（`external: true`）。

**[high][运行时源码]** GIF 图片不进行裁剪/压缩（保留动画帧），但超大 GIF 会被拒绝导入——这是主动防卡死措施，移植时须保留此逻辑。

**[high][文档·多源]** TTS Voice Map 必须配置，否则 TTS 不知道用哪个语音。`[Default Voice]` 和 `disabled` 是两个特殊标记值。

**[high][运行时源码]** TTS 表情分类在 streaming 期间以 10 秒为节流间隔，不是每 token 触发——这是性能保护，不是 bug。

**[high][文档·多源]** `Send inline images` 默认关闭，必须手动在 Chat Completion 设置中开启，且后端模型必须是视觉模型，否则图片会被忽略。

**[medium·单源孤证][文档·单源]** ScreenShare 扩展依赖浏览器 ImageCapture API，**Firefox 不支持**，Safari 支持有限，主要在 Chrome 上可靠运行。

**[high][文档·多源]** Dynamic Audio 的 BGM 情绪命名必须与表情系统的 28 个情绪英文名完全一致（如 `anger_0.mp3`），大小写敏感，拼写错误将导致静默失败。【提升：草稿标 medium·推导未验证，但官方文档 dynamic-audio 页面给出了明确示例（`anger_0.mp3`、`joy_0.mp3`）且属多源文档，非推导，提升至 high。】

**[high][运行时源码]** `importUrlAsset` 失败时（CORS 被拒）会自动降级为外链模式（`external: true`），不会报错，但导出库时会有 `warnings` 列表，外链资产在离线场景无效。

---

## 11 悬案（不确定项 + 真机验证路径）

以下条目置信度均为 **low**（孤证无源或纯推导），**禁止据此直接施工**。

| 编号 | 悬案 | 当前置信度 | 真机验证路径 |
|------|------|-----------|-------------|
| P1 | `event_types.TTS_JOB_STARTED` 事件是否存在、回调的具体 payload 格式（消息 ID？文本内容？还是空参数？）——**注意：v1.14.0 源码中此事件不存在，悬案需在更新版本验证** | low | 在 v1.17.0+ 版本浏览器 console：`eventSource.on(event_types.TTS_JOB_STARTED, (...args) => console.log(args))` 实测；v1.14.0 直接排除 |
| P2 | `sendExpressionCall()` 能否从角色卡世界书脚本直接调用（还是仅限 ST 扩展内部？） | low | 注入 `window.sendExpressionCall = sendExpressionCall` 后在卡内脚本测试 |
| P3 | Dynamic Audio 的 `/music` 命令是否支持播放外部 URL（而非仅服务端文件名） | low | ST 控制台执行 `/music https://example.com/test.mp3` 观察行为 |
| P4 | AllTalk Narrator 模式是否能区分不同角色的引号文字（非全局叙述） | medium·推导未验证 | AllTalk 文档 wiki 深读 + 实测 Narrator 模式下多角色场景 |
| P5 | `ext_settings.tts.voiceMap` 对象在运行时是否可被角色卡 JS 直接读写以动态改变语音 | low | `getContext().extensionSettings?.tts?.voiceMap` 在控制台检查，尝试修改后触发 TTS |
| P6 | ComfyUI 工作流中 `%char_avatar%` 的 base64 编码格式（有无 `data:image/...` 头？JPEG 还是 WebP？） | medium·推导未验证 | 抓 `/api/sd/` 网络请求 payload，检查 `%char_avatar%` 字段内容 |
| P7 | STscript 中 `/expression-set` 在 Group Chat（多角色）场景下的目标角色确定逻辑 | medium·推导未验证 | Group Chat 环境中实测 `/expression-set type=sprite`，观察哪个角色表情变化 |
| P8 | Talkinghead 废弃后是否有社区 fork 维持在 ST v1.17+ 的兼容性 | low | GitHub 搜索 `sillytavern talkinghead 1.17` |

---

## 12 来源汇总

**官方文档：**
- https://docs.sillytavern.app/extensions/tts/
- https://docs.sillytavern.app/extensions/speech-recognition/
- https://docs.sillytavern.app/extensions/expression-images/
- https://docs.sillytavern.app/extensions/stable-diffusion/
- https://docs.sillytavern.app/extensions/captioning/
- https://docs.sillytavern.app/extensions/dynamic-audio/
- https://docs.sillytavern.app/extensions/talkinghead/
- https://docs.sillytavern.app/for-contributors/writing-extensions/
- https://docs.sillytavern.app/usage/st-script/
- https://docs.sillytavern.app/extensions/alltalk/
- https://docs.sillytavern.app/extensions/minimaxtts/

**深度研究源：**
- https://deepwiki.com/SillyTavern/SillyTavern/8.6-text-to-speech-extensions
- https://deepwiki.com/SillyTavern/SillyTavern/8.3-character-expressions-system
- https://deepwiki.com/SillyTavern/SillyTavern/8.2-image-generation-extensions
- https://deepwiki.com/SillyTavern/SillyTavern/8.5-image-captioning-and-multimodal-ai

**GitHub 扩展仓库：**
- https://github.com/SillyTavern/Extension-Audio
- https://github.com/SillyTavern/Extension-ScriptEvents
- https://github.com/SillyTavern/Extension-ScreenShare
- https://github.com/SillyTavern/SillyTavern-Extras/blob/main/talkinghead/README.md

**本地生产文件：**
- `C:/Users/Administrator/OneDrive/ST-/角色卡工作区/星月/星月 2.5.0/components/media_library.js`
